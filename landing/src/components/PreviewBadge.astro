---
/** Shared product-status badge for capability previews across landing sections. */
interface Props {
  label?: string;
  execution?: boolean;
}

const { label = 'Private preview', execution = false } = Astro.props;
---

<span
  class="preview-badge"
  data-private-preview
  data-execution-preview={execution ? '' : undefined}
>{label}</span>
