---
import type { Card as CardData } from '../content/site';
import Card from './Card.astro';

interface Props {
  cards: CardData[];
}

const { cards } = Astro.props;
---

<div class="cards">
  {cards.map((card) => <Card {...card} />)}
</div>
