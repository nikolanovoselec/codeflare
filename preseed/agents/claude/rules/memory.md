# Memory and Vault routing

For requests to check, search, load, or recall memory, query the merged global graph with available Graphify tools. Current source outranks remembered context.

Before reading or writing `/home/user/Vault/`, load `vault-operations`. For requests to take, save, write, or remember a note, load `vault-note-capture`.

Automated capture and Vault change detection run on resumed-session tails and every 20 real user messages. Submit an emitted public extraction request exactly once; otherwise do not dispatch, wait for, or poll those runtime-owned workflows.
