# Frontend component routing

When visual frontend work introduces or refactors repeated component structure, invoke `frontend-components` for component ownership, composition, state boundaries, incumbent-system reuse, and behavioral verification.

`design` routes art direction. `frontend-design` owns web visual direction. `frontend-patterns` owns measured React and Next.js performance. Do not duplicate their methodology here.
