# Capability route

For broad Codeflare capability/onboarding questions (including “What can you do?”), tours, or numbered replies, read `~/.pi/agent/skills/codeflare-capabilities/SKILL.md` directly; never use `capability`. Otherwise use task context.
