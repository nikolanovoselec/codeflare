# Documentation Coverage Ledger

## 2026-07-02 — Whole-tree Pass 1 (per-element word-budget) audit, surfaced incidentally during PR #601 review

Not a diff-scoped finding: doc-updater's Pass 1 (per-element budgets) runs whole-tree
regardless of what a given PR touched, and surfaced this while reviewing PR #601's
REQ-VAULT-008/017/024 citation-repoint diff. None of this content was authored or
touched by PR #601 — it is pre-existing debt across the documentation lane. Not
gating PR #601 (per doc-updater's own recommendation, confirmed by the user
2026-07-02); tracked here so it is not silently lost. Owner: whoever runs the next
`/sdd clean --all` documentation sweep.

### Historical aggregate counts (post PR #601, superseded)

The original audit recorded 119 list-item findings, 11 table-cell findings, and
21 paragraph findings. Those counts are provenance only; the 2026-07-03 cleanup below
supersedes them.

Final Pass 1 state after cleanup: `0` over-budget list items, table cells, paragraphs,
code snippets, or heading-depth findings under `documentation/`.

### Former worst offenders (resolved)

The previously listed vault bootstrap/native-worker rows and ADR ledger bullet pattern
were resolved by extracting prose out of oversized elements and preserving facts in
nearby body text.

### 2026-07-03 partial cleanup — `/sdd clean --scope=all --unleashed`

Resolved the recorded Pass 1 findings in `documentation/lanes/api-reference.md`,
`documentation/lanes/security.md`, and `documentation/lanes/troubleshooting.md` by
splitting overlong table cells, bullets, and paragraphs into short entries plus nearby
prose/notes; facts and links were preserved.

Also fixed focused, low-risk ADR list-item overages by splitting long bullets into short
bullets plus adjacent indented paragraphs under the same ADR. Touched AD52, AD53, AD74,
AD83, AD86, AD87, AD89, and AD91. The former single-largest ADR finding
(`documentation/decisions/README.md:1839`, the 378-word AD91 bounded-driver bullet) is
resolved.

Follow-up validation cleanup cleared the remaining Pass 1 debt across ADRs and lane
docs. The final per-element scanner reports `0` over-budget cells, list items,
paragraphs, code snippets, or heading-depth findings under `documentation/`.
The previously listed manual recheck counts are closed; largest resolved offenders
included the AD87 browser-run consequence, AD84 vault green-click bullets, and
AD44/AD92 ADR bullets.

Validated files include the ADR ledger plus architecture, authentication, billing,
configuration, container, mobile, preseed, storage-and-sync, troubleshooting, and
vault lane docs. Facts and REQ/ADR links were preserved while long elements were
split into short rows, bullets, paragraphs, or trimmed diagrams.

### JUDGMENT resolved — Pass 13 repeated labels are a repeatable-field convention

- `documentation/decisions/README.md`: the bolded label `**Alternative considered:**`
  repeats 2-3 times within a single ADR section in 11 ADRs (AD53, AD54, AD55, AD56,
  AD58, AD59, AD61, AD62, AD63, AD64, AD66). Each occurrence describes a *different*
  alternative (different values, not the same fact restated) — reads as a deliberate
  repeatable-field convention, not the duplicate-fact defect the rule targets.
- `documentation/lanes/api-reference.md`: `**Source:**` repeats 6 times in the
  "Configuration Steps" section, same repeatable-list pattern.

Decision: accept this as an intended repeatable-field convention, not duplicate-fact
drift. Future enforcement should flag repeated bold labels only when they restate the
same fact; identical labels are acceptable when each occurrence records a different
alternative, source, value, or step.

---

## 2026-07-01 — PR #601 (develop→main), delta review ec738ae5..1465edc

Scope: incremental diff only, restricted to `documentation/`. Files touched:
`documentation/decisions/README.md`, `documentation/lanes/troubleshooting.md`.

### Verified: prior findings resolved correctly

1. AD91 state-shape literal (`documentation/decisions/README.md:1829`) now includes
   `halted?` alongside `total?`/`processed?`. Matches `src/lib/r2-regime-state.ts:59`.
2. The bloated "Revised 2026-07-01" note was lifted out of the AD91 "Backend gate +
   container drain" bullet into a standalone paragraph after the "Dual-regime reads +
   self-heal" bullet (`documentation/decisions/README.md:1835`). Word count: 98 words
   (budget 120). The bullet itself is back to a single coherent sentence pair with no
   dangling reference.
3. `documentation/lanes/troubleshooting.md`'s **Cause** paragraph (line 117) was split
   at the sentence boundary about the button/poll UI behavior. Neither half exceeds
   budget: first half 87 words, second half (line 119) 47 words (budget 120 each).
4. The **Diagnose** paragraph now names `halted: true` as the definitive stalled
   signal, citing both triggers (verify-retry ceiling and key-rotation detect-only
   halt). **RESOLVED 2026-07-02**: confirmed against the file on disk
   (`documentation/lanes/troubleshooting.md:150`) — the text already matches the
   proposed fix below verbatim (fixed at some point during this session's later
   troubleshooting.md edits; this ledger entry was simply never cleaned up to
   reflect it).
5. `REQ-ENTERPRISE-018` backlink added to the Spec-Coverage index
   (`documentation/lanes/troubleshooting.md:288`); anchor resolves against
   `sdd/spec/enterprise-mode.md#req-enterprise-018-...`; no duplicate entry.
6. No leftover duplicate "Revised 2026-07-01" text anywhere else in `documentation/`.
7. All relative link depths correct for nested/nested layout (`../../sdd/spec/...` from
   `documentation/lanes/` and `documentation/decisions/`, both depth-2).

---
