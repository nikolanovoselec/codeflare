# Documentation Coverage Ledger

Live unresolved documentation findings from the 2026-07-11 whole-tree `/sdd clean` pass. Resolved history lives in git and `[sdd-clean]` commit bodies.

## Layout gaps

Twelve project-specific lane files fall outside the canonical seven-lane layout: `architecture-internals.md`, `authentication.md`, `billing.md`, `ci-cd.md`, `container.md`, `mobile.md`, `pentest.md`, `preseed.md`, `storage-and-sync.md`, `stress-test.md`, `user-provisioning.md`, and `vault.md`.

These files contain distinct operational contracts and extensive inbound links. Blind concatenation would create lane violations and broken anchors. Fold each section into architecture, API reference, configuration, deployment, security, observability, or troubleshooting according to ownership, rewrite every backlink, then remove the source file.

## Lane-content gaps

Implementation-prose detection produced 22 candidate paragraphs with repeated acceptance-criterion tokens. They occur in mobile, architecture, API reference, authentication, vault, security, troubleshooting, configuration, and the ADR ledger. Reconcile each against its matching REQ: replace duplicated behavior with a backlink, but retain mechanism and operational guidance in the correct lane.

The lane-signature walk also found the 12 noncanonical lane files listed above. Their content must be classified section by section before moving; no source section was deleted during this run.

## Canonical-shape gaps

The canonical lane sweep found legacy narrative/grouped shapes that do not satisfy the per-section templates: architecture (24 sections), security (37), deployment (10), troubleshooting (17), configuration (19), and eight non-endpoint sections in API reference. The endpoint tables themselves remain uniformly grouped.

A mechanical field insertion cannot infer missing `Inputs`, `Outputs`, `Threat`, `Mitigation`, `Verification`, `Rollback`, or `Implements` values without source/REQ judgment. Re-render one lane at a time from source and REQ backlinks; preserve every paragraph during conversion.

## Repeated-field gaps

Eleven ADRs repeat `**Alternative considered:**` two or three times with different values. The current deterministic rule treats differing duplicate fields as ambiguous rather than deleting them. Convert each ADR's alternatives to one `**Alternatives considered:**` field containing a short list; preserve every alternative verbatim.

## Source-anchor gaps

Direct anchor verification resolved 48 lane/ADR anchors with zero value drift after three deterministic repairs. The literal `<path>::<symbol>` example in `preseed.md` is an example, not an anchor. Remaining load-bearing legacy facts rely on transitive REQ backlinks; source anchors must be added during the lane re-render where a fact lacks that transitive path.

## Authoring debt

The whole-tree prose scan found 16 seed-word paragraphs without an adjacent source path, REQ/ADR link, or external reference and 279 broader implementation-claim candidates. Many are concrete trade-offs, but each requires paragraph-level source verification before rewriting; do not delete them by lexical match alone.

## Cold-read gaps

Eight canonical stranger tasks were attempted from the documentation alone. API usage, deployment rollback, configuration overview, and the newest ADR were discoverable. Four tasks were partial:

- `architecture.md` does not surface one unambiguous source path that owns admin-endpoint authentication.
- `configuration.md` does not provide a bounded dev-bypass variable inventory.
- `security.md` does not provide one load-bearing sentence answering whether the production session cookie is JavaScript-readable.
- `troubleshooting.md` has no direct "500 after login" recipe with the first three diagnostics.

Add those facts only after direct source verification, with REQ or `@impl` anchors.
