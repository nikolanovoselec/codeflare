# Documentation Coverage Ledger

## 2026-07-15 — autonomous review completion and explicit PR targeting

Scope: task-scoped fully-autonomous review policy, explicit `gh pr update-branch` target propagation, focused session-graph and during-run Vault requirements, and keyboard-transition coverage on PR #645. `preseed.md` owns the user-directed override, `ci-cd.md` owns affected-PR review/CI lookup, `mobile.md` owns the keyboard ownership transition, and `vault.md` owns deterministic graph identity and follow-up extraction. No HTTP API, binding, secret, production deployment target, or external dependency changed.

REQ-AGENT-091, REQ-AGENT-092, REQ-MEM-017, and REQ-VAULT-028 are `Implemented`; REQ-MOB-012 remains `Implemented`. Every changed criterion has resolving source and behavioral-test anchors, and each requirement remains at seven or fewer acceptance criteria. The fully-autonomous override changes only the round stop; all review, CI, SDD/TDD truth, deployment, and root-only mutation gates remain binding.

## 2026-07-14 — Graphify 0.9.15 compatibility and cache isolation

Scope: Graphify `0.9.12 → 0.9.15`, Codeflare's Claude/Pi semantic extraction skills, executable skill-code coverage, regenerated agent seed, and PR #641. `documentation/lanes/preseed.md` owns the shared extraction/cache flow; REQ-AGENT-023 AC7 owns per-dispatch cache isolation. No native query-tool command, Python build/cluster/detect/export/report call signature, graph schema, provider, binding, route, secret, or deployment target changed.

REQ-AGENT-023 remains `Implemented` at seven ACs. AC7 has source anchors for both runtime skill snippets and separate behavioral anchors that execute each deployed snippet against a fake Graphify cache boundary. Upstream source comparison found no breaking API/CLI change; the skill correction adopts the new optional `allowed_source_files` parameter rather than compensating for an incompatibility.

## 2026-07-14 — Pi shadow-pin lockfile regeneration

Scope: `.github/workflows/bump-shadow-pins.yml`, its shared Pi lockfile regeneration script, and behavioral host coverage on branch `develop`. `documentation/lanes/ci-cd.md` owns the workflow execution contract; `sdd/spec/operations.md` owns REQ-OPS-020 AC6. No runtime package manifest, Docker copy layout, dependency version, API, binding, secret, deployment target, or agent-seed content changed.

REQ-OPS-020 remains `Implemented` with six ACs. AC6 resolves to both owning workflow jobs, the shared command kernel, and `host/__tests__/pi-preseed-lockfile-regeneration.test.js`; the test executes the production script against a temporary package and observes lockfile creation without the lifecycle marker.

## 2026-07-14 — Pi inline-transcript extraction correction

Scope: Pi memory-capture request/worker contract on branch `pi`. `documentation/lanes/preseed.md` owns generated request and agent delivery, `architecture.md` owns the self-contained snapshot boundary, `troubleshooting.md` owns stale mirror detection, and AD103 is clarified in `decisions/README.md`. No route, API schema, binding, secret, deployment procedure, graph schema, counter lifecycle, or Vault manifest behavior changed.

REQ-MEM-014 remains `Implemented` at seven ACs. AC7's existing source/test anchors resolve to the public request builder, generator, exact request-value test, and generated frontmatter test. The request-value test was changed first and fails against the old generic request because it lacks the inline-transcript/no-`INPUT_FILE` boundary.

## 2026-07-14 — restored-page Access expiry recovery

Scope: authenticated SPA resume and fingerprinted asset delivery on branch `pi`. `documentation/lanes/authentication.md` owns Access response/session behavior, `architecture.md` owns Workers Assets routing and immutable build-asset delivery, and `troubleshooting.md` owns the observed blank/partly styled restored-page symptom. No Access application policy, identity provider, API schema, binding, secret, or deployment procedure changed; no ADR was needed because this closes incomplete paths in the accepted REQ-AUTH-022 behavior rather than introducing a new trade-off.

REQ-AUTH-022 remains `Implemented` at seven ACs. AC5-AC7 have resolving source and behavioral-test anchors for 3xx/opaque/HTML-login top-level recovery, visibility/bfcache revalidation, guarded immutable asset responses, SPA fallback exclusion, and parsed `run_worker_first` configuration. Existing 401, redirecting-state, non-empty root, and error-boundary coverage remains intact.

## 2026-07-14 — terminal scroll ownership correction

Scope: terminal viewport behavior on branch `pi`. `documentation/lanes/mobile.md` owns the terminal scroll modes, xterm trimming boundary, touch-keyboard lock, gesture routing, and deployed-browser checklist; `documentation/decisions/README.md` records AD104. No HTTP route, API schema, binding, configuration, deployment procedure, security boundary, or external dependency changed.

REQ-TERM-014, REQ-MOB-004, and REQ-MOB-012 remain `Implemented` and below the seven-AC cap. Every changed criterion has resolving `@impl` and behavioral `@test` anchors. Regression coverage composes a batched write with native-like `onScroll` feedback, continues output beyond the intent-correlation window, verifies ownership release only at bottom, and preserves keyboard-open bottom anchoring plus keyboard-open/closed gesture routing.

## 2026-07-14 — root-owned Pi extraction execution-profile and graph correction

Scope: live-smoke correction on branch `pi` after a one-file Vault worker consumed 84.2k tokens and 336 seconds without committing, followed by a bounded session capture whose note was accurate but whose model-authored graph used noncanonical IDs and duplicate edges. `architecture.md` owns bounded data flow and deterministic chunk construction; `preseed.md` owns generated/public agent settings and helper delivery; `vault.md` owns one-pass Vault/session behavior and graph identity; `troubleshooting.md` owns stale-mirror, slow-worker, and malformed-chunk recovery; AD103 owns the medium-reasoning/four-turn and deterministic-helper decision and amends AD60/AD62/AD102. No HTTP route, binding, deployment command, secret, security boundary, or external API changed.

REQ-MEM-001, REQ-MEM-002, REQ-MEM-009, REQ-MEM-014, REQ-MEM-016, REQ-MEM-017, REQ-VAULT-027, and REQ-VAULT-028 retain seven or fewer ACs and finish only after bounded request/frontmatter, uncaptured-interval compaction, deterministic H1/concept graph tests, serialized edge-evidence tests, post-commit note/chunk promotion tests, and artifact-gated terminal completion resolve. Claude's worker profile remains unchanged; the shared Claude/Pi cumulative merge preserves distinct edge evidence and collapses exact duplicates.

## 2026-07-14 — root-owned Storage pagination checkpoint coverage

Scope: append-only R2 browser continuation on branch `pi`; root-authored coverage record before final exact-head CI. `documentation/lanes/storage-and-sync.md` owns listing behavior and now documents continuation admission, deduplication, stale-generation rejection, retry preservation, and periodic-refresh suppression. No Worker route, API schema, binding, deployment, security, configuration, or backend limit changed.

REQ-STOR-018 owns seven single-behavior pagination criteria with resolving source and behavioral anchors for continuation availability, scroll-boundary loading, append-only rows, stale-response rejection, failed-row preservation, token reuse, and timer ownership. REQ-STOR-016 retains presentation and traversal safety. Exact-diff review is active on the release PR.

## 2026-07-14 — root-owned Pi extraction checkpoint coverage

Scope: extraction-only checkpoint on branch `pi`; root-authored coverage record before branch CI. Updated owner lanes: `architecture.md` (root/session data flow), `vault.md` (memory and user-edit operation), `storage-and-sync.md` (committed versus staged R2 persistence), `preseed.md` (deployed Pi contracts), `troubleshooting.md` (bounded retry/GIVEUP recovery), and `decisions/README.md` (AD102). No route, API, binding, deployment, security, or configuration contract changed.

Static SDD preflight resolved all six touched requirements as `Implemented`, kept every REQ at seven or fewer ACs, and verified all 16 changed AC lines carry existing source and behavioral-test anchors. REQ-VAULT-027 owns Pi transactional delivery because REQ-VAULT-003 already has seven ACs; Claude AC1–AC7 remain unchanged. Exact-diff `spec-enforce`/`doc-enforce` review is intentionally deferred until the completed implementation opens its new PR to `main`.

## 2026-07-12 — doc-updater PR-boundary review, develop (9604990..9066bd6)

Scope: cumulative diff `origin/main...HEAD` restricted to `documentation/` + root
`README.md` (20 files). Report-only; no fixes applied by this run. Skill invocations:
`doc-enforce` (16-row manifest run inline), `doc-enforce-lanes` (per touched file),
`doc-enforce-shape` (api-reference.md + architecture.md touched), `doc-enforce-truth`
Pass 15 (all `@impl` anchors added in diff verified against source).

### HIGH — orphaned duplicate binary assets (8.4 MB)

`assets/documentation/{dashboard.png,guided-setup.png,hero-ide-fullscreen.png,
logo-icon.svg,mobile-foldable.jpg,mobile-phone.jpg,og.png}` were added new in this
diff (7 files, byte-identical to their `documentation/images/` counterparts —
confirmed via `md5sum` on `og.png`). `README.md` was repointed to the new
`assets/documentation/*` paths (lines 1, 3, 11, 51, 87, 94, 126). Nothing in the
tracked repository references `documentation/images/*` anymore (`git grep` returns
zero hits outside a historical changelog entry). The old directory is now 8.4 MB of
fully unreferenced duplicate weight, and future OG-card regen
(`web-ui/scripts/rasterize-og.mjs` writes only to `web-ui/public/og.png`; the copy
into place is manual) now has two silently-divergeable destinations instead of one.

**Fix:** `git rm -r documentation/images/` in the same change that introduced
`assets/documentation/`. Nothing else needs updating (no remaining references).

### MEDIUM — `documentation/lanes/api-reference.md` TOC out of sync

`## Contents` (lines 9-29) lists every `###` endpoint-group heading except
`### Public (Landing)` (heading at line 573, added prior to this diff but never
added to the TOC; this diff touched the TOC directly — added `Browser IDE`, removed
`Presets` — without closing this pre-existing gap). **Fix:** add
`- [Public (Landing)](#public-landing)` to the Contents list, positioned between
`Discoverability Documents` and `Setup` per heading order (actual body order has it
between `Public (Unauthenticated)` and `Discoverability Documents`).

### MEDIUM — `documentation/lanes/architecture.md` new grouped-table section vs. file's per-item dominant shape

The new `## Landing composition implementation` section (lines 619-637) renders as a
grouped table (`Component | Responsibility | Inputs | Outputs | Source`), which
matches the per-lane template's required field set exactly. However the file's
pre-existing `## System Components` section (lines 41-317, ~13 components) uses
per-item narrative shape (`### {Component}` + `**File:**` + free prose), which
dominates the file. This is a `rendering-shape-mismatch` candidate under Pass 6.
Noting for the record: the pre-existing dominant shape itself does not strictly
carry the template's bolded `**Responsibility:**/**Inputs:**/**Outputs:**/**Source:**`
fields either (uses `**File:**` + prose instead), so a full reconciliation is a
`/sdd clean` structural pass, not a one-section fix. **Fix (deferred, JUDGMENT):**
either re-render `Landing composition implementation` to per-item narrative shape to
match the file's resolved dominant shape, or (preferred) treat this as the seed of a
file-wide re-render to grouped-table shape in the next `/sdd clean --scope=all` pass,
since grouped-table is the shape that actually satisfies the per-lane template.

### MEDIUM — `documentation/lanes/architecture.md` "Manual verification checklist" (127 items) spans domains outside architecture's lane

New section (lines 639-770) lists 127 REQ checkboxes across REQ-AGENT-*, REQ-MOB-*,
REQ-VAULT-*, REQ-STOR-*, REQ-SUB-*, REQ-LANDING-*, REQ-BROWSER-*, REQ-SESSION-*,
REQ-MEM-*, REQ-GITHUB-*, REQ-TERM-*, REQ-IDE-003 — i.e. nearly every domain except
deployment/security/config (which already carry their own smaller "Manual
verification checklist" sections). `architecture.md`'s lane is layout/data-flow;
REQ-MOB-*/REQ-VAULT-*/REQ-STOR-*/REQ-SUB-* already have natural homes in
`mobile.md`/`vault.md`/`storage-and-sync.md`/`billing.md`. Noting this pattern is not
unprecedented — `security.md`'s existing checklist already mixes in AGENT/BROWSER/
GITHUB/OPS alongside SEC — so this is escalated as JUDGMENT rather than asserted as a
clear-cut new violation. **Fix (deferred, JUDGMENT):** split by REQ-domain prefix into
the lane file that already exists for that domain where one exists (MOB, VAULT, STOR,
SUB); leave a residual cross-cutting bucket (AGENT/BROWSER/GITHUB/LANDING/MEM/
SESSION/TERM, none of which have a dedicated lane file) either in architecture.md or
a new project-wide QA tracker — the latter requires user confirmation per the
"never create new doc files without confirmation" rule.

### LOW — heading capitalization inconsistency (`architecture.md`)

New headings `## Landing composition implementation` and
`## Manual verification checklist` use sentence-case; every sibling `##` heading in
the file uses Title Case (`Architecture Overview`, `System Components`, `Data Flow`,
`Module-Level Caches`, `Design Rationale`, `Specification Coverage`,
`Related Documentation`). **Fix:** rename to `## Landing Composition Implementation`
and `## Manual Verification Checklist` (update the two Contents TOC entries and the
GitHub anchor slugs they link to in the same edit).

### LOW — non-standard `@impl` anchor format (2 instances, content verified accurate)

- `documentation/lanes/security.md:43` —
  `<!-- @impl: src/routes/github-auth.ts::Set-Cookie = HttpOnly; Secure; SameSite=Lax -->`.
  Verified: `src/routes/github-auth.ts:240,311` do emit
  `Set-Cookie: codeflare_session=...; HttpOnly; Secure; SameSite=Lax`. The claim is
  correct, but the anchor after `::` is a header/value description, not a resolvable
  source symbol — every other `@impl` anchor in this codebase is `file::symbolName`.
- `documentation/decisions/README.md` (AD97) —
  `<!-- @impl: Dockerfile::OPENVSCODE_VERSION = "1.109.5" -->`. Verified: `Dockerfile:146`
  sets `OPENVSCODE_VERSION="1.109.5"` verbatim. Same format deviation (symbol name is
  real, but the `= "value"` suffix breaks the plain-locator convention).

**Fix:** either drop the `= value` suffix (keep `::Set-Cookie` /
`::OPENVSCODE_VERSION` as the locator, let the prose carry the value) or, if a
value-bearing anchor form is wanted going forward, establish it as a documented
second `@impl` form rather than an ad hoc one-off.

### LOW — prose-weasel

`documentation/lanes/deployment.md:123` — "Inspect repository settings and a staging
workflow run, including job graph, logs, artifacts, and billing metrics **where
applicable**; compare each observable result with every AC." "Where applicable" has
no anchor disambiguating which of the four items applies to which of the 6 checklist
REQs below it. **Fix:** either drop the qualifier or state the condition (e.g.
"billing metrics for REQ-OPS-006 only").

### LOW — pre-existing whole-tree Pass 1 debt (NOT introduced by this diff, informational only)

Confirmed via `git blame` that none of the following predate this diff — all blame to
commits already on `origin/main` before this branch:
- 6 paragraphs > 120 words in `documentation/decisions/README.md` (AD entries, lines
  537, 609, 620, 781, 902, 1985).
- 2 paragraphs > 120 words in `documentation/lanes/architecture.md` (lines 581, 589 —
  the `updateKvStatus()`/dashboard-KV-read paragraphs).
- Numerous table cells in the 40-50 word LOW band scattered across
  `configuration.md`, `troubleshooting.md`, `vault.md`, `ci-cd.md`, `api-reference.md`,
  `architecture-internals.md`, `security.md`, `storage-and-sync.md` — none newly
  introduced by this diff (spot-checked several via `git blame`).
- 1 paragraph > 120 words each in `ci-cd.md` (x2), `mobile.md`, `preseed.md` (x2).

Not gating this PR. Owner: next `/sdd clean --scope=all` documentation sweep.

### Clean / verified — no finding

- Pass 16 (doc index integrity): zero unindexed lane files, zero dangling
  `documentation/README.md` links.
- Pass 15 (`@impl` source-anchor truth-check) on every anchor added in this diff (18
  anchors across `decisions/README.md`, `architecture.md`, `configuration.md`,
  `container.md`, `preseed.md`, `security.md`): all resolve to real symbols in the
  cited files (2 flagged above for format only, not resolution).
- Feature removal sync: the `Presets`/`lastPresetId` API section and preference field
  were correctly removed from `api-reference.md` in step with the source-side
  `session-presets` removal (`sdd/spec/changes.md:33`, `REQ-TERM-010` deleted); no
  orphaned reference to "preset" remains anywhere under `documentation/` or `sdd/spec/`.
- New feature docs (Browser IDE: `api-reference.md`, `container.md`, `security.md`,
  `architecture.md` checklist entry) all carry correct `REQ-IDE-00{1,2,3,4}` backlinks
  that resolve, and match the cited REQs' actual Acceptance Criteria (spot-checked
  AC7 of REQ-IDE-003 against the api-reference.md error-response prose).
- New ADRs AD95/AD96/AD97: indexed in the Decision Index table with working links,
  carry `**Status:**`/`**Context:**`/`**Decision:**`/`**Consequences:**`, no
  dual-narrative pattern.
- No Big-O jargon, no forbidden content (TODO/strikethrough/roadmap prose), no new
  Pass 13 duplicate-field or hybrid-shape findings in any diff-touched section.
- Positioning copy in `README.md` (`# Codeflare`, OG alt text, hero line) does not
  name Cloudflare; factual Cloudflare references remain correctly in the technical
  lane files (security.md, configuration.md, etc.).
- `documentation/lanes/vault.md`'s 21-line directory-tree code block (over the
  15-line cap) carries a valid `<!-- doc-allow-element: AD54 -->` exemption; AD54
  exists. Not a finding.
- All rewritten/verified relative links in `README.md`
  (`authentication.md#authentication-modes`, `authentication.md#e2e-testing-auth`,
  `pentest.md#test-results`) resolve to real headings.

---

## 2026-07-12 — Non-default deployment operations moved private

Default-mode deployment remains public in the root README and the default configuration lanes. Exact Onboarding, SaaS, Enterprise, enterprise-egress, Governed Mode, E2E credential, fallback-registry, and non-default GitHub Environment procedures are owned by [Codeflare private operations](https://github.com/nikolanovoselec/codeflare-private). Public lanes preserve behavior, REQ/ADR backlinks, and their established file/heading structure while linking to the private operational source of truth.

The private README was refreshed before public redaction: obsolete `AIG_LANGUAGE_MODEL` guidance was replaced by the Setup-managed dynamic-route catalog; dual Workers AI + AI Gateway Run token permissions, enterprise account overrides, strict-egress preparation/rollback, Governed Mode migration, onboarding build settings, E2E setup, stress/pentest variables, and Docker Hub fallback credentials were retained there. Verification resolved 2,078 documentation-internal links and 27 public/private cross-repository links with zero failures.

## 2026-07-12 — Project documentation structure restored and retained

The user explicitly retained Codeflare's 28-file documentation tree from commit `98b8a1b`. The specialized lane files and seven referenced images are project-canonical; they must not be folded into generic mega-lanes or deleted by automated cleanup. This resolves the earlier structural-consolidation approach as rejected, not pending work.

Content cleanup was applied in place: per-element budget repairs, consolidated ADR alternatives, current Browser IDE transport and risk facts, Fast Start reset behavior, API corrections, cold-read guidance, rollback steps, current REQ links, and AD97. Verification resolved 1,642 source anchors, 2,120 documentation-internal links, and every Lane Index entry with zero failures. No open content findings.

## 2026-07-02 — Whole-tree Pass 1 (per-element word-budget) audit, surfaced incidentally during PR #601 review

Not a diff-scoped finding: doc-updater's Pass 1 (per-element budgets) runs whole-tree
regardless of what a given PR touched, and surfaced this while reviewing PR #601's
REQ-VAULT-008/017/024 citation-repoint diff. None of this content was authored or
touched by PR #601 — it is pre-existing debt across the documentation lane. Not
gating PR #601 (per doc-updater's own recommendation, confirmed by the user
2026-07-02); tracked here so it is not silently lost. Owner: whoever runs the next
`/sdd clean --all` documentation sweep.

### Historical aggregate counts (post PR #601, superseded)

The original audit recorded 119 list-item findings, 11 table-cell findings, and
21 paragraph findings. Those counts are provenance only; the 2026-07-03 cleanup below
supersedes them.

Final Pass 1 state after cleanup: `0` over-budget list items, table cells, paragraphs,
code snippets, or heading-depth findings under `documentation/`.

### Former worst offenders (resolved)

The previously listed vault bootstrap/native-worker rows and ADR ledger bullet pattern
were resolved by extracting prose out of oversized elements and preserving facts in
nearby body text.

### 2026-07-03 partial cleanup — `/sdd clean --scope=all --unleashed`

Resolved the recorded Pass 1 findings in `documentation/lanes/api-reference.md`,
`documentation/lanes/security.md`, and `documentation/lanes/troubleshooting.md` by
splitting overlong table cells, bullets, and paragraphs into short entries plus nearby
prose/notes; facts and links were preserved.

Also fixed focused, low-risk ADR list-item overages by splitting long bullets into short
bullets plus adjacent indented paragraphs under the same ADR. Touched AD52, AD53, AD74,
AD83, AD86, AD87, AD89, and AD91. The former single-largest ADR finding
(`documentation/decisions/README.md:1839`, the 378-word AD91 bounded-driver bullet) is
resolved.

Follow-up validation cleanup cleared the remaining Pass 1 debt across ADRs and lane
docs. The final per-element scanner reports `0` over-budget cells, list items,
paragraphs, code snippets, or heading-depth findings under `documentation/`.
The previously listed manual recheck counts are closed; largest resolved offenders
included the AD87 browser-run consequence, AD84 vault green-click bullets, and
AD44/AD92 ADR bullets.

Validated files include the ADR ledger plus architecture, authentication, billing,
configuration, container, mobile, preseed, storage-and-sync, troubleshooting, and
vault lane docs. Facts and REQ/ADR links were preserved while long elements were
split into short rows, bullets, paragraphs, or trimmed diagrams.

### JUDGMENT resolved — Pass 13 repeated labels are a repeatable-field convention

- `documentation/decisions/README.md`: the bolded label `**Alternative considered:**`
  repeats 2-3 times within a single ADR section in 11 ADRs (AD53, AD54, AD55, AD56,
  AD58, AD59, AD61, AD62, AD63, AD64, AD66). Each occurrence describes a *different*
  alternative (different values, not the same fact restated) — reads as a deliberate
  repeatable-field convention, not the duplicate-fact defect the rule targets.
- `documentation/lanes/api-reference.md`: `**Source:**` repeats 6 times in the
  "Configuration Steps" section, same repeatable-list pattern.

Decision: accept this as an intended repeatable-field convention, not duplicate-fact
drift. Future enforcement should flag repeated bold labels only when they restate the
same fact; identical labels are acceptable when each occurrence records a different
alternative, source, value, or step.

---

## 2026-07-01 — PR #601 (develop→main), delta review ec738ae5..1465edc

Scope: incremental diff only, restricted to `documentation/`. Files touched:
`documentation/decisions/README.md`, `documentation/lanes/troubleshooting.md`.

### Verified: prior findings resolved correctly

1. AD91 state-shape literal (`documentation/decisions/README.md:1829`) now includes
   `halted?` alongside `total?`/`processed?`. Matches `src/lib/r2-regime-state.ts:59`.
2. The bloated "Revised 2026-07-01" note was lifted out of the AD91 "Backend gate +
   container drain" bullet into a standalone paragraph after the "Dual-regime reads +
   self-heal" bullet (`documentation/decisions/README.md:1835`). Word count: 98 words
   (budget 120). The bullet itself is back to a single coherent sentence pair with no
   dangling reference.
3. `documentation/lanes/troubleshooting.md`'s **Cause** paragraph (line 117) was split
   at the sentence boundary about the button/poll UI behavior. Neither half exceeds
   budget: first half 87 words, second half (line 119) 47 words (budget 120 each).
4. The **Diagnose** paragraph now names `halted: true` as the definitive stalled
   signal, citing both triggers (verify-retry ceiling and key-rotation detect-only
   halt). **RESOLVED 2026-07-02**: confirmed against the file on disk
   (`documentation/lanes/troubleshooting.md:150`) — the text already matches the
   proposed fix below verbatim (fixed at some point during this session's later
   troubleshooting.md edits; this ledger entry was simply never cleaned up to
   reflect it).
5. `REQ-ENTERPRISE-018` backlink added to the Spec-Coverage index
   (`documentation/lanes/troubleshooting.md:288`); anchor resolves against
   `sdd/spec/enterprise-mode.md#req-enterprise-018-...`; no duplicate entry.
6. No leftover duplicate "Revised 2026-07-01" text anywhere else in `documentation/`.
7. All relative link depths correct for nested/nested layout (`../../sdd/spec/...` from
   `documentation/lanes/` and `documentation/decisions/`, both depth-2).

---

### Resolution — main session, 2026-07-12 (develop @ 9066bd6)

- **HIGH orphaned duplicate assets — RESOLVED.** `git rm -r documentation/images/`
  (7 byte-identical files). Verified README references all 7 via `assets/documentation/*`
  and no live markdown image link targets `documentation/images/*`. Also repointed the
  stale changelog path in `sdd/spec/changes.md` (`documentation/images/og.png` →
  `assets/documentation/og.png`) — a reference the doc lane's grep missed.
- **MEDIUM TOC out of sync — RESOLVED.** Added `- [Public (Landing)](#public-landing)` to
  `api-reference.md` `## Contents`, placed between `Public (Onboarding)` and `Health` to
  match source heading order (569 → 573 → 611); the reviewer's suggested "between
  Discoverability and Setup" slot would have broken source order.
- **MEDIUM prose-weasel — RESOLVED.** `deployment.md` "billing metrics **where applicable**"
  → "billing metrics when present".
- **MEDIUM rendering-shape-mismatch (`architecture.md` Landing composition) — KEPT (reasoned).**
  The grouped table is per-lane-template-compliant (the reviewer's own note); converting to
  per-item narrative would add prose the doc-enforce rules discourage, and a whole-file
  re-render is `/sdd clean --scope=all` territory (reviewer option b). Not a defect.
- **MEDIUM lane-scope-broad (`architecture.md` Manual verification checklist) — KEPT, rationale corrected.**
  Correction: an earlier framing of this disposition claimed a `security.md` cross-domain-checklist
  precedent and called `architecture.md` the home for "domains lacking a dedicated lane." The
  re-review verified both were inaccurate — `security.md`'s checklist is only {AUTH, ENTERPRISE,
  SEC}, and 37 of the 127 items (VAULT 12, STOR 10, SUB 9, MOB 6) do have dedicated lane files
  (`vault.md`, `storage-and-sync.md`, `billing.md`, `mobile.md`). Accurate picture: this is a
  deliberate **single-source** design — every one of the 127 manual-check REQs carries a `Notes`
  field pointing to the one `architecture.md#manual-verification-checklist`, and the majority (90
  items: AGENT 54, MEM 9, SESSION 8, LANDING 6, BROWSER 5, TERM 4, GITHUB 3, IDE 1) have **no**
  documentation lane file at all. Kept consolidated because the reviewer's partial split (relocate
  the 37 lane-homed items) fragments that uniform contract into a hybrid — a reader would have to
  know whether a domain "has a lane file" to know where its checklist lives — and requires
  repointing 37 spec `Notes` (~74 edits) for lane-purity on under 30% of items. The clean full
  resolution (a dedicated verification lane hosting all 127, or lane files for the 90 homeless
  domains, with every `Notes` repointed) is a new-file whole-structure decision that belongs in
  `/sdd clean --scope=all` or explicit user direction, not a review-fix pass. Lane-scope stretch
  acknowledged and left OPEN/escalated in this ledger, not silently closed.
- **LOW heading capitalization — DEFERRED to `/sdd clean`.** The two new sentence-case headings
  match the identical "Manual verification checklist" heading in three other lane files added
  this diff; Title-casing only `architecture.md` would break cross-file consistency. GitHub
  anchor slugs are capitalization-insensitive, so no link breaks either way — whole-lane
  cosmetic convention, not a defect.
- **LOW non-standard `@impl` anchor format (2×) — DEFERRED to `/sdd clean`.** Content verified
  accurate; normalizing the value-bearing anchor form risks anchor-resolution churn for a
  cosmetic gain.
- **LOW pre-existing whole-tree Pass 1 prose debt — DEFERRED to `/sdd clean --scope=all`.**
  Confirmed by the reviewer via `git blame` to predate this diff; non-gating.
