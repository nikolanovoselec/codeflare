export const README_MEDIA = Object.freeze([
  Object.freeze({ name: 'execution', playback: 'once' }),
  Object.freeze({ name: 'browser-vscode', playback: 'loop' }),
  Object.freeze({ name: 'browser-e2e', playback: 'once' }),
  Object.freeze({ name: 'review-governance', playback: 'loop' }),
  Object.freeze({ name: 'deployment', playback: 'once' }),
  Object.freeze({ name: 'inference-mesh', playback: 'loop' }),
]);

export const RETIRED_README_PICTURES = Object.freeze([
  'mobile-foldable.jpg',
  'mobile-phone.jpg',
  'hero-ide-fullscreen.png',
  'guided-setup.png',
]);

export const README_MEDIA_BUDGETS = Object.freeze({
  gifBytes: 10 * 1024 * 1024,
  aggregateBytes: 30 * 1024 * 1024,
});
