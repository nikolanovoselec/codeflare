# The merge driver is registered globally in the codeflare container image
# (AD52). Disable text diffs without applying the `binary` macro, which would
# unset merge=graphify. The generated JSON can be refreshed after a conflict.
graphify-out/graph.json -diff -text merge=graphify
