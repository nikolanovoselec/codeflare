# Spec Changes

Semantic changes to the specification. Git history captures diffs; this file captures intent.

## 2026-08-30

- **Administration material-state UI keeps user-owned validation** ([REQ-OPS-022](operations.md#req-ops-022-coverage-threshold-gate-fails-closed-on-missing-evidence) amended; remains Implemented). Changed-line coverage still fails closed for affected production files except the six Administration UI owners explicitly listed by the frontend workflow. Their visual and interaction acceptance remains manual on Integration under the approved execution boundary; backend contracts and all other frontend production files retain automated coverage gates.

- **Administration and Analytics contracts are owned before implementation** ([REQ-SETUP-017](setup.md#req-setup-017-mode-aware-administration-configuration-read), [REQ-SETUP-018](setup.md#req-setup-018-stateless-environment-preview-and-bounded-execution), [REQ-SETUP-019](setup.md#req-setup-019-administration-and-analytics-shell), [REQ-SUB-025](subscription.md#req-sub-025-durable-historical-usage-accounting), [REQ-SUB-026](subscription.md#req-sub-026-admin-organization-analytics-and-deletion-history), [REQ-SUB-027](subscription.md#req-sub-027-monthly-organization-usage-reports), [REQ-OPS-056](operations.md#req-ops-056-non-destructive-d1-deployment-boundary), and [REQ-OPS-057](operations.md#req-ops-057-bounded-administration-operation-envelope) added; REQ-SETUP-017, REQ-SETUP-018, REQ-SETUP-019, REQ-SUB-025, REQ-SUB-026, REQ-SUB-027, REQ-OPS-056, and REQ-OPS-057 are Implemented; Integration exception-visibility evidence remains a deployment gate; [AD150](../../documentation/decisions/README.md#ad150-d1-owns-historical-usage-and-report-delivery-records) accepted). Routine administration uses closed Environment sections, stateless preview, reconnectable sanitized runs, and existing mode owners. D1 owns historical organization usage and report claims while Timekeeper and KV keep live quota authority. Deployment uses a separate D1-capable token and additive migrations. The product adds no workflow engine, generic patch layer, queue, cache, coordinator Durable Object, or second database.

- **Purpose and platform now route independently through one design authority** ([REQ-AGENT-182](agents.md#req-agent-182-purpose-and-platform-design-routing), [REQ-AGENT-183](agents.md#req-agent-183-native-mobile-design-authority), [REQ-AGENT-184](agents.md#req-agent-184-operational-information-design), [REQ-AGENT-185](agents.md#req-agent-185-component-system-and-registry-boundaries), [REQ-AGENT-186](agents.md#req-agent-186-conditional-complex-motion-delegation), [REQ-AGENT-187](agents.md#req-agent-187-design-routing-evaluation-matrix), and [REQ-AGENT-188](agents.md#req-agent-188-external-design-dependency-safety) added as Implemented). The router classifies Persuade, Operate, Read, or Experience independently from web, native, desktop, or static delivery. A new native-mobile authority preserves product identity while enforcing real iOS, Android, cross-platform, lifecycle, device, and accessibility constraints. Progressive operational, component-system, external-dependency, and complex-motion guidance prevents dashboard widget defaults, registry-led art direction, automatic package execution, and unjustified animation tooling. Ten isolated scenarios verify dashboard density, platform divergence, component-system restraint, missing-registry operation, and conditional choreography.

- **Portable frontend design has one lazy-loaded art-direction authority** ([REQ-AGENT-179](agents.md#req-agent-179-portable-visual-design-routing), [REQ-AGENT-180](agents.md#req-agent-180-portable-frontend-design-authority), and [REQ-AGENT-181](agents.md#req-agent-181-design-specialist-compatibility) added as Implemented; [REQ-AGENT-134](agents.md#req-agent-134-advanced-design-skill-suite), [REQ-AGENT-135](agents.md#req-agent-135-ui-ux-pro-max-query-and-generation), [REQ-AGENT-136](agents.md#req-agent-136-ui-ux-pro-max-persistence), and [REQ-AGENT-137](agents.md#req-agent-137-ui-ux-pro-max-data-validation) completed as Implemented). Advanced Pi spends one short always-active sentence on visual-work discovery, then the compact `design` router selects only the relevant specialist. `frontend-design` owns tool-neutral frontend art direction with focused references for new work, redesign, assets and motion, visual QA, and conditional Astro/Cloudflare use. The 87 KB taste manual becomes a compatibility redirect; UI UX Pro Max becomes advisory research; Impeccable discovery narrows to critique and finishing while every explicit command remains available. Impeccable refresh validates routing metadata before replacing either target, so invalid bundles leave both existing skills unchanged.

- **Herdr Pi uses application-owned fullscreen history and deterministic mobile input** ([REQ-MOB-022](mobile.md#req-mob-022-herdr-mobile-input-focus-and-viewport) added as Implemented; [REQ-TERM-040](terminal.md#req-term-040-stable-herdr-pane-scrollback), [REQ-MOB-017](mobile.md#req-mob-017-fullscreen-application-touch-scrolling), [REQ-MOB-020](mobile.md#req-mob-020-terminal-touch-activation), and [REQ-TERM-036](terminal.md#req-term-036-browser-pointer-interaction-with-herdr) amended and remain Implemented). Fresh Herdr Pi starts fullscreen, every Herdr frame is delivered in order, vertical swipes scale with finger distance without inertia, and a trusted touchend sends one same-cell SGR tap before opening mobile input. Compatibility clicks after Herdr movement gestures are suppressed so releasing a scroll cannot open mobile input. Opening mobile input sends Pi's standard `End` sequence to reset application-owned history to the live bottom; taps while input is already open preserve the viewport. Classic remains unchanged.

- **Herdr suppresses complete touch-derived compatibility sequences** ([REQ-MOB-023](mobile.md#req-mob-023-opt-in-mobile-input-diagnostics) added as Implemented; [REQ-MOB-020](mobile.md#req-mob-020-terminal-touch-activation) amended and remains Implemented). The bounded post-touch shield owns `mousedown`, `mouseup`, and delayed `click` events identified by touch capability or coordinate fallback; explicitly identified hardware mouse input remains unchanged. A confirmed Herdr scroll releases stale hidden-input focus so Samsung Internet cannot reopen the keyboard at gesture end. The opt-in `?debug=1` overlay records bounded, content-free input ordering and keyboard geometry for diagnosis.

- **Rejected Herdr clipboard writes are latest-write-wins** ([REQ-TERM-041](terminal.md#req-term-041-rejected-herdr-clipboard-recovery) added as Implemented; [REQ-TERM-032](terminal.md#req-term-032-herdr-clipboard-compatibility-boundary) remains Implemented). A late rejection from an older OSC 52 write cannot replace newer clipboard state; the trusted floating Paste control retries and consumes only the latest rejected value.

## 2026-08-29

- **Herdr semantic status now owns delayed completion notifications** ([REQ-TERM-024](terminal.md#req-term-024-pi-native-terminal-notification-producer), [REQ-TERM-029](terminal.md#req-term-029-herdr-status-gated-terminal-completion), [REQ-TERM-038](terminal.md#req-term-038-herdr-semantic-status-owns-completion-readiness), and [REQ-TERM-039](terminal.md#req-term-039-herdr-completion-delivery-is-readiness-oriented) amended; remain Implemented). Immediate validated input-required signals remain unchanged. Pi no longer reconstructs completion from settled turns, subagent lineage, failures, or a five-minute timer. Herdr aggregates every recognized Pi and Claude pane across tabs and splits. After at least one pane was `working`, one ten-minute timer starts only when all tracked panes are `idle` or `done`; any `working`, `blocked`, or `unknown` pane prevents completion. A `working` snapshot also cancels queued completion, and the socket monitor re-snapshots after a bounded timeout, tracked-pane lifecycle change, malformed status event, or disconnect. Classic emits no completion notification.

- **Memory and Vault extraction use prompt cadence instead of continuous Vault polling** ([REQ-MEM-002](memory.md#req-mem-002-capture-triggers-every-50-user-messages-and-on-resume) and [REQ-VAULT-003](vault.md#req-vault-003-user-curated-edits-use-bounded-prompt-cadenced-ingestion) amended; [REQ-MEM-022](memory.md#req-mem-022-resumed-sessions-re-emit-memory-graph-guidance), [REQ-VAULT-030](vault.md#req-vault-030-pi-vault-request-delivery-has-one-root-owner) and [REQ-VAULT-031](vault.md#req-vault-031-vault-hash-checks-follow-successful-prompt-cadence) added; all Implemented). Memory capture now runs every 50 real prompts and immediately captures only the durable uncaptured tail on the first prompt after resume. Every resumed-tail capture hash-checks Vault; regular Vault checks occur at crossed 100-prompt epochs and no-op without changed content. Failed Claude scans retry on the next prompt without consuming the epoch. The polling extraction daemon no longer starts, one Pi root owns each request, and Pi requests allow seven bounded turns.

- **Herdr clipboard actions bridge browser copy and paste directly** ([REQ-TERM-032](terminal.md#req-term-032-herdr-clipboard-compatibility-boundary) amended and remains Implemented; [REQ-TERM-041](terminal.md#req-term-041-rejected-herdr-clipboard-recovery) owns rejected-write recovery). Bounded Herdr OSC 52 copy writes are no longer blocked by the unrelated desktop right-click-paste toggle. When Samsung rejects an asynchronous browser write, Codeflare retains that value; the next trusted floating Paste control retries the system clipboard write and pastes the retained value into the active terminal.

## 2026-08-28

- **CI failure monitoring waits for the complete exact-head result** ([REQ-AGENT-068](agents.md#req-agent-068-independent-pi-ci-monitoring), [REQ-AGENT-070](agents.md#req-agent-070-claude-on-demand-ci-monitoring-policy), and [REQ-AGENT-125](agents.md#req-agent-125-pi-ci-result-and-launch-checkpoint) amended; remain Implemented). Pi no longer returns when the first check fails, and Claude no longer returns when the first workflow fails. Each monitor waits until every observed row is terminal and the full fingerprint remains stable across two polls, then reports every failed or cancelled row together for one triage and fix round.

- **Herdr scrollback stability and touch activation requirements added** ([REQ-TERM-040](terminal.md#req-term-040-stable-herdr-pane-scrollback), [REQ-MOB-020](mobile.md#req-mob-020-terminal-touch-activation), [REQ-MOB-021](mobile.md#req-mob-021-terminal-follows-visible-container-changes), and [REQ-OPS-055](operations.md#req-ops-055-herdr-release-integration) added as Implemented; [REQ-TERM-016](terminal.md#req-term-016-terminal-pane-reconnect-and-resize-authority), [REQ-TERM-036](terminal.md#req-term-036-browser-pointer-interaction-with-herdr), [REQ-MOB-010](mobile.md#req-mob-010-fitaddon-fit-calls-are-coordinated), and [REQ-MOB-017](mobile.md#req-mob-017-fullscreen-application-touch-scrolling) amended; remain Implemented). Final implementation uses Pi-owned fullscreen history, direct ordered Herdr frames, and deterministic same-cell touch taps. Container CI checks the consumed Herdr schema, and the weekly shadow-pin workflow owns release updates.

- **Remote curation restores and declares its image-owned Pi runtime companion** ([REQ-STOR-031](storage.md#req-stor-031-managed-resource-container-sync) AC7 added; remains Implemented). Remote curation deliberately excludes image-owned context-mode runtime code while its managed `/ctx` command imports that companion. Initial R2 sync can remove the local image copy when the object is absent, especially after Exclusive cleanup; post-restore startup now restores only the explicitly declared image dependency without replacing release-owned extension bytes. Behavioral coverage couples every declaration to restoration before PTY release and the bisync baseline.

- **Default Workspace has a visible file-tree marker** ([REQ-IDE-048](browser-ide.md#req-ide-048-default-workspace-and-dashboard-owned-vs-code-sessions) AC7 added; remains Implemented). Advanced and enterprise users now see the same section-header visual treatment on the workspace selector as other settings groups, without changing workspace availability or preference behavior.

- **Canonical reviewer launch evidence fails closed without an authoritative window** ([REQ-AGENT-177](agents.md#req-agent-177-canonical-reviewer-launch-evidence) added as Implemented; [REQ-AGENT-170](agents.md#req-agent-170-joint-review-and-ci-triage) AC1 narrowed; remains Implemented). Pi and Claude launch-credit rules moved from REQ-AGENT-170 AC1 to focused REQ-AGENT-177 AC1-AC4 without changing canonical launch values. Pi now leaves reviewer calls uncredited when the active scope and output contract cannot be resolved instead of waiving those checks.

- **Malformed Pi review launches receive actionable feedback** ([REQ-AGENT-176](agents.md#req-agent-176-malformed-review-launches-receive-actionable-feedback) added as Implemented). Required reviewer or CI subagent calls that return successfully but violate the emitted launch contract remain uncredited and now receive one visible rejection naming each mismatched field. Repeated settlement does not repeat the warning, while corrected launches continue the same in-memory round.

- **Fresh Herdr Pi readiness follows live initialization and fails open on integration drift** ([REQ-TERM-035](terminal.md#req-term-035-terminal-readiness-follows-mode-and-workspace) AC6-AC7 added; remains Implemented). Fresh startup previously completed bootstrap as soon as Herdr detected the `pi` process, exposing the submitted command while Pi still initialized. Pinned Herdr v0.8.2 now uses the same official lifecycle authority as restored sessions before completing bootstrap, with a shorter bounded fresh-start budget. Unsupported Herdr versions, a hung version probe, or a failed native signal retain regular process-detection startup rather than blocking access.

- **Pi completion follows the complete background-work lifecycle** ([REQ-TERM-038](terminal.md#req-term-038-herdr-semantic-status-owns-completion-readiness) and [REQ-TERM-039](terminal.md#req-term-039-herdr-completion-delivery-is-readiness-oriented) added as Implemented; [REQ-TERM-029](terminal.md#req-term-029-herdr-status-gated-terminal-completion) remains Implemented). This entry records the former five-minute Pi lifecycle timer and Classic delivery. The 2026-08-29 change above replaced that trigger with Herdr semantic status while retaining fixed readiness copy.

- **Pi can use Herdr as an agent-control plane** ([REQ-AGENT-173](agents.md#req-agent-173-pi-can-orchestrate-coding-agents-through-herdr) and [REQ-AGENT-174](agents.md#req-agent-174-pi-safely-controls-herdr-topology) added as Implemented). Default and advanced Pi sessions now carry an on-demand skill for focusing current tabs, creating and safely closing splits, creating helper panes, starting named agents, sending and steering tasks, waiting on lifecycle state, and reading terminal transcripts. The always-loaded system instruction spends one sentence on discovery; the skill first proves that Pi is inside a live Herdr pane and otherwise leaves plain Codeflare terminals alone. Working-agent steering is asynchronous, so immediate settlement is not completion evidence.

- **Herdr preserves Pi's image-owned Caveman policy** ([REQ-AGENT-172](agents.md#req-agent-172-herdr-preserves-the-pi-extension-policy) added as Implemented). Herdr's private XDG root previously redirected Caveman away from `~/.pi/agent/caveman.json`, causing the extension's full-mode, visible-status defaults to replace Codeflare's lite, status-hidden policy. Herdr now gives Pi its canonical configuration root explicitly while retaining private Herdr state.

- **Restored Pi readiness follows live transcript initialization** ([REQ-TERM-033](terminal.md#req-term-033-durable-herdr-structural-session-recovery) AC5-AC7 added; remains Implemented). Herdr restores persisted agent metadata before it starts `pi --session`, so metadata alone can make Codeflare ready while Pi still loads its transcript. With pinned Herdr v0.8.2, bootstrap now waits for every restored Pi pane to establish official full-lifecycle authority after `session_start`; version drift or a bounded wait failure preserves existing metadata readiness as a compatibility fallback for Pi and every other agent.

- **Browser-hosted Herdr receives its standard keyboard prefix** ([REQ-TERM-037](terminal.md#req-term-037-browser-keyboard-interaction-with-herdr) added as Implemented). Browsers reserve `Ctrl+B` for bookmarks, preventing Herdr's standard prefix map from activating reliably. Herdr sessions now suppress that browser action and send the canonical control prefix through the existing terminal transport; Herdr remains authoritative for Help, Settings, tabs, panes, splits, workspaces, and other following action keys, while classic keyboard handling stays unchanged.

- **Malformed failed-CI triage receives a visible correction path** ([REQ-AGENT-170](agents.md#req-agent-170-joint-review-and-ci-triage) AC6 added; remains Implemented). A triage row used `CI_RESULT failure: …` instead of the required exact `CI_RESULT failure` value, so review enforcement correctly withheld acknowledgement but silently left root without the separate FIX reminder. Pi now distinguishes a present malformed table from absent triage, emits one exact-row correction follow-up, and completes the same round only after canonical triage.

- **Browser-hosted Herdr receives explicit SGR mouse input** ([REQ-TERM-036](terminal.md#req-term-036-browser-pointer-interaction-with-herdr) added as Implemented; [REQ-MOB-017](mobile.md#req-mob-017-fullscreen-application-touch-scrolling) corrected; remains Implemented). Integration showed that xterm did not reliably encode Herdr's captured hardware or synthesized touch events, leaving tabs, settings, panes, and agents inaccessible by pointer. A Herdr-only browser adapter now maps terminal-cell clicks, hardware drags, modifier-aware right clicks, touch taps, and wheels directly to SGR reports while classic keeps its existing input path.

- **Herdr bootstrap follows the pinned runtime's canonical snapshot envelope** ([REQ-TERM-005](terminal.md#req-term-005-herdr-runtime-and-configured-agent-startup) corrected; remains Implemented). Integration stayed at “Preparing terminal” because the launcher looked for a focused pane directly under `result` while official Herdr v0.8.2 returns it under `result.snapshot`. Launcher fixtures now use the official response shape, and packaged-image verification asks the pinned binary to validate every managed configuration key before deployment.

- **The immutable Node runtime overlays patched pacote before deployment scanning** ([REQ-OPS-046](operations.md#req-ops-046-fixable-dependencies-in-immutable-runtime-artifacts) amended; remains Implemented). The pinned Node image bundled pacote 21.5.0 after CVE-2026-9496 became fixable in 21.5.1, so the image now verifies and installs the exact fixed package artifact into npm, then loads and version-checks that runtime before the fail-closed Trivy gate.

- **Herdr structural session state becomes durable through normal home sync** ([REQ-TERM-033](terminal.md#req-term-033-durable-herdr-structural-session-recovery) added as Implemented). Herdr's official `session.json` now lives under `~/.codeflare` and follows the existing initial restore, 15-minute bisync, explicit Sync, and final-sync boundaries. Workspaces, tabs, panes, layout, cwd, focus, and supported native agent references may recover after replacement; pane history, sockets, logs, user settings, and arbitrary running processes remain ephemeral.

- **Herdr becomes an opt-in immutable per-session terminal mode while full classic topology returns** ([REQ-AGENT-003](agents.md#req-agent-003-agent-cli-auto-started-in-tab-1), [REQ-TERM-001](terminal.md#req-term-001-terminal-surface-count-follows-session-mode), [REQ-TERM-005](terminal.md#req-term-005-herdr-runtime-and-configured-agent-startup), [REQ-TERM-006](terminal.md#req-term-006-herdr-owns-in-session-terminal-topology), [REQ-TERM-007](terminal.md#req-term-007-classic-topology-remains-available), [REQ-TERM-032](terminal.md#req-term-032-herdr-clipboard-compatibility-boundary), and [REQ-MOB-017](mobile.md#req-mob-017-fullscreen-application-touch-scrolling) amended; [REQ-TERM-034](terminal.md#req-term-034-terminal-mode-assignment-is-immutable) and [REQ-TERM-035](terminal.md#req-term-035-terminal-readiness-follows-mode-and-workspace) added; all are Implemented; [AD146](../../documentation/decisions/README.md#ad146-terminal-mode-is-an-immutable-per-session-choice) supersedes AD145). A server-backed preference defaults off and affects only new sessions. Settings marks Herdr as beta and contrasts its workspaces, splits, panes, and agent status with classic tabs and tiling. The Worker stamps `classic` or `herdr`; missing or invalid historical stamps resolve classic. Classic retains six outer terminals, tabs, labels, tiling, saved layouts, right-click paste, and existing mobile behavior; unknown or tab-count-incompatible saved layouts reset to disabled tabbed mode. Herdr retains one outer terminal, receives browser terminal mouse input, and owns inner topology. Startup, routing, prewarm, cleanup, clipboard, MultiView, and frontend ownership use the persisted stamp rather than current preference, and Herdr readiness never bypasses configured-command bootstrap on timeout; an expired unbootstrapped prewarm terminates the host for container recovery instead of remaining permanently unready.

- **Web Push is optional and absent configuration removes its Settings control** ([REQ-OPS-013](operations.md#req-ops-013-deploy-command-and-post-deploy-hooks) AC6-AC7, [REQ-TERM-025](terminal.md#req-term-025-per-device-notification-enrollment) AC6, and [REQ-SEC-023](security.md#req-sec-023-agent-notification-capability-boundaries) AC7 amended; all remain Partial). Deployments with no VAPID fields proceed without Web Push. Any partial or invalid sender identity still fails before promotion, configured values remain masked, and authenticated Settings renders notification enrollment only when the complete sender configuration is available.

- **Managed reconciliation uses compact environment-update status copy** ([REQ-AGENT-049](agents.md#req-agent-049-auto-upgrade-preseed-on-release) AC5 and [REQ-STOR-022](storage.md#req-stor-022-managed-reconciliation-admission) AC2 narrowed; [REQ-AGENT-175](agents.md#req-agent-175-environment-update-ui-lockdown) added; all Implemented). Dashboard and session-menu New Session controls now display "Updating" with an environment-specific accessible label during seed sync or managed reconciliation. A managed update waiting for an owning session to stop displays "Update pending" and blocks both entry points.

## 2026-08-27

- **Herdr named-session sockets stay within Linux path limits** ([REQ-TERM-005](terminal.md#req-term-005-herdr-runtime-and-configured-agent-startup) AC2 corrected; remains Implemented). Deployed integration evidence showed maximum-length session IDs generated a 113-byte Herdr client socket, so the server could not bind Linux's 108-byte `sun_path` and the outer terminal exited after readiness timeout. The launcher now places Herdr's XDG config root directly at the private per-session runtime root, producing a maximum 106-byte client socket, and failure cleanup relies on process-owned descriptor release without an EXIT trap that can reference an unwound local variable.

- **Herdr terminal compatibility uses fixed local notifications and bounded OSC 52** ([REQ-TERM-031](terminal.md#req-term-031-herdr-notification-compatibility-boundary) and [REQ-TERM-032](terminal.md#req-term-032-herdr-clipboard-compatibility-boundary) added as Implemented). Inner Herdr panes use one enum-only loopback helper to enqueue through the existing primary Session notification queue because the inner emulator consumes OSC 777. The browser accepts only bounded standard-selector UTF-8 clipboard writes, leaves permission authority with the existing setting, and gives Herdr right-click menu ownership. This compatibility layer remains a separate removable commit from the core runtime integration.

- **Initial Herdr integration replaced Codeflare-owned terminal tabs and within-session tiling** ([REQ-TERM-001](terminal.md#req-term-001-terminal-surface-count-follows-session-mode), [REQ-TERM-005](terminal.md#req-term-005-herdr-runtime-and-configured-agent-startup), [REQ-TERM-006](terminal.md#req-term-006-herdr-owns-in-session-terminal-topology), [REQ-TERM-007](terminal.md#req-term-007-classic-topology-remains-available), [REQ-TERM-009](terminal.md#req-term-009-process-label-ownership-follows-terminal-mode), and [REQ-TERM-030](terminal.md#req-term-030-herdr-owns-inner-pane-focus-lifecycle); [AD145](../../documentation/decisions/README.md#ad145-herdr-owns-topology-inside-one-codeflare-terminal-surface)). This entry records the initial universal single-surface implementation; the 2026-08-28 entry above restores classic as the default complete mode while retaining this ownership model inside opt-in Herdr sessions.

- **Enterprise managed resources have an applied persistence policy** ([REQ-SETUP-013](setup.md#req-setup-013-managed-environment-configuration) and [REQ-STOR-021](storage.md#req-stor-021-managed-content-ownership) extended; [REQ-SETUP-015](setup.md#req-setup-015-managed-resource-persistence-controls), [REQ-SETUP-016](setup.md#req-setup-016-managed-resource-policy-safety), [REQ-STOR-028](storage.md#req-stor-028-canonical-managed-resource-persistence-policy), [REQ-STOR-029](storage.md#req-stor-029-managed-resource-reconciliation), [REQ-STOR-030](storage.md#req-stor-030-managed-resource-policy-loading), [REQ-STOR-031](storage.md#req-stor-031-managed-resource-container-sync), [REQ-STOR-032](storage.md#req-stor-032-exclusive-managed-resource-boundaries), [REQ-ENTERPRISE-027](enterprise-mode.md#req-enterprise-027-managed-resource-admission-and-transport), [REQ-ENTERPRISE-028](enterprise-mode.md#req-enterprise-028-managed-resource-request-classification), [REQ-ENTERPRISE-029](enterprise-mode.md#req-enterprise-029-managed-resource-egress-enforcement), and [REQ-ENTERPRISE-030](enterprise-mode.md#req-enterprise-030-managed-resource-storage-enforcement) added; all Implemented; [AD147](../../documentation/decisions/README.md#ad147-active-managed-resource-policy-supersedes-provenance-ownership) accepted). Setup normalizes and explains mutable, immutable-with-personal-resources, and exclusive modes, then rolls policy changes per idle user bucket without draining the deployment. Existing managed reconciliation derives, writes, reads, verifies, and finally stamps one canonical user-bucket path policy; exclusive cleanup bounds include exact root objects, and per-object batch errors block policy stamping. Protected session admission and Storage mutations validate full desired/applied identity through one curation release digest; strict `EgressController` accepts canonical S3 multi-delete XML and denies protected mutation forms immediately before exact scoped signing with privacy-safe decision logs. Container-restored policy only generates common bisync exclusions after initial restore. Mutable behavior and unrelated personal paths remain unchanged; no policy KV store, cache object, database, queue, new reconciler, or Durable Object persistence was added.

- **Goal prompts stay compact and merge transitions preserve review consent** ([REQ-AGENT-111](agents.md#req-agent-111-native-goal-workflow-in-pi-sessions) AC6 added; [REQ-AGENT-168](agents.md#req-agent-168-review-boundary-classification) AC3-AC5 and [REQ-AGENT-171](agents.md#req-agent-171-user-scoped-review-completion-and-common-consent) AC1/AC3 corrected; all remain Implemented). Image-patched Goal triggers retain objective and completion identity without repeating terminal wait or blocked-tool coaching. Successful Git and GitHub PR merges that change the active checkout or full `HEAD` now reuse marker-or-dialog consent for the resulting exact open protected-base PR; unchanged, failed, detached, unsynchronized, or ineligible results remain inert.

- **Reviewer completion requires standalone exact prompt assignments** ([REQ-AGENT-170](agents.md#req-agent-170-joint-review-and-ci-triage) remains Implemented). Pi emits copy-ready `scope`, range or base, and lane output-path assignments on separate lines. Correlation accepts only exact whole-line values, so punctuation, prefixes, suffixes, prose examples, and one-line concatenation cannot falsely earn reviewer completion; behavioral coverage retains valid multiline launches. Claude accepts only assistant-originated successful background Bash launches through the emitted canonical runner with exactly the emitted head and boundary environment, bound to the exact PR, lane, and scope; stale, quoted, substitute, environment-altered, or forged same-PR commands cannot acknowledge completion.

- **Review completion follows the user and interrupted rounds carry no authority** ([REQ-AGENT-171](agents.md#req-agent-171-user-scoped-review-completion-and-common-consent) and [REQ-STOR-027](storage.md#req-stor-027-review-completion-marker-sync) added as Implemented; [AD144](../../documentation/decisions/README.md#ad144-user-scoped-review-completion-uses-marker-or-dialog-ingress) supersedes AD142). Pi and Claude resolve the active checkout at startup, resume, clone, switch, checkout, pull, push, PR creation, and PR reopen, then honor an exact user-scoped marker. Successful checked-out-branch push, PR creation, and PR reopen retain deterministic automatic review-and-CI launch; other misses offer the same mark-or-launch choice. Immutable markers retain ten heads per repository and branch for 30 days, sync through the common R2 home filters, and signal the existing daemon after local acknowledgement. Active review coordination is ephemeral; stopped work, reload, and container replacement do not recover partial lanes, CI, counters, or old delivery authority. Canonical triage after all mandated reviewers and terminal exact-head CI—success, failure, or timeout—writes the marker immediately before FIX. Delivery targets must match the checked-out repository, branch, PR, and head; marker identity includes protected base; only `review-state/v1/**` syncs; and no executable review path reads legacy `.git/sdd-review-*` state.

## 2026-08-26

- **Strict Enterprise R2 interception retains per-user bucket authority** ([REQ-SEC-003](security.md#req-sec-003-per-user-r2-tokens-scoped-to-user-bucket) remains the credential-scope dependency, [REQ-ENTERPRISE-023](enterprise-mode.md#req-enterprise-023-strict-gateway-egress-controller-transport) AC2 retains account-scoped routing, and [REQ-ENTERPRISE-026](enterprise-mode.md#req-enterprise-026-strict-r2-interception-preserves-user-bucket-authority) owns bound-bucket authorization and scoped signing; all remain Implemented; [AD143](../../documentation/decisions/README.md#ad143-strict-r2-interception-signs-only-with-the-bound-users-scoped-credential) supersedes AD87's signer authority). The catch-all receives the existing DO-held bucket-scoped credential and exact bound bucket through Worker-side props. Path-style and virtual-hosted requests for another bucket fail before any send, missing scoped credentials fail closed, and deployment-wide R2 credentials no longer sign intercepted user traffic. Restart payloads restore the memory-only pair after a Durable Object wake, and a changed pair refreshes an already-wired strict interceptor before later traffic. Placeholder containment, streaming, SSE-C, Governed Mode, and non-strict behavior remain unchanged.

- **Claude CI completion uses its production transport** ([REQ-AGENT-070](agents.md#req-agent-070-claude-on-demand-ci-monitoring-policy), [REQ-AGENT-170](agents.md#req-agent-170-joint-review-and-ci-triage), and [REQ-OPS-049](operations.md#req-ops-049-cleanup-safe-coordination-and-background-work) corrected; remain Implemented). PR-boundary directives launch one attached background `ci-monitor` Agent after reviewers. Native terminal evidence is bound to that Agent's tool-use ID and exact canonical repository, PR, and head; the bounded monitor creates no detached script, PID, or result-log artifact. CI lookup uses the exact commit SHA, so Git branch text never enters the monitor command, and nonzero GitHub CLI results remain unavailable even when they include parseable stdout.

- **Review evidence is correlated and explicit** ([REQ-AGENT-168](agents.md#req-agent-168-review-boundary-classification), [REQ-AGENT-169](agents.md#req-agent-169-clone-consent-lifecycle), and [REQ-AGENT-170](agents.md#req-agent-170-joint-review-and-ci-triage) added as Implemented; [REQ-AGENT-036](agents.md#req-agent-036-pr-boundary-review-trigger-conditions), [REQ-AGENT-063](agents.md#req-agent-063-pr-boundary-candidate-detection), [REQ-AGENT-080](agents.md#req-agent-080-unified-pi-pr-boundary-launch-plan), [REQ-AGENT-098](agents.md#req-agent-098-pi-review-triage-acknowledgement-barrier), [REQ-AGENT-121](agents.md#req-agent-121-checked-out-branch-boundary-synchronization), [REQ-AGENT-132](agents.md#req-agent-132-pr-delivery-and-existing-head-consent), [REQ-AGENT-153](agents.md#req-agent-153-in-flight-delivery-reconciliation-continuity), and [REQ-AGENT-171](agents.md#req-agent-171-user-scoped-review-completion-and-common-consent) corrected). Clone preexistence is target-specific, reviewer plans name required temporary reports, CI completion is tied to the launched monitor, and failure or timeout requires a dedicated exact-token CI row before acknowledgement. AD142 supersedes AD121 without rewriting its accepted history.

- **PR review ingress is narrow while completion is joint** ([REQ-AGENT-053](agents.md#req-agent-053-pi-native-review-result-correlation), [REQ-AGENT-074](agents.md#req-agent-074-pi-settled-review-handoff), [REQ-AGENT-080](agents.md#req-agent-080-unified-pi-pr-boundary-launch-plan), [REQ-AGENT-098](agents.md#req-agent-098-pi-review-triage-acknowledgement-barrier), [REQ-AGENT-121](agents.md#req-agent-121-checked-out-branch-boundary-synchronization), [REQ-AGENT-132](agents.md#req-agent-132-pr-delivery-and-existing-head-consent), [REQ-AGENT-153](agents.md#req-agent-153-in-flight-delivery-reconciliation-continuity), and [REQ-AGENT-171](agents.md#req-agent-171-user-scoped-review-completion-and-common-consent) remain Implemented). Push and PR creation launch automatically, successful clones alone may request consent for the checkout they produced, and ordinary Git or GitHub activity is inert. Delivery queries run immediately and retry after 1, 3, 5, 10, and 15 seconds. Reviewer-bearing acknowledgement and FIX now wait for terminal exact-head CI plus every required reviewer and their later joint triage table. Goal pause ownership, drift refusal, bypass, closure, and reload recovery remain unchanged.

- **Patched Code dependencies retire two bounded Trivy exceptions** ([REQ-SEC-011](security.md#req-sec-011-container-image-scanned-for-cves-before-deploy) and [REQ-OPS-002](operations.md#req-ops-002-docker-image-build-vulnerability-scan-and-registry-push) remain Implemented). Integration scan run `32956748637` proved the VS Code tree no longer contains `ip-address` 10.2.0 or `undici` 7.28.0. Both exact exceptions are removed, and behavioral coverage now rejects either tuple if it returns.

- **Committed runtime-lock integrity has one focused owner** ([REQ-OPS-054](operations.md#req-ops-054-committed-npm-runtime-lock-integrity) added as Implemented; contract extracted from [REQ-OPS-033](operations.md#req-ops-033-lock-backed-npm-bump-coherence)). Reviewed dependency security floors and exact Claude CLI/platform release coherence are owned separately from npm bump candidate routing and lock regeneration. Every platform-specific Claude package in the privileged npm runtime lock must match the exact CLI manifest pin, preventing image installation from failing on a partially advanced optional dependency set.

- **Impeccable decision and asset-audit workflows retain bounded user intent** ([REQ-AGENT-163](agents.md#req-agent-163-impeccable-browser-question-idle-lifecycle), [REQ-AGENT-165](agents.md#req-agent-165-impeccable-next-hand-delivery-lifecycle), [REQ-AGENT-164](agents.md#req-agent-164-impeccable-raster-scan-traversal), [REQ-AGENT-166](agents.md#req-agent-166-impeccable-raster-prompt-recovery), and [REQ-AGENT-167](agents.md#req-agent-167-impeccable-raster-scan-result-contract) added as Implemented). Question waits use the same configurable idle grace as their retained server, while validated next-hand delivery keeps its separate short claim window. Prompt-metadata scans recursively report missing intent with their existing exit contract, skip nested hidden/dependency trees, and never follow broken, cyclic, or boundary-escaping symbolic links. Spawned behavioral coverage exercises both Claude and Pi copies.

- **Active subagent resume remains guarded after the 19.3.2 review** ([REQ-AGENT-159](agents.md#req-agent-159-active-subagent-resume-guard) remains Implemented). Upstream still routes any session-ready record to `manager.resume()` without rejecting queued or running status. The independent Codeflare guard and its behavioral coverage therefore remain, and the fail-closed compatibility sentinel advances to the reviewed package version.

## 2026-08-25

- **Mobile GitHub and Storage flip faces use independent content geometry** ([REQ-GITHUB-012](github.md#req-github-012-responsive-github-and-storage-panel-allocation) AC3-AC5 corrected; remains Implemented). Single-panel mobile and short-window layouts retain each face's measured natural height and apply the current viewport cap only to the active face. Flipping from GitHub to R2 Storage therefore adopts Storage's own content height instead of retaining sibling geometry; flipping back restores GitHub's height, and viewport resizing refreshes the cap. Desktop and tablet split allocation remain unchanged.

- **Every session image includes lock-pinned Oxlint** ([REQ-OPS-051](operations.md#req-ops-051-image-owned-oxlint-lifecycle) added as Implemented). Image-owned `oxlint` is exposed on the container-wide command path for every coding-agent selection and executed during image assembly after alternate platform bindings are pruned. Its dedicated image lock stays outside managed-seed runtime compatibility and receives cooldown-backed weekly Dependabot updates.

- **Hosted image-build critical path is shorter and measurable** ([REQ-OPS-050](operations.md#req-ops-050-hosted-image-build-critical-path-optimization) and [REQ-OPS-052](operations.md#req-ops-052-concurrent-image-security-preparation) added as Implemented). Pi extension, Browser IDE, and generated seed edits now retain unrelated expensive dependency/toolchain layers while invalidating their own late assembly; fresh builds upload plain BuildKit timing evidence; vulnerability scanning, CycloneDX generation, and locked Wrangler preparation run concurrently after one DB preparation; all are awaited and bounded scan enforcement plus SBOM upload still precede push. Runner-owned Trivy scratch/cache directories also restore without the prior root-owned tar metadata warning.

- **PR test matrices use current measured work** ([REQ-OPS-003](operations.md#req-ops-003-pr-checks-run-lint-test-typecheck-and-security-audit) AC3 updated and [REQ-OPS-045](operations.md#req-ops-045-parallel-pr-checks-performance) AC3 retained; both Implemented). The Workers backend expands from six to eight duration-weighted groups plus its independent Node-runtime leg. Host test partitions are regrouped from exact-head timings so its two jobs carry approximately equal native-test work while retaining each partition exactly once.

- **CI security evidence follows the refactored pipeline** ([REQ-OPS-018](operations.md#req-ops-018-weekly-fuzz-testing) AC6-AC7 added, [REQ-OPS-021](operations.md#req-ops-021-workflow-file-static-analysis) verified unchanged, [REQ-OPS-051](operations.md#req-ops-051-image-owned-oxlint-lifecycle) AC1-AC5 refined, and [REQ-OPS-053](operations.md#req-ops-053-dependency-review-evidence-exceptions) added; all Implemented). Managed-release winner and conflict properties join the 50,000-run fuzz suite. Packaged-image smoke executes exact Oxlint instead of treating Dockerfile text as runtime proof. Dependency Review keeps vulnerability, license, and available OpenSSF evidence while limiting Apache-2.0 exceptions to six exact Codex platform releases. Zizmor continues to gate all `.github/` changes and publish lockstep SARIF without a scope change.

- **Session start consumes only the persisted managed-release snapshot** ([REQ-AGENT-162](agents.md#req-agent-162-session-start-managed-release-snapshot) added as Implemented). Session start performs no managed repository or release-cache refresh, reads only the compatible persisted active pointer, and uses a previously applied verified bucket release as its sole degraded fallback. Dashboard reconciliation retains ownership of repository refresh.

- **Container-lifetime runtime state survives disposable temporary cleanup** ([REQ-OPS-047](operations.md#req-ops-047-cleanup-safe-synchronization-state), [REQ-OPS-048](operations.md#req-ops-048-cleanup-safe-service-and-browser-ide-state), and [REQ-OPS-049](operations.md#req-ops-049-cleanup-safe-coordination-and-background-work) added as Implemented). Synchronization work, service PID/status records, Browser IDE data and triggers, graph locks, context-mode scratch, and detached CI-monitor logs now live under root-owned `/run/codeflare` subdirectories. Build-stage files and disposable counters may remain in `/tmp`; no session KV projection or hot-path scan is introduced.

- **One Session Agent terminal claim spans Browser IDE windows and server generations** ([REQ-IDE-048](browser-ide.md#req-ide-048-default-workspace-and-dashboard-owned-vs-code-sessions) AC4 amended; remains Implemented). Delayed terminal lookup still reuses hydrated terminals, while one atomic container-scoped `/run/codeflare/openvscode/session-agent.claimed` marker prevents separate browser-window extension hosts—or a restarted code-server—from each creating another `Codeflare Session Agent` when their local `window.terminals` view is empty.

- **Browser IDE card activation replaces the separate Open control** ([REQ-IDE-049](browser-ide.md#req-ide-049-dashboard-vs-code-startup-and-recovery) AC1-AC2 amended, [REQ-IDE-050](browser-ide.md#req-ide-050-browser-ide-status-and-ownership) AC1-AC4 amended, and [REQ-IDE-054](browser-ide.md#req-ide-054-browser-ide-card-activation) added; all remain Implemented). Stopped, failed, and ready VS Code cards now perform Start, Retry, and Open directly; preparing and stopping cards retain enabled child controls without parent button semantics. A transparent whole-card selection button preserves the visual design while menu and timer buttons remain separate accessibility controls. Readiness dots follow VS Code state rather than terminal WebSocket state.

- **VS Code readiness repair preserves authoritative session state** ([REQ-IDE-049](browser-ide.md#req-ide-049-dashboard-vs-code-startup-and-recovery) AC4-AC5 and [REQ-SESSION-009](session-lifecycle.md#req-session-009-container-destroy-wipes-session-state) AC4 remain Implemented). Healthy editor metrics and successful proxy traffic fresh-read the primary `session:*` record before merging only readiness, activity, and metrics. Metrics also check the durable shutdown marker before their final read, so Stop/Delete suppresses later writes. Recovery adds no secondary session projections or prefix scans.

- **Metadata compatibility remains bounded and separately owned** ([REQ-SESSION-028](session-lifecycle.md#req-session-028-session-metadata-projection-compatibility) restored as Implemented; [REQ-SESSION-007](session-lifecycle.md#req-session-007-running-session-count-limited-per-tier) AC1 and [REQ-SESSION-010](session-lifecycle.md#req-session-010-session-status-observable-from-dashboard) AC1 focused). The target session retains its required existence read; non-target comparison entries and batch entries stay on the zero-fallback-read path when metadata exists. Only bounded legacy entries without metadata receive compatibility `KV.get` reads. The primary `session:*` record remains authoritative, with no concern overlays or prefix scans.

## 2026-08-24

- **Shared managed seed paths require curation synchronization** ([REQ-AGENT-156](agents.md#req-agent-156-bounded-lossless-pi-prompt) AC6-AC7 and constraints amended; remains Implemented). Delivery of a Codeflare fallback seed change is now explicitly incomplete when the same manifest-owned path remains stale in `codeflare-curation`. The ownership ledger fails closed when this synchronization duty is absent, while protected curation verification owns the pre-publication byte check; image-owned paths and private curation content remain independently owned.

- **Pi startup headers fit narrow Browser IDE terminals** ([REQ-AGENT-161](agents.md#req-agent-161-pi-startup-header-terminal-width-safety) added as Implemented). The custom startup header now treats Pi's supplied terminal width as authoritative instead of enforcing a 44-cell inner minimum that produced a 46-cell line in a 44-cell mobile pane and terminated Pi. Behavioral coverage exercises desktop, narrow mobile, and degenerate widths.

- **Goal runtime loads the reviewed control and cadence transform** ([REQ-AGENT-111](agents.md#req-agent-111-native-goal-workflow-in-pi-sessions) AC2 amended; remains Implemented). Goal 0.53.0 publishes a generated `dist/index.ts` runtime alongside the authored source transformed by the image. Image construction now accepts the published `dist/index.ts` declaration or its already-transformed `src/index.ts` state, normalizes the sole Pi entrypoint to patched source, and warms the same installed path. Exact-archive tests load the package-declared entrypoint and prove review-owned pause/FIX resume plus workflow-mutex behavior, preventing generated distribution bytes from bypassing the reviewed control and continuation patches.

- **Advanced users can choose Terminal or VS Code for future sessions** ([REQ-IDE-048](browser-ide.md#req-ide-048-default-workspace-and-dashboard-owned-vs-code-sessions), [REQ-IDE-049](browser-ide.md#req-ide-049-dashboard-vs-code-startup-and-recovery), and [REQ-IDE-050](browser-ide.md#req-ide-050-browser-ide-status-and-ownership) added; [REQ-IDE-003](browser-ide.md#req-ide-003-ide-lifecycle-and-availability), [REQ-IDE-047](browser-ide.md#req-ide-047-bash-first-browser-ide-terminals), and [REQ-SESSION-001](session-lifecycle.md#req-session-001-session-creation-with-name-and-agent-type) amended; all Implemented). Terminal remains the backward-compatible default. New VS Code sessions stay dashboard-owned, eagerly warm the existing editor, reuse the existing Codeflare Session Agent profile, create no host terminal, and open through one explicit session-keyed browser tab; existing sessions retain their snapshot when preferences or mode change.

- **Late Pi extension loading survives disposable temporary-file cleanup** ([REQ-AGENT-160](agents.md#req-agent-160-durable-late-pi-extension-loading) added as Implemented). Startup now exports a stable `/run/codeflare/pi-tmp` root before the terminal host starts and exposes the image-baked writable Jiti cache beneath it. Interactive Plan Mode chunks and other late transpiles therefore remain writable even if `/tmp` is cleaned during a running container.

- **Goal startup enforces a three-minute continuation interval** ([REQ-AGENT-129](agents.md#req-agent-129-goal-continuation-settings-policy) AC1-AC3 amended; remains Implemented). Every successful container start now writes `continuationLimits.minIntervalMs: 180000`, replacing persisted zero or shorter values that caused immediate repeated continuations while preserving unrelated Goal preferences. The latest-settled timer restart, stale-marker guards, cancellation, and upstream zero-delay compatibility remain unchanged; an already running Pi process must restart to load the corrected policy.

- **Caveman uses lite response compression by default** ([REQ-AGENT-155](agents.md#req-agent-155-image-owned-caveman-response-policy) AC2 amended; remains Implemented). The image-owned, fail-closed startup policy now restores lite mode instead of full mode while keeping status/footer output disabled and preserving package, seed-exclusion, and JITI warm-cache ownership.

- **Tiled terminal focus preserves every visible pane** ([REQ-TERM-030](terminal.md#req-term-030-tiled-pane-focus-lifecycle) added as Implemented). Tiled panes now retain stable keyed descriptors while only their reactive focus state changes, matching the established MultiView lifecycle. Switching focus inside one session no longer remounts terminal components, reconnects their WebSockets, or reloads the other visible tiles; real tab ID additions, removals, and reordering still reconcile the affected pane tree.

- **Goal continuation pacing restarts after the latest settled activity** ([REQ-AGENT-130](agents.md#req-agent-130-goal-continuation-runtime-pacing) AC4 amended; remains Implemented, with exact-head CI owning regression verification). The existing positive interval could remain anchored to an earlier transient settled boundary while later Goal work continued, allowing an automatic continuation only seconds after the final visible stop. Each later settled boundary now clears and re-arms the one pending timer, preserving cancellation, stale-marker, long-delay, and zero-delay behavior while enforcing the full configured interval after the latest activity.

- **Pi starts context-mode off and exposes optional tool schemas on demand** ([REQ-AGENT-076](agents.md#req-agent-076-pi-context-mode-enablement-and-tool-extension-defaults) amended and [REQ-AGENT-158](agents.md#req-agent-158-bounded-initial-pi-tool-exposure) plus [REQ-AGENT-159](agents.md#req-agent-159-active-subagent-resume-guard) added; all are Implemented after exact-head deployment, signed managed release, activation, and live Standard and Advanced acceptance; [AD140](../../documentation/decisions/README.md#ad140-pi-starts-context-mode-off-and-exposes-optional-tool-schemas-on-demand) supersedes AD138). Fresh containers retain the exact context-mode package but write its disabled marker; a dedicated Standard-and-Advanced `/ctx` command explicitly enables or disables it and reloads Pi. After all tool registration and before provider serialization, capability keeps only read, Bash, edit, write, and itself active, then adds requested optional tools for the next model step and resets them on the next user prompt. Subagent activation includes result and steering controls, while a schema-free local guard blocks resume for queued or running records through pi-subagents' public service without changing the package or its working max-turn behavior. Curation owns complete managed extension bytes and Codeflare retains an independent embedded fallback; registered and active schemas are reported separately without a fixed tool-token limit.

## 2026-08-23

- **Pi terminal completion waits for sustained root inactivity** ([REQ-TERM-024](terminal.md#req-term-024-pi-native-terminal-notification-producer) narrowed to fixed producer behavior and [REQ-TERM-029](terminal.md#req-term-029-herdr-status-gated-terminal-completion) added; both were Implemented after exact-head deployment, signed managed release, KV activation, and live verification in both integration environments). This entry records the retired five-minute settled-turn trigger. Herdr semantic status replaced it on 2026-08-29; validated input-required signals remain immediate.

- **Pi prompt reduction has a bounded lossless migration contract** ([REQ-AGENT-156](agents.md#req-agent-156-bounded-lossless-pi-prompt) added as Partial). The isolated provider-boundary baseline is 32,416 characters and the public and managed target is at most 14,000 characters in default and advanced modes, with registered-tool schemas and additive project context reported separately. The shared compiler now derives one compact per-mode index from every model-invocable final source-root skill, keeps all skill files explicitly invocable at conventional paths, and omits only duplicate native XML entries. The public fallback still requires exact-head CI, while the independently owned managed policy requires an exact deployed compiler pin and guarded managed-mode evidence before the requirement can become Implemented.

- **Pi uses an image-owned compressed response policy without footer animation** ([REQ-AGENT-155](agents.md#req-agent-155-image-owned-caveman-response-policy) added; Implemented). The reviewed Caveman package is exact-pinned, integrity-locked, included in required Pi packages, and fail-closed JITI-warmed in the image. Caveman configuration is excluded from agent seeds; the image carries the full-mode, status-disabled policy that each successful container start atomically restores, while a missing, invalid, or unwritable policy blocks startup. Weekly Pi-extension discovery owns future coherent package, lock, runtime-registration, assertion, and generated-seed bumps.

- **Managed releases match the npm runtime available across agent surfaces, and inactive interactive browsers close sooner** ([REQ-AGENT-147](agents.md#req-agent-147-signed-managed-agent-configuration-releases), [REQ-BROWSER-001](browser-run.md#req-browser-001-browser-run-as-a-webfetch-fallback-claude-code-via-chrome-devtools-mcp), and [REQ-BROWSER-006](browser-run.md#req-browser-006-pi-interactive-browser-via-chrome-devtools-through-the-pi-mcp-adapter) amended; remain Implemented). Runtime compatibility now derives from the shared npm-tools, Claude Browser Run MCP, and Pi lockfiles instead of Pi alone, while both interactive registrations request a 30-second inactivity timeout rather than ten minutes.

- **Clone-created sessions retry their repository clone after an ephemeral stop** ([REQ-GITHUB-014](github.md#req-github-014-clone-created-session-resume) added; Implemented). Session KV already retained the validated repository and ref, but the idempotent Durable Object reconfiguration path discarded them after a wake. Resume now re-applies that in-memory startup directive and blocks if configuration restoration fails; entrypoint then attempts its established best-effort clone before agent startup, while the collision guard preserves an existing or workspace-synced target.

## 2026-08-22

- **Managed environments select the newest seed compatible with each deployed build** ([REQ-AGENT-154](agents.md#req-agent-154-build-compatible-managed-release-discovery) added and [REQ-STOR-020](storage.md#req-stor-020-managed-environment-reconciliation) AC2-AC3 amended; remain Implemented). Release discovery searches a bounded immutable history window instead of treating the latest global seed as authoritative, so environments on different dependency sets may select different releases while builds with identical lockfile hashes share the same newest match. An incompatible cached pointer cannot win merely through a higher global sequence.

- **Pi enables context-mode on fresh container startup** ([REQ-AGENT-076](agents.md#req-agent-076-pi-context-mode-enablement-and-tool-extension-defaults) AC1-AC2 and [REQ-AGENT-096](agents.md#req-agent-096-on-demand-pi-tool-activation) AC6 amended; remain Implemented; [AD138](../../documentation/decisions/README.md#ad138-context-mode-is-on-by-default-in-pi) supersedes AD107). Startup exposes context-mode skills and `ctx_*` tools through Codeflare's existing single foreground owner while filtering the package extension that would create a second owner. `/ctx off` remains a per-container opt-out, `/ctx on` restores the managed owner, and restart restores the enabled default.

- **Fixable vulnerabilities in immutable runtime artifacts are remediated and exercised rather than excepted** ([REQ-OPS-046](operations.md#req-ops-046-fixable-dependencies-in-immutable-runtime-artifacts) added; Implemented). The image replaces every affected npm and embedded Code OSS node-tar copy from one exact integrity-verified 7.5.21 artifact. Packaged-image smoke verifies each version, loads each runtime path, and completes archive round trips before the fail-closed deployment scan runs without a CVE-2026-73566 exception.

- **Automatic Pi delivery remains authoritative throughout asynchronous reconciliation** ([REQ-AGENT-171](agents.md#req-agent-171-user-scoped-review-completion-and-common-consent) retains established-plan behavior; [REQ-AGENT-153](agents.md#req-agent-153-in-flight-delivery-reconciliation-continuity) added as Implemented). Each push or PR-create boundary stays independently pending until its own authoritative lookup completes. Ordinary activity in the same repository waits without opening a competing choice, then reuses an established plan or automatically resumes normal eligibility when delivery proves ineligible.

- **Usage plan labels are limited to metered SaaS plans** ([REQ-SUB-018](subscription.md#req-sub-018-usage-dashboard-page) AC7 added; remains Implemented). Non-SaaS and custom unmetered usage pages show personal time without subscription tier or mode naming. Metered SaaS retains the centered tier-and-mode label.

- **Pi and Claude gain bounded, lazy-loaded local static checks** ([REQ-AGENT-052](agents.md#req-agent-052-pi-commit-attribution-and-local-build-hook-hardening) AC6 and [REQ-AGENT-157](agents.md#req-agent-157-managed-local-check-delivery-policy); remain Implemented). A short always-loaded rule keeps builds, tests, type checks, dependency analysis, installs, servers, and direct analyzers CI-only, while directing agents to lazy-load `safe-local-checks` when read-only lint or syntax feedback is useful. Its cross-repository wrapper uses only installed local analyzers at low priority with a three-minute deadline and no file-count limit; mutation, initialization, and watch flags fail closed. Both runtime guards permit only that wrapper, local results remain supplemental, and the user-only bypass is unchanged.

- **Goal and Plan Mode now share upstream workflow exclusion** ([REQ-AGENT-111](agents.md#req-agent-111-native-goal-workflow-in-pi-sessions) and [REQ-AGENT-152](agents.md#req-agent-152-native-plan-mode-workflow-in-pi-sessions) remain Implemented). Goal moves to the latest 0.53.0 release, preserving manual and review-owned pause/resume behavior while refusing overlap with Plan Mode through the shared session mutex. Plan Mode retains its manual reload verification for planning, completion, and implementation handoff.

- **Pi retains user consent without repeating it after a review plan launches** ([REQ-AGENT-132](agents.md#req-agent-132-pr-delivery-and-existing-head-consent) and [REQ-AGENT-171](agents.md#req-agent-171-user-scoped-review-completion-and-common-consent) remain Implemented). Git and GitHub commands still trigger an authoritative checked-out-branch PR check, fresh or switched-to heads still ask the user to launch or acknowledge, and confirmed push/PR creation remains automatic. Once either path launches a plan, later commands reuse that exact PR/head decision instead of showing another question; non-delivery lookup failures are one-shot and cannot attach themselves to a PR created later.

## 2026-08-21

- **Pi sessions gain a managed native Plan Mode workflow** ([REQ-AGENT-152](agents.md#req-agent-152-native-plan-mode-workflow-in-pi-sessions) added as Implemented). Every Pi session provides `/plan` with a start-authoritative read-only discovery policy, retained implementation context, explicit completion, and implementation handoff. Codeflare keeps the upstream package unchanged and writes no plan file automatically.

- **Boundary review aligns away-notification requirements with binding SDD shape and late suppression** ([REQ-SESSION-011](session-lifecycle.md#req-session-011-graceful-shutdown-with-final-sync) retains sync ownership; [REQ-SESSION-027](session-lifecycle.md#req-session-027-final-notification-drain-precedes-shutdown-sync), [REQ-TERM-025](terminal.md#req-term-025-per-device-notification-enrollment), [REQ-TERM-027](terminal.md#req-term-027-service-worker-notification-display-and-navigation), [REQ-TERM-028](terminal.md#req-term-028-notification-reconnect-reconciliation-and-cancellation), [REQ-TERM-026](terminal.md#req-term-026-claude-native-terminal-notification-producer), and [REQ-SEC-024](security.md#req-sec-024-agent-notification-delivery-trust-boundaries) take the split concerns). AC mapping is SESSION-011 AC8-AC10 → SESSION-027 AC1-AC7; TERM-023 AC6-AC8 → TERM-025 AC1-AC5 and TERM-027 AC1-AC3 while old AC9 remains TERM-023 AC6 and reconnect reconciliation becomes TERM-028 AC1-AC4; TERM-024 AC6-AC8 → TERM-026 AC1-AC3; SEC-023 delivery AC4-AC7 → SEC-024 AC1-AC6; OPS-013 AC6 becomes AC6-AC7. Exact named test anchors now resolve, and a client that becomes active during pending display sends a late suppress instead of confirming display. Deployment-dependent requirements remain Partial.

- **Away-notification implementation anchors match the implemented trust and delivery boundaries** ([REQ-SESSION-011](session-lifecycle.md#req-session-011-graceful-shutdown-with-final-sync) and [REQ-SESSION-027](session-lifecycle.md#req-session-027-final-notification-drain-precedes-shutdown-sync) are Implemented; [REQ-TERM-023](terminal.md#req-term-023-away-only-agent-notification-delivery), [REQ-TERM-025](terminal.md#req-term-025-per-device-notification-enrollment), [REQ-TERM-027](terminal.md#req-term-027-service-worker-notification-display-and-navigation), [REQ-TERM-028](terminal.md#req-term-028-notification-reconnect-reconciliation-and-cancellation), [REQ-TERM-024](terminal.md#req-term-024-pi-native-terminal-notification-producer), [REQ-TERM-026](terminal.md#req-term-026-claude-native-terminal-notification-producer), [REQ-SEC-022](security.md#req-sec-022-container-proxy-bearer-validation), [REQ-SEC-023](security.md#req-sec-023-agent-notification-capability-boundaries), [REQ-SEC-024](security.md#req-sec-024-agent-notification-delivery-trust-boundaries), [REQ-AUTH-018](authentication.md#req-auth-018-user-management-admin-panel), and [REQ-OPS-013](operations.md#req-ops-013-deploy-command-and-post-deploy-hooks) remain Partial). Source and behavioral coverage resolve fixed event mapping, event-specific dispositions, cancellation-aware grant, authenticated two-phase drain, trusted Session enrichment, deadline-cancelled Web Push, canonical clicks, final-drain ordering, subscription cleanup, and fail-closed VAPID validation. Missing acceptance remains explicit in each Partial requirement's Notes.

- **Away-only agent notifications use globally coordinated local suppression plus standards-based Web Push fallback** ([REQ-TERM-023](terminal.md#req-term-023-away-only-agent-notification-delivery), [REQ-TERM-025](terminal.md#req-term-025-per-device-notification-enrollment), [REQ-TERM-027](terminal.md#req-term-027-service-worker-notification-display-and-navigation), [REQ-TERM-028](terminal.md#req-term-028-notification-reconnect-reconciliation-and-cancellation), [REQ-TERM-024](terminal.md#req-term-024-pi-native-terminal-notification-producer), [REQ-TERM-026](terminal.md#req-term-026-claude-native-terminal-notification-producer), [REQ-SEC-022](security.md#req-sec-022-container-proxy-bearer-validation), [REQ-SESSION-027](session-lifecycle.md#req-session-027-final-notification-drain-precedes-shutdown-sync), [REQ-AUTH-018](authentication.md#req-auth-018-user-management-admin-panel), and [REQ-OPS-013](operations.md#req-ops-013-deploy-command-and-post-deploy-hooks) amended; [REQ-SEC-023](security.md#req-sec-023-agent-notification-capability-boundaries) and [REQ-SEC-024](security.md#req-sec-024-agent-notification-delivery-trust-boundaries) own trust boundaries). A bounded terminal-one queue coordinates client presence before local or Push delivery. Unprocessed events use an authenticated two-phase drain, trusted Session enrichment, exact-pinned RFC 8291 Web Push, canonical session paths, and pre-stop delivery without changing activity or wake behavior. Physical-device, suppression, pickup, drain-authentication, no-wake, and click-navigation evidence remains pending on integration.

## 2026-08-20

- **Native Inline Chat is edit-first and OpenAI Responses reasoning stays visible** ([REQ-IDE-025](browser-ide.md#req-ide-025-shared-ide-pi-surface-isolation), [REQ-IDE-027](browser-ide.md#req-ide-027-native-pi-panel-reasoning-and-bounded-progress), and [REQ-IDE-030](browser-ide.md#req-ide-030-native-inline-chat-result-envelope) amended; remain Partial pending fresh integration evidence). Editor turns now frame every invocation as a requested document change and reserve `noChange` for an already-satisfied request or one with no valid safe edit, so brief instructions such as “add a joke here” do not probabilistically fall into the former informational escape hatch. Known OpenAI Responses payloads receive the same exact single-result-tool constraint as Chat Completions, while panel reasoning requests upgrade provider summary mode from automatic to detailed; Qwen-shaped payloads remain unchanged and only provider-authored summaries—not hidden raw chain-of-thought—reach native thinking UI. The signed `codeflare-curation` runtime source and Codeflare's independent image fallback publish the same extension behavior.

- **Current-release reconciliation does not depend on disposable cache history** ([REQ-STOR-022](storage.md#req-stor-022-managed-reconciliation-admission) AC7 and [REQ-STOR-024](storage.md#req-stor-024-managed-release-application) AC5-AC7 added; remain Implemented). Replacing the deployment cache keeps only the current verified signed release. A user bucket can apply that current release without the deleted cache's historical bundle; bounded by-name cleanup removes prior-digest paths that the current release excludes from the resolved mode, while signed retirements cover globally removed paths. If Managed Environment is instead disabled while the prior release is unavailable, reconciliation fails before bucket mutation and retains the applied state rather than guessing which other prior managed paths to remove.

- **Managed release cache buckets identify their Worker deployment** ([REQ-STOR-025](storage.md#req-stor-025-managed-deployment-cache-migration) and [REQ-STOR-026](storage.md#req-stor-026-managed-deployment-cache-identity) added as Implemented). Cache names now combine a bounded sanitized Worker name with the existing account-and-Worker hash. Normal managed resolution recognizes only the exact deterministic former `codeflare-managed-<hash>` bucket, creates and verifies the active signed release in the new bucket, records an operational cache mapping without rewriting the repository/key trust selection, and then empties and deletes the old bucket. A failed population leaves the old cache active; a failed cleanup remains pending in the separate migration state and retries without scanning or prefix deletion.

- **Managed Browser IDE extension manifests are byte-authenticated** ([REQ-IDE-042](browser-ide.md#req-ide-042-additive-company-extension-reconciliation), [REQ-IDE-045](browser-ide.md#req-ide-045-company-extension-reconciliation-orchestration), and [REQ-STOR-024](storage.md#req-stor-024-managed-release-application) amended). Reconciliation stamps and transports a Worker-derived SHA-256 for the exact synthesized company-extension manifest. Browser IDE preparation, live reconciliation, and the generation-reap capture backstop reject different bytes. Missing or unverifiable active manifests perform no company-extension mutation and show the Settings remediation warning; personal extension intent remains in its separate manifest.

- **Managed-release decisions use the entitlement-clamped session mode** ([REQ-SEC-015](security.md#req-sec-015-blocked-user-cannot-self-upgrade-subscription) amended). Status polling, reconciliation, applied-release stamping, and container startup now resolve one billing-aware effective mode. A downgraded SaaS user with a stale Pro preference therefore reconciles to Standard instead of starting against previously applied Pro managed content.

- **Browser IDE terminals now open in Bash by default** ([REQ-IDE-047](browser-ide.md#req-ide-047-bash-first-browser-ide-terminals) added as Implemented). Every IDE inventory manages one Bash login-shell profile carrying the existing `MANUAL_TAB=1` bypass and one explicit `Codeflare Session Agent` login-shell profile without that bypass. New integrated terminals therefore start as plain Bash while users can still select the session's configured agent from the terminal profile menu. The change reuses existing shell initialization and does not add a launcher or agent process.

- **Pi review authorization and FIX delivery are correlated to one boundary cycle** ([REQ-AGENT-098](agents.md#req-agent-098-pi-review-triage-acknowledgement-barrier), [REQ-AGENT-126](agents.md#req-agent-126-pi-review-checkpoint-persistence-and-head-drift), [REQ-AGENT-132](agents.md#req-agent-132-pr-delivery-and-existing-head-consent), and [REQ-AGENT-171](agents.md#req-agent-171-user-scoped-review-completion-and-common-consent) amended; all remain Implemented). Pi freezes the initiating boundary tool-use ID and reuses a launch or acknowledgement choice only when recovering that exact cycle. A separate ordinary Git boundary at the same PR head asks again, the latest same-head review window owns its own FIX handoff, and an older same-head FIX cannot suppress it. Reviewer-plan, missing-work, and FIX deduplication are cycle-scoped; CI and the existing Git acknowledgement remain exact-SHA scoped. No persistence service, coordinator, or Claude lifecycle change is introduced.

## 2026-08-19

- **Managed-environment preference changes fail closed and preserve retry state** ([REQ-MEM-011](memory.md#req-mem-011-session-mode-storage-resolution-and-propagation) and [REQ-STOR-022](storage.md#req-stor-022-managed-reconciliation-admission) remain Implemented; [AD136](../../documentation/decisions/README.md#ad136-managed-environments-reconcile-signed-releases-before-session-start) context extended). A `sessionMode` change applies the no-hot-mutation session gate only while a managed release is active or was previously applied, so an unconfigured deployment keeps byte-identical baked behavior, and a failed managed-release lookup now fails closed with the typed update-pending error instead of being read as "no active release". A failed managed auto-reconcile propagates instead of being logged as non-fatal, and restores the pre-request preference document first so a retry reconciles rather than comparing against its own half-applied mode. Session-list polling reuses the resolver's freshness window for a settled release, consumes that window only on a completed check, and skips an overlapping probe while one is already in flight.

## 2026-08-18

- **Usage speaks to engineers in its own voice** ([REQ-SUB-024](subscription.md#req-sub-024-usage-page-product-context) added as Implemented). The Usage page now gives its developer audience a page-specific line about agents having the full environment while the local laptop stays out of it. Subscribe keeps its existing subscription positioning. The exact joke is deliberately not pinned in a prose-equality test.

- **Managed environments deliver signed company configuration through the existing safe R2 upgrade path** ([REQ-SETUP-013](setup.md#req-setup-013-managed-environment-configuration), [REQ-SETUP-014](setup.md#req-setup-014-managed-repository-credential-boundary), [REQ-AGENT-147](agents.md#req-agent-147-signed-managed-agent-configuration-releases), [REQ-AGENT-148](agents.md#req-agent-148-protected-managed-release-publication), [REQ-AGENT-149](agents.md#req-agent-149-shared-compiler-cli-compatibility), [REQ-AGENT-150](agents.md#req-agent-150-independent-managed-release-activation-validation), [REQ-AGENT-151](agents.md#req-agent-151-bounded-managed-release-streaming), [REQ-STOR-020](storage.md#req-stor-020-managed-environment-reconciliation), [REQ-STOR-021](storage.md#req-stor-021-managed-content-ownership), [REQ-STOR-022](storage.md#req-stor-022-managed-reconciliation-admission), [REQ-STOR-023](storage.md#req-stor-023-managed-release-status-and-discovery), [REQ-STOR-024](storage.md#req-stor-024-managed-release-application), [REQ-IDE-042](browser-ide.md#req-ide-042-additive-company-extension-reconciliation), [REQ-IDE-044](browser-ide.md#req-ide-044-exact-company-vsix-verification), [REQ-IDE-045](browser-ide.md#req-ide-045-company-extension-reconciliation-orchestration), and [REQ-IDE-046](browser-ide.md#req-ide-046-session-local-company-vsix-installation) added, all Implemented; [AD136](../../documentation/decisions/README.md#ad136-managed-environments-reconcile-signed-releases-before-session-start) accepted). One side-effect-free compiler continues to produce the baked seed and a private, immutable, draft-first release workflow signs exact deterministic gzip bytes. Setup verifies a repository-scoped release before selecting its configuration-fingerprint cache namespace. The Worker alone reaches GitHub, verifies immutable metadata, digest, Ed25519 signature, repository identity, monotonic sequence, runtime ABI, paths, limits, and extension records, then caches content-addressed assets in a deterministic deployment R2 bucket. User buckets reconcile only from that verified cache and only while no session owns the bucket; provenance preserves edits, applied state is stamped last, and container startup keeps its established restore/transcript/bisync order. The Browser IDE lazily verifies exact company VSIX bytes before personal restore while keeping company and personal intent distinct; failed company-extension removals remain protected and retry before personal restoration. A replacement signing public key activates only after Setup verifies its release without rolling back or conflicting with the active sequence; a repository-stable conditional pointer records the authoritative trust selection, while a concurrent lower-sequence loser performs bounded KV repair and fails explicitly if the pointer does not settle. Status polling reads only the verified active descriptor, and actual reconciliation validates and writes the unchanged signed gzip through bounded streaming rather than retaining the full expanded release. No second startup materializer, webhook, queue, daemon, database, poller, user offboarding path, PAT exposure, or VSIX persistence is introduced.

- **Setup reconfiguration cannot offboard users, and GitHub revocation fails open** ([REQ-SETUP-012](setup.md#req-setup-012-setup-wizard-step-sequence) and [REQ-GITHUB-005](github.md#req-github-005-disconnect-and-offboarding-revocation) amended). A full Setup payload no longer treats stored users absent from its explicit email list as stale, so changing deployment configuration cannot destroy sessions, credentials, control state, or R2 storage. GitHub App/OAuth revocation remains attempted and logged, but missing provider configuration, network failure, or a non-success response no longer retains the local token or aborts explicit cleanup. Production logs proved the old path emptied and deleted two enterprise user buckets before a later GitHub 404 stopped configuration. Proper authoritative enterprise offboarding is deferred to [issue #905](https://github.com/nikolanovoselec/codeflare/issues/905).

- **Native Inline Chat requires one truthful host-correlated result** ([REQ-IDE-025](browser-ide.md#req-ide-025-shared-ide-pi-surface-isolation), [REQ-IDE-029](browser-ide.md#req-ide-029-native-inline-chat-feedback), and [REQ-IDE-030](browser-ide.md#req-ide-030-native-inline-chat-result-envelope) amended; [REQ-IDE-043](browser-ide.md#req-ide-043-native-pi-provider-history-isolation) added as Partial; [AD135](../../documentation/decisions/README.md#ad135-inline-chat-requires-one-host-correlated-result) accepted). Inline mode replaces the edit-only, model-correlated proposal with one flat edit-or-no-change result whose request ownership stays in the active backend generation and serialized host turn. The OpenAI Chat Completions route receives only the exact forced result tool with parallel calls disabled, while unknown provider shapes remain unchanged. A minimal Inline system contract plus current-turn-only provider context removes persistent panel history without mutating it or dropping tool-validation feedback needed for a bounded retry. Edit results retain bounded host validation and native Keep/Close; informational no-change results render their explanation without invoking a text-edit method. No completion cap, session-global thinking change, custom provider, or speculative multi-provider adapter is introduced.

- **Claude Code and Pi transcript retention uses native activity rather than restored mtimes** ([REQ-STOR-012](storage.md#req-stor-012-main-session-transcript-cleanup) amended). Each agent now keeps ten main raw transcript files across its nested project/session directories. Strict native adapters rank recoverable Claude 2.1 and Pi version-3 transcripts by their latest timestamp; if one candidate has an unsupported shape, the entire agent falls back to path-safe mtime ordering for that pass. Existing Claude subagent, Pi task, and Codex behavior remains unchanged. Cleanup runs after restore and before every regular or final bisync, so bounded local state reaches R2 without involving Vault captures or extraction outputs.

- **Selected-text Inline Chat uses the proposal coordinate system** ([REQ-IDE-041](browser-ide.md#req-ide-041-native-chat-coordinate-representation) added as Partial pending fresh integration verification). The host previously serialized the invoking selection and whole range as one-based human-readable context while the proposal tool and validator required zero-based UTF-16 positions. Pi could copy a selected range into an otherwise valid proposal and have its line or end column rejected as `Invalid Inline Chat edit range`; cursor-only requests hid the mismatch because they omit selection context. Editor requests now snapshot host ranges without the display offset, while panel context deliberately keeps its existing one-based coordinates under REQ-IDE-041 AC1. Behavioral coverage exercises both boundaries without relaxing document-range validation.

## 2026-08-17

- **PR Checks stop redownloading immutable external tools** ([REQ-OPS-045](operations.md#req-ops-045-parallel-pr-checks-performance) amended). Host and workflow-audit lanes cache exact rclone, zizmor, and actionlint archives by platform plus version/checksum, verify integrity after every restore, and download only on a cache miss.

- **ADR indexes explain decisions without requiring a full-ledger read** ([REQ-AGENT-146](agents.md#req-agent-146-self-contained-adr-decision-index-summaries) added as Implemented; [AD133](../../documentation/decisions/README.md#ad133-adr-indexes-use-bounded-self-contained-summaries) accepted). The ADR ledger and canonical template now pair a concise decision label with one bounded, body-supported sentence naming the component, choice, driver or consequence, category, and current state; existing shape enforcement and documentation review reject vague, repeated, unbounded, or state-incomplete summaries without adding a framework file.

- **PR path classification fails safe when GitHub cannot generate a large diff** ([REQ-OPS-003](operations.md#req-ops-003-pr-checks-run-lint-test-typecheck-and-security-audit) constraint amended). The primary classifier remains selective. If its PR-files API fails, the fallback verifies the exact checked-out base and head commits, records their local changed-file set, and selects all six workload lanes. Behavioral host tests execute the owned script against a temporary Git repository and prove malformed or missing commit identities emit no outputs.

- **The weekly `pi-goal` preflight admits reviewed successor 0.49.7** ([REQ-AGENT-111](agents.md#req-agent-111-native-goal-workflow-in-pi-sessions), [REQ-AGENT-130](agents.md#req-agent-130-goal-continuation-runtime-pacing), [REQ-AGENT-144](agents.md#req-agent-144-review-owned-goal-pause-command-compatibility), and [REQ-OPS-020](operations.md#req-ops-020-shadow-pin-version-bump-automation) behavior unchanged). Bump run `32003306201`, job `95307753678`, reached the fail-closed version boundary before any package write because the compatibility window still ended at 0.49.5. Registry integrity and the downloaded 0.49.7 split lifecycle, commands, runtime tool-policy, and settings anchors match the reviewed 0.49 line. The host suite verifies and extracts that exact registry archive before applying every patch. Its revised pause path carries the review-owned non-abort option through the upstream stop request while retaining manual pause's default abort, and every direct continuation dispatch routes through its native single-flight scheduler, including bounded re-arms above Node's timer maximum, without adding the legacy timer. The bounded successor and integrity allowlists admit 0.49.7 while all other versions and source drift still fail closed.

- **User-managed Open VSX extensions gain bounded manifest continuity without persisting package bytes** ([REQ-IDE-002](browser-ide.md#req-ide-002-session-isolated-ide-not-bucket-stable), [REQ-IDE-016](browser-ide.md#req-ide-016-bounded-ide-state-capture-and-restore-ordering), and [REQ-IDE-017](browser-ide.md#req-ide-017-unsupported-ide-base-inventory-isolation) amended; [REQ-IDE-036](browser-ide.md#req-ide-036-persistent-user-managed-extensions), [REQ-IDE-037](browser-ide.md#req-ide-037-lazy-extension-restoration), [REQ-IDE-038](browser-ide.md#req-ide-038-extension-warning-acknowledgement), and [REQ-IDE-040](browser-ide.md#req-ide-040-user-extension-allowance-policy) added as Implemented). Pi, Claude, and unsupported selections remain immutable base inventories, symlink-seeded into one writable per-container extension layer. The built-in welcome extension requires the durable arbitrary-code acknowledgement before executing restored intent, lazily reinstalls missing exact versions from Open VSX after workbench startup, restores contributed settings after extension registration, then captures registry-truth identities and bounded settings into `~/.codeflare/ide-extensions.json`. Setting-only changes schedule that same capture, and welcome deactivation flushes a pending capture before the post-reap Python backstop closes the remaining registry debounce race. Only `.obsolete` proves uninstall, and that evidence overrides stale raw registry rows so gallery outages cannot erase intent. The exact manifest joins the existing R2 filter and signals the existing bisync daemon only after changed atomic writes. It records one durable arbitrary-code warning acknowledgement but no VSIX/extracted bytes, extension storage, SecretStorage, Accounts, enablement, keybindings, snippets, or secondary downloads. AD132 records the inherited root-capable extension trust, proposed-API, unsigned-gallery, and newest-wins concurrency consequences. GitHub Actions owns shell, manifest, managed-settings, packaged-bootstrap, registry, and complete-image evidence; there is no local, browser-automation, Chromium, or manual gate.

## 2026-08-16

- **Pi PR-boundary delivery survives queued follow-ups and ordinary session restoration without repeated decisions** ([REQ-AGENT-036](agents.md#req-agent-036-pr-boundary-review-trigger-conditions), [REQ-AGENT-074](agents.md#req-agent-074-pi-settled-review-handoff), [REQ-AGENT-098](agents.md#req-agent-098-pi-review-triage-acknowledgement-barrier), REQ-AGENT-110, [REQ-AGENT-126](agents.md#req-agent-126-pi-review-checkpoint-persistence-and-head-drift), [REQ-AGENT-132](agents.md#req-agent-132-pr-delivery-and-existing-head-consent), and REQ-AGENT-141 amended, and REQ-AGENT-145 added; all statuses remain Implemented). The existing evaluated transcript entry now checkpoints an exact-head launch or acknowledgement before follow-up delivery, so multiple same-head Git commands and normal resume cannot repeat the question or queue duplicate plans. Missing-work follow-ups remain single-flight until their queued message becomes visible. Existing-session startup or resume recovers one accepted-but-undelivered plan and one acknowledged-but-undelivered FIX handoff. Once that FIX is transcript-visible, a later PR head does not emit stale drift for the completed window. The latest persisted review window remains selectable when newer unpublished Git activity appears. Only ambiguous failed `gh pr create` results with HTTP 5xx or already-existing-PR evidence receive exact authoritative reconciliation; every other failed command remains inert. No new state store, review coordinator, background monitor, or lifecycle component is introduced.

- **PR path classification fails safe when GitHub cannot generate a large diff** ([REQ-OPS-003](operations.md#req-ops-003-pr-checks-run-lint-test-typecheck-and-security-audit) constraint amended). The primary classifier remains selective. If its PR-files API fails, the fallback verifies the exact checked-out base and head commits, records their local changed-file set, and selects every workload lane. Behavioral host tests execute the owned script against a temporary Git repository and prove malformed or missing commit identities emit no outputs.

- **Browser IDE branding uses existing supported and packaged surfaces** ([REQ-IDE-039](browser-ide.md#req-ide-039-codeflare-browser-ide-branding) added as Implemented). code-server receives its existing `--app-name Codeflare` override, so product-name dialogs no longer identify the editor as code-server. The Pi Chat participant and welcome panel retain their existing `media/agent.svg` path while that asset now contains the established Codeflare brand mark. No Code OSS patch, new component, or new asset path is introduced.

- **Fixed-workspace projection now shares the browser's canonical remote authority** ([REQ-IDE-015](browser-ide.md#req-ide-015-clean-browser-ide-url-and-private-workspace-selection) narrowed to private navigation and clean redirects; [REQ-IDE-035](browser-ide.md#req-ide-035-canonical-browser-ide-workspace-projection) added as Implemented; [REQ-IDE-033](browser-ide.md#req-ide-033-controller-owned-inline-review-lifecycle) remains Partial pending fresh deployed interaction evidence). Integration probe `uri-authority-probe-v1` proved the correct image and editor surface streamed the invoking extension-host `file:` URI, then the pinned controller opened an ordinary duplicate file in a side group. Pinned code-server emits a deliberate server-side `remote` authority and later replaces only `remoteAuthority` with `location.host`; the existing host projection had copied that placeholder into `folderUri`, while remote-extension-host edits transformed through the public authority. The host now projects the fixed folder with its already-validated canonical external `Host`, preserves every other workbench setting, and fails closed on malformed authorities. No Worker route, agent lifecycle, edit stream, pinned-host source, or review UI changes. CI and image smoke cover the placeholder/public split; one fresh integration session must still prove one-tab localized review, Keep, Undo, immediate follow-ups, and no missing-document error.

- **Proxied Inline failures gain bounded URI and tab-identity evidence before another lifecycle change** ([REQ-IDE-033](browser-ide.md#req-ide-033-controller-owned-inline-review-lifecycle) clarified; [REQ-IDE-034](browser-ide.md#req-ide-034-bounded-inline-lifecycle-diagnostics) added and decomposed into seven focused ACs as Implemented). Exact-host reproduction proved that a third-party participant receives the native one-tab Keep/Close lifecycle when edit and session URIs match, while integration still opens another tab and loses those controls. The leading unresolved boundary is remote scheme or authority translation behind the Worker proxy: the pinned controller unconditionally side-opens any modified URI different from the session URI, and that same mismatch leaves the invoking group without a modified-entry context key. Under [AD131](../../documentation/decisions/README.md#ad131-inline-diagnostics-retain-only-sanitized-resource-identity), one bounded editor request records a diagnostic revision, effective settings, sanitized request/stream scheme, authority, basename and input type, plus capped tab events and snapshots without userinfo, query, fragment, full paths, tab labels, document content, or panel instrumentation. No confirmation, notification action, Chat Editing command, navigation, proxy change, host patch, or extra process is introduced; one fresh integration run must classify rollout, surface, or URI identity before implementation continues.

- **Controller-owned Inline review replaces extension-owned confirmation and editor restoration** ([REQ-IDE-029](browser-ide.md#req-ide-029-native-inline-chat-feedback) and [REQ-IDE-030](browser-ide.md#req-ide-030-native-inline-chat-result-envelope) amended; former REQ-IDE-031 and REQ-IDE-032 removed; [REQ-IDE-033](browser-ide.md#req-ide-033-controller-owned-inline-review-lifecycle) added; active requirements remain Partial pending fresh integration evidence). Extension-owned confirmation created a second participant request and duplicate review UI, while direct Chat Editing commands resolve from widget focus and `showTextDocument()` cannot guarantee ordinary editor restoration. Later exact-host evidence traced the `ExtHostDocuments` failure to first-request location data whose document was not synchronized, not to the location-less confirmation follow-up. The pinned private `location2` document, selection, and whole range now define context, version, and edit URI even if focus changes. Codeflare emits a host-ingested empty-start/edit/done sequence and keeps the configuration-gated edited-file opener false; this setting does not gate the controller's different-URI side-group path. `InlineChatController` owns Keep/Close, settlement, disposal, and navigation. Former REQ-IDE-031 review-action behavior maps to REQ-IDE-033 AC3–AC7; the removed custom decision-registry behavior is intentionally absent. REQ-IDE-029 retains bounded progress and result details without claiming durable Inline prose. REQ-IDE-030 now permits up to three raw proposal attempts, accepts the first valid correlated proposal, categorizes invalid-only settlement, and still rejects a second valid proposal. No Code OSS patch, locality threshold, staged confirmation, direct-write replay, panel handoff, timeout, or additional Pi process is introduced.

- **Native Pi panel turns expose reasoning without repetitive progress floods** ([REQ-IDE-027](browser-ide.md#req-ide-027-native-pi-panel-reasoning-and-bounded-progress) added as Partial pending fresh integration evidence). Pi's provider-emitted reasoning deltas now use Code OSS's native thinking presentation while final answer text remains separate. Tool execution reports one bounded, argument-free status for each activity category instead of appending `Running read…` or `Running bash…` for every call. Inline Chat remains a host-owned edit transaction and does not render final-answer markdown.

- **Deployed Browser IDE evidence replaces three source-level assumptions with owned runtime boundaries** ([REQ-IDE-019](browser-ide.md#req-ide-019-codeflare-eligibility-in-editor-inline-chat), [REQ-IDE-020](browser-ide.md#req-ide-020-native-pi-editor-proposal-execution), [REQ-IDE-021](browser-ide.md#req-ide-021-account-free-browser-ide-chrome), and [REQ-IDE-024](browser-ide.md#req-ide-024-codeflare-browser-ide-welcome) amended; [REQ-IDE-025](browser-ide.md#req-ide-025-shared-ide-pi-surface-isolation), [REQ-IDE-026](browser-ide.md#req-ide-026-native-inline-chat-edit-validation), and [REQ-IDE-028](browser-ide.md#req-ide-028-native-inline-chat-dispatch-error-isolation) added; all remain Partial pending fresh deployed verification). Code OSS 1.132 excludes ordinary responses when no confirmation is pending and waits for host-owned edit transactions, so unrestricted Pi direct writes cannot truthfully stream there or be replayed as transactional edits. Integration deployment `31914050650` proved that Code OSS's `inlineChat2.continueInChat` action is termination-only and therefore a no-op during the active participant request. Fresh-session validation after deployment `31918973796` then proved the proposal command could still hang: it called nonexistent `ExtensionCommandContext.sendUserMessage`, Pi emitted a swallowed command `extension_error`, and the backend waited forever for settlement. The command now uses `ExtensionAPI.sendUserMessage`; both command-attributed failures and its asynchronous `<runtime>` `send_user_message` failures retire the active inline backend while unrelated extension errors remain isolated. The Pi package admits the pinned private location field and keeps one persistent IDE-owned Pi conversation across surfaces. Panel turns retain unrestricted tools. An editor turn saves that tool set, exposes only one terminating proposal tool, correlates its bounded edits, validates them against the captured active document version, emits native `textEdit` parts, and restores the prior panel tools at settlement. No third Pi process, Code OSS patch, direct-write replay, or panel handoff is introduced. Pi now disables unrelated built-in AI setup through managed settings and marks that setup hidden before refreshing its own account-free models instead of trying to seed browser-owned status visibility through server-side `User/State/storage.json`; Claude and unsupported inventories retain the same managed disablement. Finally, the welcome renderer drops validation of unused `webview.cspSource`, whose real built-in-extension value may contain multiple CSP sources and previously left the already-created editor without HTML. Nonce-bound inline CSP, the agreed welcome content, unrestricted panel tools, account independence, code-server/Code OSS source, Docker pin ownership, and Bump Shadow Pins remain unchanged.

## 2026-08-15

- **Vault browser scripts remain self-contained after the production Worker bundle** ([REQ-VAULT-018](vault.md#req-vault-018-vault-control-gating-and-on-demand-prewarm-trigger) AC6, [REQ-VAULT-020](vault.md#req-vault-020-vault-prewarm-focus-safety) AC2, [REQ-VAULT-022](vault.md#req-vault-022-vault-armed-state-open-flow-and-persistence) AC5, [REQ-VAULT-024](vault.md#req-vault-024-vault-bootstrap-hop-key-arming-and-service-worker-retention) AC1-AC2, and [REQ-VAULT-029](vault.md#req-vault-029-canonical-browser-state-cutover-and-future-worker-safety) AC3-AC5/AC7 amended; statuses remain Implemented). The production Wrangler bundle preserves function names by adding bundle-scoped `__name` calls; serializing transformed Worker functions into separate browser realms omitted that helper and stopped v3 immediately after stale-worker cleanup began. Bootstrap, cleanup, registration, focus, readiness, and controlled-reload logic now use authored browser-script source bytes with escaped dynamic arguments, and a Node behavioral test bundles the real injector module with esbuild `keepNames`, then executes the exact emitted page scripts. The v3 canonical scope, explicit stale-worker removal, untouched historical IndexedDB, SilverBullet 2.10 worker, and white → accent breathing → green → second-click lifecycle remain unchanged.

- **Vault v3 performs one clean worker cutover and restores strict user-owned preparation** ([REQ-VAULT-018](vault.md#req-vault-018-vault-control-gating-and-on-demand-prewarm-trigger), [REQ-VAULT-021](vault.md#req-vault-021-bucket-stable-vault-url-and-bucket-derived-key), [REQ-VAULT-022](vault.md#req-vault-022-vault-armed-state-open-flow-and-persistence), [REQ-VAULT-024](vault.md#req-vault-024-vault-bootstrap-hop-key-arming-and-service-worker-retention), and [REQ-VAULT-029](vault.md#req-vault-029-canonical-browser-state-cutover-and-future-worker-safety) amended; statuses remain Implemented). The earlier v2 compatibility design left old workers alive and tried to prove readiness through session metadata embedded in SilverBullet's precached shell. The v3 token creates one permanent fresh scope; on the user's first click the bootstrap unregisters every stale same-origin Vault worker, verifies none remains, and registers only the canonical native worker. Historical IndexedDB stores remain untouched orphan caches and R2 remains authoritative. The shell no longer carries session-specific readiness metadata, and the ready bridge reports canonical scope plus runtime, sync, index, required-file, and file-list proof. Every fresh dashboard load stays white until click one, timeout/error attempts retry only on another click, green means proven ready for the current dashboard lifetime, and click two opens immediately.

- **Superseded later the same day by the v3 clean-cutover entry above: canonical Vault readiness accepted the worker update transition** ([REQ-VAULT-029](vault.md#req-vault-029-canonical-browser-state-cutover-and-future-worker-safety) AC4 amended; status remains Implemented). The uploaded integration console showed repeated complete SilverBullet sync/index/runtime-ready cycles while readiness still failed, and the working baseline accepted a present exact-scope active worker without requiring its transient state to be `activated`. Behavioral coverage now accepts `activating` while rejecting missing or `redundant` workers. Exact scope and observed DB proofs remain mandatory, and the existing sync, index, content, stable-streak, key, and second-click gates remain unchanged.

- **Review-owned Goal pause no longer aborts the queued PR-boundary turn** ([REQ-AGENT-144](agents.md#req-agent-144-review-owned-goal-pause-command-compatibility) added as Implemented). The trusted review bridge delegated to pi-goal's ordinary pause command, which unconditionally aborted Pi's current turn. If the queued review follow-up had started before the deferred bridge call landed, the pause stopped that unrelated turn. The compatibility transform now gives the command an optional non-aborting path used only by trusted review control; Goal state, usage recording, continuation cancellation, persistence, and status updates remain native, while manual pause retains its default current-turn abort.

- **Latest `gh` and `lazygit` Go stdlib findings use reviewed CVE-level suppressions** ([REQ-SEC-011](security.md#req-sec-011-container-image-scanned-for-cves-before-deploy) AC2 and [REQ-OPS-002](operations.md#req-ops-002-docker-image-build-vulnerability-scan-and-registry-push) AC4 remain Implemented). Integration run `31889621501` built the latest available CLI releases with pre-fix Go runtimes. Eight fixable findings are temporarily accepted through `.trivyignore`, whose CVE identifiers apply across scanner targets and carry adjacent impact and removal rationale. Exact package/path/PURL identity output remains limited to separately validator-managed exceptions.

- **Superseded later the same day by the v3 clean-cutover entry above: Vault installation left legacy browser state orphaned while cutting over to one safe future scope** ([REQ-VAULT-029](vault.md#req-vault-029-canonical-browser-state-cutover-and-future-worker-safety) added as Implemented; [REQ-STOR-001](storage.md#req-stor-001-dedicated-per-user-r2-bucket) AC4 added and [REQ-VAULT-021](vault.md#req-vault-021-bucket-stable-vault-url-and-bucket-derived-key) AC7 amended; statuses remain Implemented). Production telemetry tied failed native-worker precache assets and an unrelated batch-status 500 to `KV PUT failed: 429 Too Many Requests`: every authenticated request rewrote the same non-authoritative bucket-owner KV key, while the 52-request SilverBullet precache also scheduled 52 writes to one session key. Bucket ownership now remains solely in its strongly consistent Durable Object, and only cache-versioned non-navigation precache GETs for the shell or `/.client/*` skip per-asset session activity. A versioned deterministic token creates one fresh canonical worker scope and, because SilverBullet hashes `baseURI` into both database names, a fresh encrypted `sb_data_*`/`sb_files_*` pair. Existing registrations and databases are neither enumerated nor deleted. Exact scope-bound readiness prevents those orphans from greening the control, while the visible white static → theme-accent prewarm → green ready → instant second-click lifecycle remains unchanged.

- **Personal compute usage is visible in every deployment mode without turning non-SaaS usage into billing** ([REQ-SUB-006](subscription.md#req-sub-006-real-time-usage-tracking-via-timekeeper-do) AC2 and [REQ-SUB-007](subscription.md#req-sub-007-quota-enforcement-at-session-start-402) AC5 amended; [REQ-SUB-022](subscription.md#req-sub-022-cross-mode-personal-usage-data) and [REQ-SUB-023](subscription.md#req-sub-023-deployment-mode-account-actions) added as Implemented; [REQ-SUB-018](subscription.md#req-sub-018-usage-dashboard-page), [REQ-AUTH-016](authentication.md#req-auth-016-header-user-dropdown), [REQ-ENTERPRISE-008](enterprise-mode.md#req-enterprise-008-enterprise-frontend-surface-suppression), and [REQ-ENTERPRISE-015](enterprise-mode.md#req-enterprise-015-enterprise-setup-user-administration-suppression) narrowed to their owned behaviors). Container metrics send monotonic Timekeeper totals in SaaS, onboarding/default, and enterprise deployments. Non-SaaS Timekeepers include durable monthly usage but never enforce subscription quotas or trial billing. Header and Dashboard menus expose Usage everywhere; enterprise retains a Usage-only menu while Subscription, Guided Setup, and Logout stay suppressed. Non-SaaS usage responses omit quota values, so the page shows today/month totals without a billing progress bar.

- **Guided Setup reflects the installed coding-agent inventory and provider-family subscription support** ([REQ-AUTH-015](authentication.md#req-auth-015-guided-onboarding-flow) AC5 added; status remains Implemented). The stale Gemini card is replaced by Antigravity, Pi joins the six installable agents, and every card identifies its supported subscription provider families. OpenCode and Pi show their multi-provider subscription families rather than implying one vendor.

- **Decision history and Security evidence maps become explicit and framework-enforced** ([REQ-AGENT-142](agents.md#req-agent-142-unambiguous-documentation-decision-history) and [REQ-AGENT-143](agents.md#req-agent-143-resolvable-documentation-evidence-references) added as Implemented). The ADR ledger explains Active, Superseded, Partially superseded, and Redirect anchor states; every fully superseded index row is struck through, while partially superseded decisions stay active and historical sections remain intact. Security source-map ADRs, REQ/CON identifiers, and requirement domains link directly instead of using bare identifiers or labels such as `Operations SDD`. `/sdd init`, `/sdd clean`, documentation review, templates, and the reusable shape checker enforce the same rules without a product-specific decision inventory.

## 2026-08-14

- **SDD documentation lanes share optimized initialization, cleanup, and review shapes** ([REQ-AGENT-139](agents.md#req-agent-139-optimized-documentation-lane-rendering-and-delivery) added and promoted to Implemented after live promotion/rollback validation; [REQ-AGENT-140](agents.md#req-agent-140-lossless-documentation-lane-cleanup-and-enforcement) added and promoted to Implemented after live full-scope cleanup validation). Greenfield and Import Mode select the same bundled Architecture, API, Configuration, Deployment, Security, Observability, Troubleshooting, and optional project-lane templates through one narrow renderer, while `/sdd clean` and documentation review validate those same positively recognized collections without a product-specific inventory or file-wide dominant shape. Existing discovery, triage, cleanup modes, and review ownership remain unchanged; legacy records stay readable, and ambiguous normalization defers instead of suppressing or deleting content.

- **Terminal scrollback capacities match the two runtime owners** ([REQ-MOB-004](mobile.md#req-mob-004-scroll-drop-detection-during-burst-output) constraint corrected; status remains Implemented). Frontend xterm retains 5,000 lines while the host headless renderer retains 1,000; source enforces both values, while current behavioral evidence covers the frontend 5,000-line value only, so no runtime behavior changed.

- **Exhausted container transport now converges zombie sessions to stopped** ([REQ-SESSION-022](session-lifecycle.md#req-session-022-transport-recovery-is-confirmed-and-bounded) AC3 and AC6–AC7, [REQ-SESSION-023](session-lifecycle.md#req-session-023-accelerated-recovery-preserves-usage-and-quota) AC3, [REQ-SESSION-024](session-lifecycle.md#req-session-024-transport-recovery-ownership-is-durable) reorganized as AC1–AC5, [REQ-SESSION-025](session-lifecycle.md#req-session-025-transport-recovery-failures-are-observable) and [REQ-SESSION-026](session-lifecycle.md#req-session-026-transport-recovery-scheduling-failures-reach-lifecycle-callers) added; requirements remain Implemented). After two coordinator reconstructions, another complete failure of both host probes is terminal evidence even when the Containers SDK leaves `container.running` stale at `true`. The metrics alarm writes an existing record to KV `stopped` (or accepts an already-absent record), records no usage, and requests `SIGTERM`; recovery ownership ends without another alarm only after the platform accepts that stop. Explicit terminal status/stop phases bypass probes, self-healing, and billing; retained pre-upgrade ownership migrates before probes when KV is stopped, or when KV is absent while the SDK still reports running; failed ownership reads, authoritative writes, or stop requests attempt to re-arm without billing, while retry-scheduling failures are logged and propagated. The initial cause of the Cloudflare transport interruption remains unproven and is not claimed by this change.

- **Concurrent-session admission is explicitly best effort** ([REQ-SESSION-007](session-lifecycle.md#req-session-007-running-session-count-limited-per-tier), [REQ-SUB-013](subscription.md#req-sub-013-concurrent-session-limits), [REQ-SEC-019](security.md#req-sec-019-per-endpoint-rate-limit-policy), and [REQ-ENTERPRISE-001](enterprise-mode.md#req-enterprise-001-enterprise_mode-forces-unlimited-tier-and-pro-mode) aligned; REQ-SESSION-007 returns to Implemented after the explicit product decision, and the other statuses remain Implemented). Under AD6, simultaneous starts may exceed the nominal limit; deployment `max_instances` remains the hard platform ceiling, Enterprise retains its current non-SaaS role resolver, [issue #880](https://github.com/nikolanovoselec/codeflare/issues/880) records the approved future role-independent limit of 3, its deployment configuration belongs to private [Deployment modes and environments](https://github.com/nikolanovoselec/codeflare-private/blob/main/docs/deployment/modes-and-environments.md), and no runtime behavior changed here.

- **Nine overlapping bump PRs land as one lock-coherent compatibility sweep and the official Cloudflare skills catch up** ([REQ-AGENT-075](agents.md#req-agent-075-cloudflare-platform-skills-bundled-into-the-advanced-seed) AC1–AC2 amended; [REQ-AGENT-138](agents.md#req-agent-138-bundled-turnstile-scripts-fail-closed) added; other requirements unchanged). Pi 0.84.1 adds auth preflight, a Qwen provider, blocked-tool termination, TUI fixes, and a configurable Harness factory without changing the RPC delta contract or the IDE-owned backend's APIs. MCP Adapter 2.21.0 adds opt-in modern discovery, Agent Plugin loading, permission-resolution support, and OAuth/panel controls while preserving legacy protocol defaults and the existing `mcp`/`mcpScript` proxy surface. Codex 0.147.0 removes `exec --full-auto`, which Codeflare does not invoke; Claude CLI and official VS Code 2.1.224, OpenCode 1.18.15, and zizmor 1.29.0 likewise require no owned launcher or skill migration. Impeccable 4.1.0 is taken from its workflow-owned transformed bundle. The Cloudflare skills comparison preserves Codeflare's deliberate slim mega-skill and concise runtime descriptions, refreshes canonical Turnstile, and replaces ambiguous `sandbox-sdk` guidance with separate stable, 1.0-preview, and migration skills from the reviewed upstream revision; generated manifests and the embedded seed follow those canonical sources.

- **Browser IDE opens on a Codeflare editor plane and resolves its native model before first use** ([REQ-IDE-021](browser-ide.md#req-ide-021-account-free-browser-ide-chrome) AC4 and [REQ-IDE-024](browser-ide.md#req-ide-024-codeflare-browser-ide-welcome) added with inventory-specific AC7; [REQ-IDE-023](browser-ide.md#req-ide-023-browser-ide-inventory-compatibility-preservation) verified Implemented). A separate Codeflare-owned built-in extension opens a theme-responsive welcome editor for Pi, Claude, and unsupported inventories; managed `workbench.startupEditor: none` suppresses code-server's separate default Welcome editor in a fresh browser, so Codeflare is the sole startup welcome. The owned editor establishes full VS Code as universal while it observes the same workspace and isolated ephemeral container used by the main session. Its active experience identifies native Pi, the official Claude Code panel, or editor-only mode; the matching primary action opens Codeflare Chat, Claude Code, or workspace Explorer. The package contributes no agent, model, participant, or external content, so Pi and Claude remain the only native agent selections and the unsupported fixed inventory stays empty. Native Pi now emits the provider-change event Code OSS requires to resolve full model descriptors at initial workbench readiness, preventing the selected label from degrading to the provider name after first use. Every inventory also disables the separate Chat title-bar sign-in affordance through managed User settings while retaining authentication APIs and independent agent credentials.

- **The weekly `pi-goal` lane accepts the reviewed 0.49.5 split-source layout and keeps official Claude Code under shadow-pin ownership** ([REQ-AGENT-129](agents.md#req-agent-129-goal-continuation-settings-policy) constraints amended; [REQ-OPS-020](operations.md#req-ops-020-shadow-pin-version-bump-automation) AC3 and AC7 remain unchanged). Run `31737476827`, job `94572530512`, correctly failed before branch push because the compatibility transform admitted only 0.46.0. The transform now supports both the currently locked release and cooldown-eligible 0.49.5: review control moves from the old combined goal module to the new lifecycle module, the continuation tool-policy anchor follows upstream’s extracted policy object, and the upstream automatic-turn default remains 25 while Codeflare’s minimum-interval field is added. Candidate transformation remains atomic, idempotent, and fail-closed for every other version or source drift, and the preseed contract admits only the current SHA-512 integrity or 0.49.5’s reviewed registry integrity. The existing `claude-vscode-extension` job remains the explicit owner of the official Open VSX metadata and SHA-256 pin; no official Anthropic package source or Bump Shadow Pins workflow is modified.

## 2026-08-13

- **Promotion review hardens design persistence and corrects verification boundaries** ([REQ-AGENT-134](agents.md#req-agent-134-advanced-design-skill-suite) AC2–AC7 and [REQ-AGENT-136](agents.md#req-agent-136-ui-ux-pro-max-persistence) AC3 amended; [REQ-IDE-019](browser-ide.md#req-ide-019-codeflare-eligibility-in-editor-inline-chat) AC2–AC7 amended and [REQ-IDE-023](browser-ide.md#req-ide-023-browser-ide-inventory-compatibility-preservation) added; requirements remain Partial). UI UX Pro Max now opens every owned destination directory without following symbolic links, rejects symlinked files, and atomically replaces regular destinations, preventing a repository-controlled design-system path from redirecting persistence outside the selected project. Generated-seed tests retain deterministic delivery, license, provenance, and path-adaptation contracts without treating router copy as execution proof. LLM routing, independent authorship, pinned-workbench startup behavior, and visible selector multiplicity are recorded as manual evidence boundaries rather than overstated automation. Independent Browser IDE compatibility surfaces now have separate acceptance criteria, and lifecycle guidance no longer claims a normally completed turn reaps the persistent Pi backend.

- **Pi 0.84 retires four stale scanner exceptions** ([REQ-SEC-011](security.md#req-sec-011-container-image-scanned-for-cves-before-deploy) and [REQ-OPS-002](operations.md#req-ops-002-docker-image-build-vulnerability-scan-and-registry-push) remain Implemented). Integration and enterprise integration deployment runs `31740630696` and `31740633639` proved that the upgraded shared and dedicated Pi trees no longer emit either `brace-expansion` 5.0.7 or `undici` 8.5.0. Their four exact reviewed tuples are removed rather than weakening the fail-closed missing-finding check; any recurrence is now unexpected and blocks publication. Behavioral coverage preserves acceptance of the four findings still observed in the complete image and rejects the retired Pi identities.

- **Twenty-three overlapping dependency PRs land as one compatibility-reviewed sweep** ([REQ-OPS-019](operations.md#req-ops-019-security-posture-scanning-workflows) AC7 added; other requirements unchanged). Twenty-two distinct upgrades cover the Pi, Claude, Codex, OpenCode, Graphify, uv and lazygit runtimes; Hono, TypeBox, Vite, Wrangler, Knip, Oxlint and ws; and the CodeQL, paths-filter and zizmor actions. Pi 0.84.0 removes cumulative RPC `message_update` snapshots, but the IDE-owned backend already consumes only `assistantMessageEvent.text_delta`; none of Codeflare's extensions uses the renamed model-transform, provider-refresh, OAuth, or harness APIs, so no skill or extension source changes. Claude 2.1.223's bundled `/review` alias remains shadowed by Codeflare's local review command under Claude's documented command precedence. Graphify 0.9.42 and the remaining agent patches preserve their owned command and skill contracts. The lazygit archive placeholder is replaced with the independently verified release digest, and uv retains its release digest. CodeQL's separately proposed init and analyze bumps could not land independently because mixed action releases reject each other's saved configuration, so init, autobuild, and analyze now move together under an automated coherence contract. paths-filter 4.0.3 closes its multiline-filename output advisory even though Codeflare consumes only booleans; zizmor-action 0.6.2 retains the separately pinned zizmor 1.28.0 binary and moves its exact workflow assertion with the action SHA. The incomplete Pi-only duplicate is superseded by the lockstep runtime-plus-prewarm update. Generated npm locks reapply the repository's security floors, and the generated multi-agent seed follows the reviewed canonical manifests.

- **Advanced sessions gain one master design router and three portable specialists** ([REQ-AGENT-134](agents.md#req-agent-134-advanced-design-skill-suite) and [REQ-AGENT-135](agents.md#req-agent-135-ui-ux-pro-max-query-and-generation), [REQ-AGENT-136](agents.md#req-agent-136-ui-ux-pro-max-persistence), and [REQ-AGENT-137](agents.md#req-agent-137-ui-ux-pro-max-data-validation) added; Partial pending review and CI). The canonical Claude preseed now carries a compact `design` entry point that composes UI UX Pro Max for searchable product-interface guidance, Canvas Design for static PNG/PDF artwork, an independently written Codeflare Frontend Design skill for distinctive frontend direction, and optional Impeccable critique where that full tool bundle is installed. The standard generator transforms the suite for Codex, Antigravity, OpenCode, and Pi; Copilot remains excluded because it has no skill-directory contract. UI UX Pro Max retains its MIT license, Canvas Design retains Apache-2.0 with provenance and modification notice, and no all-rights-reserved Anthropic Claude Code source is copied or transformed.

- **Native Codeflare Chat keeps one lazy IDE-owned Pi conversation across panel and editor turns** ([REQ-IDE-005](browser-ide.md#req-ide-005-selected-native-ide-agent), [REQ-IDE-006](browser-ide.md#req-ide-006-ide-conversation-context-and-credential-isolation), [REQ-IDE-008](browser-ide.md#req-ide-008-ide-agent-process-lifecycle), [REQ-IDE-019](browser-ide.md#req-ide-019-codeflare-eligibility-in-editor-inline-chat), [REQ-IDE-020](browser-ide.md#req-ide-020-native-pi-editor-proposal-execution), and [REQ-IDE-022](browser-ide.md#req-ide-022-native-pi-blocking-ui-protocol) amended; affected runtime requirements Partial pending review and CI). The first request lazily starts the fixed unrestricted local Pi RPC process, and normally completed turns retain it. Panel and editor requests execute FIFO and intentionally share its hidden in-memory transcript, while terminal Pi remains a separate process and conversation. Cold creation or replacement bootstraps from the requesting surface's bounded visible history; warm turns send only current request/editor context to avoid replaying history Pi already holds. Queued cancellation skips only that request. Active cancellation, startup cancellation after backend creation, protocol or process failure, unexpected exit, and deactivation boundedly retire the generation before replacement. No code-server, Code OSS, Docker pin, Bump Shadow Pins workflow, tool, root, interceptor, approval, or fail-closed protocol boundary changes.

- **The selected web keyboard layout joins bounded Browser IDE continuity** ([REQ-IDE-002](browser-ide.md#req-ide-002-session-isolated-ide-not-bucket-stable) amended). Code OSS's web keyboard-layout picker writes the user's selection to the `keyboard.layout` User setting, but Browser IDE continuity previously allowed only theme settings and canonical Explorer/open-file state, so every fresh session returned to the default layout. The bounded snapshot now admits only that additional string-valued setting and restores it before managed inventory settings are reapplied; the upstream layout status item and picker remain visible. Arbitrary User settings, profile state, authentication, chat, extension state, SecretStorage, and live editor databases remain excluded.

- **Deployed startup evidence corrects two Browser IDE lifecycle assumptions** ([REQ-IDE-019](browser-ide.md#req-ide-019-codeflare-eligibility-in-editor-inline-chat) and [REQ-IDE-021](browser-ide.md#req-ide-021-account-free-browser-ide-chrome) amended; Partial pending review and verification). `onStartupFinished` registered providers after editor Inline Chat's initial model snapshot, leaving Copilot setup until a panel request activated Codeflare. The Pi inventory now activates its owned extension immediately. Separately, the web workbench reads profile state from `User/State/storage.json`, not the previously seeded desktop path. Preparation now writes bounded atomic hidden-entry state to the web path for every inventory. The supported `workbench.activity.showAccounts=false` profile state removes the separate Accounts control while retaining authentication APIs. No code-server, Code OSS, Docker pin, or Bump Shadow Pins workflow change.

- **Inline Pi dialogs and account-free Browser IDE chrome complete the owned host adapter** ([REQ-IDE-020](browser-ide.md#req-ide-020-native-pi-editor-proposal-execution) amended and [REQ-IDE-021](browser-ide.md#req-ide-021-account-free-browser-ide-chrome) plus [REQ-IDE-022](browser-ide.md#req-ide-022-native-pi-blocking-ui-protocol) added). The deployed failure was an evidenced `select` request from Pi review enforcement, while the packaged ask-user-question extension also depends on Pi's documented `input` method. The existing manifest-backed `confirm` path remains unchanged; bounded select/input requests now use cancellable native VS Code dialogs, honor Pi's bounded request timeout, write a correlated value or cancellation response, and let the same RPC turn continue. Malformed, editor, and unknown blocking UI methods still fail closed. Separately, Code OSS's bottom-right **Sign In** control is `chat.statusBarEntry`, not the Accounts icon. Every fixed inventory now atomically seeds only that supported status-entry visibility key in ephemeral profile storage while preserving unrelated values and all authentication APIs. Configured preparation tests and packaged-inventory smoke cover that owned behavior; they do not claim to behaviorally exercise the upstream workbench. No code-server/Code OSS file or Bump Shadow Pins workflow changes.

- **Deployed evidence corrects the native Pi Inline Chat host contract** ([REQ-IDE-019](browser-ide.md#req-ide-019-codeflare-eligibility-in-editor-inline-chat) and dependent [REQ-IDE-020](browser-ide.md#req-ide-020-native-pi-editor-proposal-execution) amended). The first implementation registered its selectable compatibility model under Code OSS's reserved `copilot` vendor and relied on metadata-only tests. In the deployed host, the Models control consequently remained owned by Copilot entitlement and showed **Sign in to use Copilot** instead of Codeflare. Codeflare now retains a hidden, non-selectable panel fallback under `copilot` solely for the pinned extension host's absent-request-model lookup and contributes a second visible model under its distinct `codeflare` vendor for the picker. Both compatibility generation paths remain fail-closed, and participant inference remains on existing local Pi RPC. Code OSS also discovered the same seeded custom agents from both `~/.copilot/agents` and `~/.claude/agents`, deduplicating by URI rather than name, so every Agent entry appeared twice. The Pi inventory now disables only the Claude personal-agent source and retains the VS Code-compatible Copilot-format source. No agent files are removed, and Pi's own `~/.pi/agent/agents` discovery is unchanged.

## 2026-08-12

- **Native Pi joins the existing editor Inline Chat surface without becoming a language-model provider** ([REQ-IDE-019](browser-ide.md#req-ide-019-codeflare-eligibility-in-editor-inline-chat) and [REQ-IDE-020](browser-ide.md#req-ide-020-native-pi-editor-proposal-execution) added; Implemented after exact-head review and green CI). The same `codeflare.pi` participant is contributed to panel and editor locations, and one selectable Codeflare compatibility row satisfies the pinned host's editor model filter without authorization. Its then-used reserved vendor key and tool-calling flag were intended as host selectors only; deployed evidence recorded above disproved the vendor assumption. Provider generation remains fail-closed, while every panel or editor turn still runs through a fresh unrestricted local Pi RPC process. Pi may write beyond the selected range or workspace file and may run commands; those direct effects are deliberately not described as transactional Inline Chat edits or as Keep/Undo-safe. No code-server or Code OSS patch, Copilot extension, login, new approval boundary, tool restriction, or second runtime is introduced. Promotion is gated only on the repository PR-boundary review and required GitHub Actions CI, followed by a successful monitored integration deployment. There is no local, browser-automation, manual-session, or pass@3 gate.

- **The dependency sweep lands with the code-server checksum supplied and one vulnerability suppression retired** ([REQ-OPS-027](operations.md#req-ops-027-code-server-coupled-pin-automation) and [REQ-IDE-010](browser-ide.md#req-ide-010-pinned-ide-inventory-compatibility) unchanged). Twelve open bump branches overlapped on the same generated files and none carried CI, so they land as one reviewed sweep rather than twelve serial merges: code-server 4.132.0 / Code OSS 1.132.0, Claude Code 2.1.222, Copilot 1.0.78, opencode-ai 1.18.13, Graphify 0.9.41, and Pi's rpiv, pi-mcp-adapter and pi-web-access extensions. One of those is not the patch bump its version suggests: pi-mcp-adapter 2.20.1 takes its MCP transport from the `@modelcontextprotocol/client` and `@modelcontextprotocol/core` 2.0.0 packages and pulls `jose`, `pkce-challenge`, `eventsource` and `cross-spawn` into the session container's tree, while `@modelcontextprotocol/sdk` remains behind a different dependent — new supply-chain surface that a version-only record would have hidden from the next audit. The bump job deliberately invalidates the code-server archive checksum with a placeholder so an operator supplies it after review, which is the contract working as designed — but a placeholder that reaches a merge candidate unfilled is a broken image build, because the Dockerfile pipes it straight into `sha256sum -c -`, and it means the one control standing between the build and a substituted release artifact shipped unreviewed. The digest is now pinned and was confirmed twice: from the release asset record, and by streaming the artifact the build actually fetches through `sha256sum`. Retiring the bounded js-yaml exception is that same verification paying off again — the suppression's own stated removal condition was "a pinned code-server release bundles js-yaml 4.3.0 or later", and 4.132.0 bundles exactly 4.3.0, so it is removed rather than re-dated, which is also what the comment justifying it had drifted on (it still described 4.131.0 vendoring 4.2.0). The separately integrity-pinned overlay to 4.3.1 stays, still inside code-server's declared `^4.1.0` range.

- **code-server 4.132.0 patches one of its two `ip-address` copies, so the reviewed exception bound to the other one is retired** ([REQ-SEC-011](security.md#req-sec-011-container-image-scanned-for-cves-before-deploy) and [REQ-OPS-002](operations.md#req-ops-002-docker-image-build-vulnerability-scan-and-registry-push) unchanged). The integration deploy failed its Trivy exception gate with `missing reviewed finding: CVE-2026-69192 ip-address 10.2.0`, which is the gate working rather than breaking: reviewed exceptions are bound to a scanner identity and fail closed when the finding they describe stops appearing, because an exception nobody can see is an exception nobody re-reviews. CVE-2026-69192 is a HIGH SSRF via leading-zero octet decoding, unpatched at or below 10.3.0. 4.131.0 carried 10.2.0 in both of code-server's trees; 4.132.0 lifts the server tree to 10.4.0 and leaves the bundled VS Code tree at 10.2.0. So exactly one of the two exceptions is now obsolete and is removed, the VS Code one still stands on its own evidence, and a recurrence in the server tree fails closed as unexpected. Worth recording that the bump quietly fixed a HIGH finding and that no gate before the image scan could have seen it — the review lanes read a version literal, and only building and scanning the image reveals which vendored copies moved. The security lane's dated run evidence is left as written and the retirement narrated after it, matching how the CVE-2026-56852 occurrences were recorded when they retired.

- **Goal moves to 0.46.0 after the review its version gate exists to force** ([REQ-AGENT-129](agents.md#req-agent-129-goal-continuation-settings-policy) Constraints updated, no AC change; [REQ-AGENT-111](agents.md#req-agent-111-native-goal-workflow-in-pi-sessions) unchanged). Dependabot proposed `@narumitw/pi-goal` 0.46.0 and it was first held, because the image build would have failed on it: the exact-version review-control transform refuses every version but the one it was reviewed against, and the build runs it in an `&&` chain immediately after `npm ci`, so that refusal is the entire build. Worth recording how the bump got that far — the weekly shadow-pin job runs this transform against a candidate release before it opens a PR, but Dependabot opens bump PRs without that preflight, so a version the transform could not yet patch arrived looking like an ordinary version bump and was carried into the sweep. Holding it was the safe first move, but holding is not the answer the gate asks for: the gate wants the review done, not the version avoided. So the preflight was run directly — all four patch functions (`commands`, `goal`, `runtime`, `settings`) applied cleanly to 0.46.0's sources, every anchor still matching exactly once, which is the same check the shadow-pin job performs. The only thing that was ever incompatible was the version literal itself. The contract moves to 0.46.0 with the anchors untouched, because none of them needed to move. Matching anchors are the weaker claim, though — an anchor can still match textually while the code around it has moved, which would patch silently and wrongly. So the two releases' four patched files were diffed directly: `commands.ts`, `goal.ts`, `runtime.ts` and `settings.ts` are byte-identical between 0.43.0 and 0.46.0, zero changed lines, so there is no surrounding-semantics drift to reason about. That evidence is recorded here rather than turned into a build-time upstream diff, which would add a network-dependent step to the image build to re-derive a fact fixed for this pin, and which `replaceOnce`'s fail-closed anchor count plus the shadow-pin preflight already cover.

- **Pi gains an adversarial post-execution evaluator as a skill, not as machinery** ([REQ-AGENT-133](agents.md#req-agent-133-native-evaluation-workflow-in-pi-sessions) added). `pi-evaluate` 0.1.5 joins the required package set at an exact pin and SHA-512 lock. What is worth recording is how little of it there is: the extension's entire body registers the packaged skill directory on `resources_discover`, so the package contributes no tool, no command, and nothing that runs until a user types `/skill:evaluate`. That is why it needs none of the review machinery Goal carries -- there is no source to patch and no behaviour to neutralize, only a skill the user invokes, which reads the stated contract and the produced outputs and reports gaps it will not fix. The one place it does touch the image is the JITI warm block, because a package whose declared entrypoint is never warmed cold-transpiles on first use in every session; `extensions/evaluate.ts` is now declared, warmed, and re-verified next to Goal and Usage. That wiring had no test of its own: the warm helpers are package-agnostic and would faithfully warm a list that had silently lost an entry, so nothing failed if the Dockerfile stopped naming an entrypoint. The build wiring is now asserted for all three managed entrypoints, which is what makes the equivalent Usage criteria falsifiable too.

- **Pi owns its memory-capture agent instead of inheriting Claude's through string replacement** ([REQ-MEM-016](memory.md#req-mem-016-vault-extraction-runs-on-bounded-one-pass-inputs) and [REQ-MEM-018](memory.md#req-mem-018-background-extraction-agents-are-bounded-and-visible); [AD103](../../documentation/decisions/README.md#ad103-pi-extraction-agents-use-bounded-medium-reasoning-and-one-pass-inputs) budget restored, behavioral fix). Pi's capture agent was generated by six exact-string replacements over the Claude agent's prose, which is a transform that fails open: when the prose moves, the replacement finds nothing, silently keeps the Claude sentence, and the other runtime ships the wrong contract with nothing to notice. Two of the six had already gone stale, so Pi was shipping Claude's `.vars` retry-carrier wording in place of its own immutable-execution-snapshot contract. Then an ordinary edit to the Claude agent this same day moved four more anchors at once and handed Pi Claude's six-turn budget over its own four, plus Claude's inline `CAPTURE_REQUEST` transport — a delivery Pi does not use — while Pi's own retained paragraph still told it to read a snapshot, leaving the document self-contradictory. Nothing caught it: the review lanes read the Claude source and treat the generated seed as derived output, and no row pinned the two budgets apart. Pi now owns `preseed/agents/pi/agents/memory-capture.md` outright, the pattern already used for its five other agents, and the pi-native branch emits it verbatim in place of any transform; the dead replacement block is removed rather than left as anchors for the next edit to miss. A row pins both halves — Pi four, Claude six, and no Claude-transport identifier present in Pi's document — so re-linking them fails in CI instead of in a runtime nobody is looking at. `vault-extract` runs the same mechanism and was checked: all five of its anchors still match, so it is correct today and left alone.

- **One refused lane spawn no longer waves its siblings through the triage gate** ([REQ-AGENT-104](agents.md#req-agent-104-review-acknowledgement-requires-a-published-verdict) AC7 unchanged; behavioral fix). Three lane spawns issued as one batch got two different answers: the first was refused and the other two launched. The gate reads each lane's newest spawn record and treats one with no completion as a lane still in flight — the escape that stops it blocking a round genuinely running. But a refused call still leaves its `tool_use` record behind, and that record never completes, so refusing the first call of a batch wrote the very evidence that classified the lane as in flight and let the siblings through a second later. Replaying the recorded transcript isolated the sequence exactly: the refusal was decided before the batch's spawn records were flushed, and both later calls saw the refused record. It is not only a batch effect — that record stays newest for the rest of the round, so one refusal reopens the gate until another spawn lands. Each lane now walks back to its newest spawn that actually reported a completion, and only a lane that has never once completed counts as in flight, so a refused or user-rejected record is skipped rather than trusted while a first round still running keeps its escape. A row pins the batch, which is the oracle: the sibling calls see the refused record and must still get the answer the first call got. Verified against the recorded batch (three refusals where there had been one refusal and two launches) and against the six gate states already pinned, which are unchanged either way.

- **The capture request travels in the prompt instead of as a path to itself** ([REQ-MEM-020](memory.md#req-mem-020-capture-requests-are-re-delivered-under-a-bound) and [REQ-MEM-021](memory.md#req-mem-021-capture-publication-requires-its-artifact) unchanged; behavioral fix). Raising the turn budget did not make a large window capturable, so the run was traced: the launcher built a self-contained 83KB request and then handed the model its *path*, which put the retrieval on the tool-result channel. That channel cannot carry a transcript — the harness persisted the output and returned a 2KB preview — so the agent spent every turn paging its own payload back in (`cat`, `Read`, `python3 -c print()`, then two offset reads of the persisted file) and hit the ceiling as it began writing the note. No budget fixes that; each extra turn buys one more page. The request is now rendered inline into the prompt, metadata and delimited transcript together, so the conversation is in context before the first turn and the first Bash call is the write. This is what Pi's contract has always required ("VARS_FILE contains the transcript inline; there is no INPUT_FILE or separate transcript file") and what this script's own comments already claimed; Pi's larger 200,000-char budget shows size was never the discriminator. The capture contract drops the evidence-read step it could not perform, and the test row asserting the model receives a path — which pinned the defect in place — now asserts it receives the conversation. Verified live against the exact window that had failed eight consecutive times: it published on the first attempt, the counter advanced, and the graph chunk merged. Inlining also makes the frame forgeable in a way a JSON string value was not — a captured line reading `--- END TRANSCRIPT ---` followed by instructions would close the launcher's own frame, and the capture holds `Write` and `Bash` under bypassed permissions — so the delimiters carry a per-run marker drawn from `/dev/urandom`, and the contract states that everything inside them is conversation data rather than instruction and that output paths come from `capture_file` alone. The `Read` tool is withdrawn along with the retrieval it existed for, verified by a live run that writes and publishes without it. The shipped `memory-capture` agent definition is brought to the same contract — six turns, the inline request, and no grant of the `Read`, `Grep` and `Glob` tools whose retrieval and filesystem search the contract no longer has a use for — because it regenerates into the seed alongside the runner and otherwise ships as a standing instruction to fetch the path this change exists to remove. The fail-closed refusal is the security-load-bearing half of the frame, so it gains a row of its own: both draws are broken, the run must exit without starting the model, and it is paired against an identical fixture that draws normally, because the exit status proves nothing on its own — the healthy run exits 3 too, from the publisher. Review then caught that the guard was measuring the wrong quantity: `capframe` is eight constant characters of the fallback's own template, so an `mktemp` that echoes the template unexpanded returns twenty entirely predictable characters and passes a length test meant to establish entropy. Replayed against the pre-fix script it started the model on `--- BEGIN TRANSCRIPT capframeXXXXXXXXXXXX ---`, which is the refusal failing open in the one case it exists for. The fallback now strips its constant prefix and rejects a remainder still carrying the template's X's, so the length test measures what was actually drawn, and a second row pins the harder case where `mktemp` succeeds rather than fails.

- **The capture turn budget goes from four to six** ([REQ-MEM-020](memory.md#req-mem-020-capture-requests-are-re-delivered-under-a-bound) Constraints updated, no AC change; [AD124](../../documentation/decisions/README.md#ad124-bounded-re-delivery-replaces-the-memory-capture-hard-block) amended). Four turns was Pi's budget, adopted when the two capture shapes were made the same, and it was not enough for a large window: the journal added last round recorded `error_max_turns` on attempt after attempt against one 1,554-line window. That exposes a gap the bound itself cannot close — re-delivery answers a *transient* failure, but a turn ceiling is *deterministic*, so every retry reproduces it exactly and the six deliveries burn on a window that was never going to land. The default and its non-numeric fallback are now six, `CODEFLARE_MEMORY_MAX_TURNS` still overrides, and the row pinning `--max-turns` moves with it. Pi's four-turn extraction budget ([AD103](../../documentation/decisions/README.md#ad103-pi-extraction-agents-use-bounded-medium-reasoning-and-one-pass-inputs), REQ-MEM-016) is a separate lever and is unchanged. Worth recording that the diagnosis was only available because the per-attempt journal and the user-visible attempt counter shipped first; the same failure had been invisible for the five attempts before them.

- **Background rounds and captures announce themselves to the user** ([REQ-AGENT-104](agents.md#req-agent-104-review-acknowledgement-requires-a-published-verdict) and [REQ-MEM-020](memory.md#req-mem-020-capture-requests-are-re-delivered-under-a-bound) directives; presentation, no AC change). Every part of a round now runs as a background process — three headless lanes, a detached CI monitor, a detached capture — and each is triggered by a hook whose entire output goes to the model. The user was left watching an idle session with no way to tell a round from a stall, or a capture retrying for the fifth time from one that started cleanly. `systemMessage` is the one hook field the client renders straight to the terminal without routing it to the model, so the two synchronous hooks now carry one. Both are generated rather than fixed: the push hook names the PR, the head, the lane count, the lanes and the range from the same values the directive was built from, so a doc-only push announces one lane instead of three, and the consent path stays silent because it is about to raise an `AskUserQuestion` that is louder than any notice. The memory hook names the live attempt number, which is what makes a run of retries visible, and says so when a request is abandoned. Six rows pin the counts, the singular/plural agreement, the acknowledged base, the consent-path silence, the attempt numbers and the give-up notice; a fixed sentence would satisfy a substring match while describing a round that is not the one running. The Stop gate emits none of this and cannot: its `asyncRewake` path never parses stdout, which was tested against the pinned CLI along with a stdout `rewakeSummary`, honoured only for hooks a plugin registered. Its one static summary line stands, and the dynamic detail rides the directive.

- **A live review round survives a transient gh failure instead of dying silently** ([REQ-AGENT-104](agents.md#req-agent-104-review-acknowledgement-requires-a-published-verdict), unchanged ACs, behavioral fix). Every Stop and PreToolUse evaluation resolved the PR through one live `gh pr view`, and its failure was read as "no PR here" — the same silent exit an unrelated directory gets. Observed on PR #827 round 22: three lanes and a CI monitor were leaning on the same API when the round's own Stop event flaked, so the lanes completed into a session that was never told to triage them; the delivery barrier froze mid-handoff and nothing re-fired. Replaying the same transcript against a healthy API took the intended demand path, isolating the flake as the only divergence. The hook now caches the last successful resolution per branch and falls back to it only while that PR's plan file shows a round in flight; an idle branch keeps the old fail-safe silence, a superseded cache self-neutralizes on the head-equality gate, and six rows pin drive-through, cold-cache silence, superseded-head silence, no-round silence, the authoritative no-PR answer staying silent, and a generic exit-1 API error still bridging. The fallback engages only for failures gh does not answer authoritatively — the "no PR" answer keeps the old silence, since bridging it would resurrect a deleted PR from a stale OPEN cache — and gh's own exit 1 does not carry that distinction: gh answers "no PR" and generic API errors with the same code, so `gh_pr_state` now reads gh's stderr and remaps a non-not-found exit 1 to a distinct transient code, keeping its callers on a purely numeric contract. That read is defended at its own edges: a host where `mktemp` fails cannot verify the phrase and reports the transient class rather than a false authoritative answer, and the phrase match tolerates gh's known variants. Capture-file cleanup is deliberately normal-path only — a `RETURN` trap was tried and reverted the next round when review verified empirically that it cannot fire on a signal, and the wider `EXIT`/`INT`/`TERM` net would hand a sourced library its caller's process-global signal handlers; one tiny strandable file in a container-lifetime `TMPDIR` is the accepted cost, and a leak-guard row pins the normal-path cleanup so an early return inserted above the `rm` fails loudly; the same row pins the per-mode exit contract, echoing the function's status so found stays 0 and the authoritative not-found stays 1 rather than drifting to the transient 3. The cached answer is stored compacted, so a gh that ever pretty-prints degrades to a dead cache rather than a corrupt one.

- **The boundary classifier reads fd-prefixed herestrings as shell code** ([REQ-AGENT-104](agents.md#req-agent-104-review-acknowledgement-requires-a-published-verdict) AC7 unchanged; behavioral fix). `bash <<<"git push"` was closed last round, but the operator match was `startsWith('<<<')`, so the equivalent `bash 0<<<"git push"` — an fd number glued to the same operator feeding the same code — classified as no git activity and the delivery gate failed open for it. The match is now `/^\d*<<</` with the inline remainder handled identically, two refusal rows pin the glued and spaced fd forms, and `.review-queue.md`'s known-limit entry records this third form as closed while its two declined forms (heredoc-fed shell, piped shell) stand unchanged.

- **Stop directives are delivered as feedback, not as a blocking error** (presentation of [REQ-AGENT-104](agents.md#req-agent-104-review-acknowledgement-requires-a-published-verdict) directives; no AC change). The label was never unreachable, only unreached. Either a JSON `{"decision":"block"}` or a bare exit 2 is recorded as a blocking error, and the client answers a blocking error by pushing an immediate "Stop hook error occurred" notification — that notification was the word the user kept seeing, not the message body, which the Stop formatter already labels "Stop hook feedback". A hook entry carrying `asyncRewake` is backgrounded instead, and the runner returns before an exit code exists, so no blocking error is recorded and the notification has nothing to fire on; the later exit 2 wakes the model with the entry's `rewakeMessage` followed by the hook's stderr, under the summary the entry names. Verified end to end against the pinned CLI before the change: a woken turn received an injected directive and acted on it. `emit_block`/`emit_block_uncounted` therefore print to stderr and exit 2, `entrypoint.sh` registers the Stop hook with `asyncRewake` plus a `rewakeSummary` for the terminal line and a `rewakeMessage` for the reminder prefix, and the self-identifying tag and `systemMessage` that existed only to apologise for the old label are gone. That registration is pinned by a test, because dropping the key silently restores the defect. Making stderr load-bearing also made every state write's failure message load-bearing: `cmd > "$FILE" 2>/dev/null` leaves the shell's own redirect error on stderr, because the shell reports it before the command's redirections apply, and that line then prefixes the text the model is woken with. All fifteen such writes now order the stderr redirect first, which suppresses it; the acknowledgement-failure directive is the row that caught it. One row pins the channel — exit 2, directive on stderr, nothing on stdout — because the channel is the fix. The harness adapts a directive into the envelope the content rows read, recognising it by shape so a fail-closed guard exit on the same channel is never mistaken for one; every assertion on a synthesised `decision` key is gone, since a row asserting that key was asserting a string the harness itself wrote, and each converted row now claims the real exit status and that the directive names its PR. The earlier presentation work stands unchanged beneath it: the full FIX contract is delivered once per PR and later rounds get the one-line form.
- **A shell string is parsed as code, so `bash -c "git push"` no longer walks the triage gate** ([REQ-AGENT-104](agents.md#req-agent-104-review-acknowledgement-requires-a-published-verdict) AC7 unchanged; remains Implemented). The wrapper list handled every form that passes a delivery as a *command* — `sudo`, `env`, `nice`, `timeout`, `xargs`, a path-invoked binary. A shell does something different: it takes the delivery as a *string* and runs it as code. The parser saw `bash`, found it neither a tool nor a known wrapper, set command position false, and dropped every remaining word — including the one holding the push. `bash -c`, `sh -c`, `zsh -c`, combined forms like `-lc`, and `eval` all returned "no git ran here", verified before the fix and again after. The window this gate exists to hold was open for any of them, and this was a regression the gate's own rewrite introduced: before it, all Bash was refused outright while a round awaited its table, so nothing needed parsing to be caught. Shells now descend into their `-c` argument, and `eval` joins its remaining words so both `eval "git push"` and `eval git push` resolve. A bare word under a shell with no `-c` is a script file, whose contents are not in the command, so it stays unread rather than guessed at — which is also what keeps `bash run-review-lane.sh` classified exactly as before. A herestring is code by the same reasoning and is read the same way, glued form (`bash <<<"git push"`) included, since `<` does not break a word here. Two forms stay unread on purpose and are recorded as a known limit rather than patched: a heredoc fed to a shell, because the same body is data when written to a file and code only when fed to a shell and `stripHeredocs` discards the association that tells them apart; and a shell fed over a pipe, whose code is not among its own words and which would need a "could not read this" return value in a contract the Stop path shares, where a false positive means reporting a delivery that never happened.

- **A corrupt capture carrier refuses publication instead of advancing the counter** ([REQ-MEM-021](memory.md#req-mem-021-capture-publication-requires-its-artifact) AC1/AC2 unchanged; remains Implemented). Reading the capture path with `jq -r … || true` collapsed two different answers into one empty string: "this request predates the field", which legitimately keeps the older unverified behaviour, and "jq failed or the carrier is corrupt", which must not. Empty then skipped the existence gate entirely, so a corrupt carrier published, advanced the counter and marked the window captured with no capture file — the precise outcome the artifact gate exists to prevent, defeated by the way its own input was read. Branching on jq's exit status separates the two: absent field keeps the old path, a real failure refuses. Separately, per-session payload directories under `/tmp` were only ever reclaimed by the same session's next launch, so a container running many sessions accumulated them, now with an embedded transcript copy inside each; stale siblings are pruned at launch.

- **Memory capture runs as a headless subprocess with its payload already built** ([REQ-MEM-020](memory.md#req-mem-020-capture-requests-are-re-delivered-under-a-bound) AC5 added, AC3/AC4 reworded from delivery to launch; [REQ-MEM-021](memory.md#req-mem-021-capture-publication-requires-its-artifact) AC1/AC2 split out unchanged; both remain Implemented; [AD115](../../documentation/decisions/README.md#ad115-review-lanes-run-as-headless-claude--p-subprocesses) transport extended). The capture was spawned as an in-session `Agent`, so it inherited the whole parent conversation on every run -- a context the extraction has no use for, paid for in full each time. Pi never pays it: the runtime assembles the payload and the capture only summarises what it was handed. The arming hook no longer asks the session to spawn anything. It launches `run-memory-capture.sh` detached, which prefilters and chunks the transcript slice into the work directory the capture prompt already reads from, then runs `claude -p` with `--max-turns 4`, `--setting-sources ""` and `--strict-mcp-config`, bounded by `timeout -k 30`. The session contributes nothing to a capture and waits on nothing. The prompt's step 2 stops rebuilding a payload that is already on disk, which was a turn out of four. A `flock -n` on the carrier stops the hook relaunching a capture underneath itself, and the hook probes that same lock before spending an attempt: counting prompts rather than launches measured how much the user typed, so six prompts inside one 900s capture would latch a request that was working and the re-arm would then delete the carrier out from under it. Pi draws the same line, returning `none` from `extractionDue` while the state is `running`. The capture runs `--model sonnet --effort medium`, overridable, since it is the most frequent agent in the system and the job is fidelity rather than reasoning (AD58). Success is still read off the artifact: the exit status is a diagnostic, and `publish-memory-capture.sh` still refuses to advance the counter when the named capture file is absent. Each attempt now journals its capture exit, the publisher's verdict, and on failure the stderr tail plus the result envelope's failure subtype (a byte count when unparseable) to `<carrier>.attempts.log`, because a detached launch discards the runner's own stderr and a live window that took five attempts left nothing that said why; the journal outlives carrier removal, the six-attempt latch bounds it, and a behavioral row pins both recorded lines, the journaled subtype, and that the model's response text stays out of it.

- **A Stop turn the hook could not actually inspect now refuses instead of passing** ([REQ-AGENT-104](agents.md#req-agent-104-review-acknowledgement-requires-a-published-verdict) AC7 unchanged; remains Implemented). The fail-safe added with the shared classifier asked one compound question -- classifier unreadable *and* the transcript mentions git -- and got two different failures wrong with it. `grep` reports "no match" and "could not look" as 1 and 2, and a bare `&&` reads both as nothing-to-see, so an unreadable transcript passed unrefused; nesting the transcript test inside the classifier condition then left the same transcript passing whenever the classifier *was* present, which is the commoner arrangement by far. Both were the exact conflation the guard existed to end, and in both cases a comment above the code stated the contract the code did not keep. Readability is now checked on its own and first, which is what makes the word test underneath trustworthy at all: it only ever runs against a file that was read. The `SCRIPT_DIR` fallback introduced alongside it is absolute, because a bare relative path satisfies `[ -r ]` from the working directory and then throws from `require` as a package specifier, slipping past the guard rather than tripping it.

- **The FIX directive stops repeating itself on every branch that has a slash in its name** ([REQ-AGENT-104](agents.md#req-agent-104-review-acknowledgement-requires-a-published-verdict) AC4 unchanged; remains Implemented). The FIX phase was given its own head-keyed counter so the full contract emits once and later turns cost one line. The counter never persisted. Its path was built from `$CURRENT`, which is the branch name, not the PR number every sibling counter uses -- so on `fix/memory-capture-bounded-redelivery` the write targeted a directory that does not exist, failed, and was swallowed by the `|| true` that keeps an unwritable counter from wedging the round. Every branch this hook guards is a PR branch and PR branches are where slashes live, so the short form was unreachable in practice and the whole directive re-emitted turn after turn; it was reported from the receiving end as the same wall of text arriving back to back. The label carried the same mistake and read `PR #fix/memory-capture-bounded-redelivery` where it meant `PR #827`, at all five sites. Both now use `$PR_NUMBER`, which is validated as all-digits before either is reachable. The regression fixture checks out a branch with a slash, which no fixture in the suite did.

- **The FIX directive drops its "do not merge" prohibition, and the assertion requiring it is retired rather than rewritten** ([REQ-AGENT-104](agents.md#req-agent-104-review-acknowledgement-requires-a-published-verdict) AC4 unchanged; remains Implemented). The directive forbade merging, which nothing in the FIX phase was about to do: it applies accepted rows, commits, and pushes. A prohibition against an action nobody is taking is text every turn pays for and no turn uses, so it was removed on instruction. The regression assertion that required the phrase went with it and was deliberately not replaced by an equivalent. Matching directive prose is the text-matching this project rules out; the directive's wording is not a contract, and a test that pins it fails on any rewording while proving nothing about behaviour. Recorded here because the assertion's absence is otherwise indistinguishable from a guarantee quietly dropped -- which is precisely how a code review read it.

- **A blocked triage gate now reads as a decision rather than a tool error** ([REQ-AGENT-104](agents.md#req-agent-104-review-acknowledgement-requires-a-published-verdict) AC7 unchanged; remains Implemented). The PreToolUse gate refused with `exit 2` and a stderr line, which the harness renders as `PreToolUse:Edit hook error` -- a gate working exactly as designed, presented as a broken tool, and read as one. It now denies through `permissionDecision`, the shape `block-attributed-commits.sh` already uses in this same plugin, falling back to `exit 2` when that cannot be rendered because a deny nobody renders is `exit 0` and `exit 0` is the gate failing open. The full table spec is also stated once per round instead of on every refused call, reusing the strike counter already keyed to that round: two edits attempted before publishing produced two identical walls of text, which is what made the instruction read as noise.

## 2026-08-11

- **The triage gate and the transcript scan now share one shell-aware delivery classifier** ([REQ-AGENT-104](agents.md#req-agent-104-review-acknowledgement-requires-a-published-verdict) AC7 unchanged; remains Implemented). The gate answered "is this command a delivery" with a regex, on the reasoning that the parser already in this hook "lives inside `transcript_scan`'s node program and is not callable per command" -- recorded in the entry below and wrong, since the program is a heredoc and the parser was simply never lifted out of it. Three revisions followed, each closing the forms one review report named and leaving the class open: tool flags, then environment assignments and wrappers and absolute paths, then shell keywords. `sudo -u me git push`, `{ git push; }` and `if false; then :; else git push; fi` still walked through the third. Shell is not a regular language and the fourth patch would not have been the last, so the parser moved to `lib/boundary-classifier.cjs` and both callers ask it instead: the Stop path for delivery boundaries, the gate for those plus `git commit`, which mints a head the round never covered. Wrapper option values (`sudo -u me`), path-invoked tools (`/usr/bin/git`) and `timeout`'s bare duration are handled there once rather than twice, and the gate's documented over-refusal of a quoted separator is gone because the parser tracks quoting. A classifier that cannot run refuses, matching the unreadable-payload rule beside it.

- **The fix push is gated on CI passing, and the pre-triage window stops blocking investigation** ([REQ-AGENT-104](agents.md#req-agent-104-review-acknowledgement-requires-a-published-verdict) AC4 unchanged; [AD123](../../documentation/decisions/README.md#ad123-the-claude-fix-directive-owns-delivery-pi-leaves-it-to-standing-rules) amended). The FIX directive said to read the CI log "until that line lands and push immediately afterwards", which reads identically for a pass and a failure, so a red head would be delivered untouched and its failure carried forward. Only the overlap between the failing test and a review finding kept that from happening on this PR. A failing `CI_RESULT` is now a finding fixed in the same commit; an absent one still only delays, which keeps AD123's terminal escape intact. The directive is a third shorter and the three surfaces that restated it agree again, including `git-review-pipeline/SKILL.md`, which had kept the old wording through one round because a mismatch report was not acted on. Separately, the PreToolUse triage gate allowed only `Read`/`TaskOutput` once lanes completed and refused everything else until the table was published. Judging a finding is what that window is for, and validity often turns on an empirical claim that only execution can settle, so the gate forced a choice between publishing an unverified table and defying the gate; it also refused unrelated read-only work. It now denies what would actually spoil the round -- a head committed, pushed, or a lane relaunched -- and lets inspection through, the same correction the capture hard block needed. The command test is a deliberately crude substring match rather than the shell-aware boundary classifier, which lives inside `transcript_scan`'s node program and is not callable per command; over-refusing a command that merely mentions a delivery verb is the behaviour the window already had.

- **Memory capture drops its hard block for bounded re-delivery, and stops marking a window captured before anything was captured** ([REQ-MEM-020](memory.md#req-mem-020-capture-requests-are-re-delivered-under-a-bound) and [REQ-MEM-021](memory.md#req-mem-021-capture-publication-requires-its-artifact) added as Implemented; REQ-MEM-012 deleted; [AD124](../../documentation/decisions/README.md#ad124-bounded-re-delivery-replaces-the-memory-capture-hard-block) added). REQ-MEM-012 closed a real gap -- an advisory directive nobody acts on leaves the carrier undrained and the threshold only re-fires on a crossing -- by blocking every tool call except the capture spawn. That made one hook the arbiter of whether any work could proceed, and the review gate refuses exactly that spawn during the post-lane, pre-triage window: reading the lane report is a tool call, publishing the triage table is what lifts the review gate, and the block forbids the read. Observed live this session. It did not wedge only because both are PreToolUse, the block runs first and marks its 600-second sentinel before the review gate refuses, so the window opened for a spawn that never happened; reorder the hooks or make the sentinel conditional on success and there is no bypass, as the block itself states. Pi meets the same requirement with a durable request that `extractionDue` re-sends at every settle, latching after six deliveries and re-arming later, so an ignored directive returns instead of freezing the session. Claude now does the same on its own event: the arming hook re-emits once per prompt up to six times, then latches and stays silent until fifteen further prompts allow a replacement. Two defects surfaced while matching Pi and are fixed in the same change. The arming hook advanced the counter immediately, so a capture that died still marked its window covered and those messages were never revisited -- the counter now advances only inside `publish-memory-capture.sh`, after the merge and global publication succeed, and never backwards. And success was taken from the subagent's own report; the capture filename and timestamp are now fixed when the request is armed, and publication refuses when that file is absent, which is how Pi's `memorySuccessQualifies` decides. Cost falls out of the same alignment: the agent loses its `model:` pin in favour of `CODEFLARE_MEMORY_MODEL`, gains Pi's four-turn budget, and the contract loses the timestamp-derivation step that the precomputed filename makes redundant.

- **Acknowledging a round now reaches its fix handoff instead of exiting silently** (REQ-AGENT-141 AC4 added as Implemented; [REQ-AGENT-104](agents.md#req-agent-104-review-acknowledgement-requires-a-published-verdict) AC4 unchanged but only now reachable). `retroactive_ack_scan` recovers checkpoints for heads whose live enforcement was missed, and it also claimed the current head. Two things then followed in one run: the checkpoint advanced, and the ack-freshness guard immediately below read the mtime of the ack the scan had just written -- zero seconds old, trivially inside its 300-second window -- and exited before the FIX directive. The round was acknowledged with no handoff, no counter touched, and no output, which is indistinguishable from the gate never having run; two consecutive rounds on PR #826 acknowledged this way. Replaying the real transcript with the checkpoint reset reproduced it exactly: zero bytes emitted, ack advanced. Excluding the current head costs the scan nothing, because the live path acknowledges that head in the same invocation and does it with the handoff attached; the same replay then emitted the directive and advanced the demand counter. The freshness guard needed no change once nothing writes the file it measures. No test caught this because every fixture in the suite lacked an upstream, so `git rev-parse @{u}` returned empty and the guard was unreachable; the regression test configures a real origin. The criterion was originally AC9 of REQ-AGENT-121; the collection-aware cleanup later moved it verbatim to REQ-AGENT-141 AC4 when the eligibility and launch-continuity contracts were split.

- **A delivery whose authoritative head is still synchronizing keeps its bounded retries in Claude, as the boundary rework already specified** ([REQ-AGENT-121](agents.md#req-agent-121-checked-out-branch-boundary-synchronization) AC4 added as Implemented; AC3 unchanged). The 2026-08-03 rework made gh's head authoritative and exact equality the eligibility test, and it kept bounded retries for "matching branches whose authoritative heads are still synchronizing". Pi implements that second half in `currentReview`, which treats a head disagreement as retryable rather than as a verdict. The Claude hook implemented only the equality, so a push landing inside the API's lag window read the previous head, disagreed with the checkout, and exited silently -- and `PostToolUse` fires once per tool call, so nothing re-fired for that push. Observed on PR #826, where the boundary arrived on the next unrelated Bash call and announced itself as a review-fix continuation rather than a push. The reminder now retries the authoritative query on a delivery boundary before conceding. Reading the reflog or `refs/remotes` was considered and rejected: that parsing machinery is what the rework deliberately removed, and it answers what this clone observed rather than what the PR carries. Non-delivery activity never retries, so an ordinary unpushed commit still costs no wait.

- **The fix directive's CI wait can only delay the push, never cancel it** ([REQ-AGENT-104](agents.md#req-agent-104-review-acknowledgement-requires-a-published-verdict) AC4 unchanged; [AD123](../../documentation/decisions/README.md#ad123-the-claude-fix-directive-owns-delivery-pi-leaves-it-to-standing-rules) added). The wait was written as "read its log until that line lands", which is unbounded and has no branch for a head whose monitor was never launched or died before writing a terminal line, so the round would stall on a line that never arrives -- the same stopped-and-waiting failure the push order exists to remove, in a different shape. An absent monitor or a log that has not advanced now means push now. The deliberate Claude/Pi divergence this created is recorded as AD123 rather than left implicit: Pi's follow-up names no push and leans on standing rules, which works there because nothing outranks them, while in Claude a hook directive outranks a rule and so a rule-only condition is inert.

- **The fix directive orders the delivery push again, with the CI wait folded in** ([REQ-AGENT-104](agents.md#req-agent-104-review-acknowledgement-requires-a-published-verdict) AC4 restated; remains Implemented). Removing the push order left the condition living only in `git-workflow.md`, and a hook directive outranks a standing rule, so the round stopped after the fix commit and waited to be prodded rather than delivering. The earlier defect was never the order itself but that it fired while the reviewed head's CI was still in flight, discarding that run. Both now travel together: the directive commits, waits for this head's terminal `CI_RESULT` if it has not landed, then pushes without asking, so the in-flight run finishes and its outcome rides the same push. This diverges from Pi's FIX follow-up, which names no push at all and relies on the agent's standing rules; the divergence is deliberate, because in Claude a directive is the only instruction strong enough to close the loop autonomously.

- **The Claude fix directive no longer orders a push, matching Pi** ([REQ-AGENT-104](agents.md#req-agent-104-review-acknowledgement-requires-a-published-verdict) AC4 reworded and its push constraint with it; remains Implemented). Pi's FIX follow-up names no push at all: it says the head is acknowledged and fixes may begin, then leaves delivery to the standing review push gate and CI-result gate, which is why a Pi round never delivers a head whose CI is still in flight. Claude ordered `commit and push the checked-out PR branch without asking` in three places -- the Stop hook's FIX directive, the `git-workflow` rule, and the `git-review-pipeline` skill -- and a hook directive outranks a standing rule, so each round pushed the moment triage closed. The CI run for the reviewed head was then discarded and its verdict landed on a head that no longer existed, buying a second round for every run CI had anything to say about. All three now stop at the commit, and the rule states the positive condition in its place: the push comes on a later turn, once that head's review is acknowledged and its terminal CI result is in. Naming the condition is what keeps removal of the order from becoming a fix commit that is never delivered at all. The regression is pinned by asserting the emitted directive's own `reason` string names no push, so re-adding the order fails rather than reading as a wording change.

- **The Stop-hook review gate no longer reopens an already-reviewed round on a read-only Git command** ([REQ-AGENT-104](agents.md#req-agent-104-review-acknowledgement-requires-a-published-verdict), unchanged AC, behavioral fix; [AD121](../../documentation/decisions/README.md#ad121-a-review-boundary-is-a-delivery-subcommand-not-any-git-invocation)). Lane coverage is measured after an anchor, and that anchor was the last transcript line running any `git`/`gh` command, so a read-only call like `git log` issued while reading lane reports moved it past those lanes' own spawns and the gate demanded them again. Measured on one transcript: 58 candidates against 8 real deliveries. `retroactive_ack_scan` shared the same breadth after its narrower matcher was removed, dropping real-delivery windows that still contained their lane spawns from 7 of 8 to 5 of 8, so those windows never completed and no head was retroactively acknowledged. The repaired ingress now triggers only on `git push` and `gh pr create`; ordinary Git/GitHub activity and `gh pr merge` are inert, while successful clone consent is handled separately. Coverage uses the same parsed delivery boundary, and Layer 2 (`gh pr view`) still decides PR eligibility.

- **The Pi runtime holds the same single-active-repo invariant instead of only ever adding** ([REQ-VAULT-014](vault.md#req-vault-014-graphify-active-repo-invariant-and-lock-serialisation) AC6 added as Implemented; AC5 gains the Pi implementation alongside the Claude one, and [REQ-VAULT-004](vault.md#req-vault-004-unified-global-graph-merges-vault-and-active-repos) AC6 likewise; all remain Implemented). Pi's session-start and repository-transition handlers called `graphify global add` and nothing else, so every repository a Pi session touched accumulated in the global manifest and a checkout without a graph kept publishing the repository before it. Pi now enumerates removals from the manifest and runs them with the addition in one lock acquisition, matching the Claude hook, and the transition handler no longer skips reconciliation when the new checkout has no graph, which was the case that most needed it. The `graph.json` beside the vault's cumulative `vault-graph.json` is described accurately across both runtimes: each merge refreshes it as a copy for the local visualization, and it is empty only until the first extraction, which is the boot state the seed fix addresses. Both runtimes' publication pre-check ([REQ-VAULT-004](vault.md#req-vault-004-unified-global-graph-merges-vault-and-active-repos) AC2, restated) now requires the manifest's recorded source path to be this checkout's graph as well as its hash, and validates that hash as hexadecimal rather than by length alone. Tags are keyed by directory basename, so two checkouts sharing one can hold byte-identical graphs, and matching on the hash alone skipped the add and left the tag resolving to the checkout the user had just left, which is the drift the reconciliation exists to remove.

- **The global graph is reconciled against an invariant instead of one recorded transition, and only a git checkout can be the active repo** ([REQ-VAULT-004](vault.md#req-vault-004-unified-global-graph-merges-vault-and-active-repos) AC6-AC7 and [REQ-VAULT-014](vault.md#req-vault-014-graphify-active-repo-invariant-and-lock-serialisation) AC5 added as Implemented; REQ-VAULT-004 AC3 absorbs checkout identification alongside the vault exclusion it already carried, since both answer what resolves as the active repo, and AC4 gains the branch and graph-presence comparisons; all remain Implemented). Active-repo resolution had also accepted a bare `graphify-out/` directory as a repo root, so any directory holding graph output minted a repo tag from its basename and evicted the real repository from the global graph; only `.git`, as a directory or as a worktree file, identifies a checkout now. Removals are enumerated from the global manifest rather than derived from the previously-recorded repository, so a tag left by a crashed reconciliation or by a formerly-misresolved directory is swept on the next tool call, `user_vault` is never a removal candidate, and a checkout without a graph leaves the vault alone in the global graph. The fast path additionally compares the checked-out branch and whether the graph exists, both recorded in a second sentinel so the active-repo sentinel keeps the first-line-is-the-path format the MCP wrapper reads. A branch change is a reconciliation trigger rather than a rebuild trigger, which preserves the existing staleness window, and a graph deleted at an unchanged path no longer reads as "not rebuilt" and stays published forever. Both runtimes describe one contract: Pi's native repo resolver drops the same `graphify-out/` marker so query scoping cannot answer from a phantom repo, and Pi's graph-layout prompts name the cumulative `vault-graph.json` rather than the scaffold.

- **The vault's boot seed publishes the cumulative vault graph, not the empty scaffold beside it** ([REQ-MEM-009](memory.md#req-mem-009-vault-graph-accumulates-monotonically-across-extractions) AC5-AC6 added as Implemented). `init_user_vault()` seeded `user_vault` from `graphify-out/graph.json`, which the graph builder leaves as an empty scaffold, so every boot republished the vault with zero nodes and dropped accumulated memory out of the global graph until the next capture happened to run. The seed reads `graphify-out/vault-graph.json` instead, and an existence guard keeps a first boot with no captures silent rather than warning about a file that is not supposed to exist yet.

- **The mid-turn triage gate no longer refuses a subagent's tool calls** ([REQ-AGENT-104](agents.md#req-agent-104-review-acknowledgement-requires-a-published-verdict) AC7 unchanged; remains Implemented). Claude Code fires `PreToolUse` for a subagent's tool calls exactly as for the main agent's and hands them the parent's `transcript_path`, so the gate read the main session's review state and refused subagents work they take no part in: background memory capture lost `Write` calls, the vault capture file among them, and the run that hit it twice spent roughly double a clean run. A subagent payload carries `agent_type` and `agent_id` while a main-agent payload carries neither, so the script now exits on the presence of `agent_type` before any transcript or sentinel handling, restoring the main-session-only scope its own header already documented and leaving the one-shot bypass sentinel for the session it belongs to.
