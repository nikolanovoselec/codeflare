# Review Queue

Layout-resolved triage file for the nested SDD layout. Holds open PR-boundary review findings only; resolved findings are removed (git history is the audit trail).

## Open

### Deferred cleanup — cf_vault_sid cookie name leaks into REQ-VAULT-021/023/024 spec prose

**Finding ID:** mechanism-leakage-cf-vault-sid-1  (2026-07-02)
**Category:** mechanism-leakage (forbidden content: cookie names belong in `documentation/lanes/security.md`, not REQ AC/Constraints prose)
**Affected:** REQ-VAULT-021, REQ-VAULT-023, REQ-VAULT-024

The literal cookie name `cf_vault_sid` appears directly in REQ prose at `sdd/spec/vault.md:681,683,693` (REQ-VAULT-021 AC2/AC4/Constraints) and `:752` (REQ-VAULT-024 Constraints, moved verbatim from REQ-VAULT-008 during the 2026-07-01 split). Not newly introduced — pre-existing across all four sites, confirmed identical pattern in three REQs. `documentation/lanes/security.md` already documents the cookie. Not fixed piecemeal on 2026-07-02 (a spec-reviewer pass on REQ-VAULT-024 alone flagged it) because trimming just one of the four sites would leave the other three inconsistent with the one just "fixed" — this needs a coordinated sweep across all three REQs in one pass, not four separate partial edits. Tracked here so it isn't silently dropped; pick up in the next `/sdd clean --all` pass alongside the whole-tree documentation backlog (`documentation/.doc-coverage.md`, 2026-07-02 entry).
