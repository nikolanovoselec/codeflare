# Review Queue

Layout-resolved triage file for the nested SDD layout. Holds open PR-boundary review findings only; resolved findings are removed (git history is the audit trail).

## Open

- **REQ-AGENT-062 sits at 8 ACs (MEDIUM `ac-count-over-cap`) — escalated, not split.** AC8 (ground-truth monitor-liveness early reclaim → prompt re-spawn) is a distinct delivery-reliability guarantee, so it is genuinely a new AC rather than a rider on AC1's claim-dedup. All 8 ACs are one coherent monitor claim→wait→summary→completion→validate→failed→manual-fallback→liveness-reclaim state machine; the mandated first-12-word clustering yields no two operationally-distinct clusters, so the mechanical fallback is an arbitrary median split that would sever tightly-coupled ACs (e.g. AC4 marker-write from AC5 record-validation). Disposition: accept at 8 with this rationale rather than force a low-quality split. Revisit only if a 9th AC is proposed (would cross into a mandatory split at >10).
