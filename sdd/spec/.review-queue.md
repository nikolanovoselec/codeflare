# Review Queue

Layout-resolved triage file for the nested SDD layout. Holds open PR-boundary review findings only; resolved findings are removed (git history is the audit trail).

## Deferred — REQ-LANDING-001 structural bloat (2026-07-11)

**Finding ID:** constraint-section-bloat-1 (2026-07-11)
**Category:** constraint-section-bloat | ac-verbose | ac-multi-behaviour | constraint-bloat | forbidden-content-in-req
**Affected:** REQ-LANDING-001 (sdd/spec/landing.md)

**Findings:** `constraint-section-bloat` (HIGH) — Constraints section ~1010 words across 9 bullets (bullets 5, 7, 9 each exceed 80 words); `ac-verbose` / `ac-multi-behaviour` (MEDIUM) — AC4 is ~1057 words / 21 semicolons chaining ~15 distinct sub-features into one AC; `constraint-bloat` (MEDIUM x3) — Constraints bullets 1, 6, 8 (53 / 48 / 78 words) exceed the 45-word conciseness threshold; `forbidden-content-in-req` (MEDIUM) — AC4 embeds CSS class names (`.substation`), a custom property (`--fs-subhead`), a pseudo-element (`::details-content`), a file reference (`orch.ts`), and JS-hook data attributes (`data-ft-loop`, `data-ft-shuffle`, `data-roll`) that belong in `documentation/lanes/architecture.md`.

**Disposition:** DEFERRED (main-session judgment). REQ-LANDING-001 packs the entire landing-page narrative into a single AC4 plus a large Constraints section. Correcting it fully — decompose AC4 into per-section ACs, move the mechanism tokens to `documentation/lanes/architecture.md`, and break the Constraints into focused bullets — is a dedicated landing-spec refactor, out of scope for the incremental bug/copy fixes that keep touching one clause and tripping the whole-REQ walk. A blind mechanical split/strip would orphan by-number cross-refs across REQ-LANDING-002..007 and multiple `@test`/`@impl` anchors, so escalation is the correct disposition over auto-fix. This is not a behavioral defect: the landing behavior is correct, tested, and shipped. Fold all of the above into a focused REQ-LANDING-001 restructure or a `/sdd clean --scope=all` pass. Re-surfaces on any edit to REQ-LANDING-001 until then.
