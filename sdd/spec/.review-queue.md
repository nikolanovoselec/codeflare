# Review Queue

## ac-count-over-cap-1 — REQ-AGENT-040 carries 8 acceptance criteria

**Finding ID:** ac-count-over-cap-1 (2026-07-26)
**Category:** `ac-count-over-cap`
**Affected:** REQ-AGENT-040
**Status:** open — accepted within band, revisit if a ninth is proposed

AC8 took the REQ from 7 to 8 criteria, which fires the 8-10 band guard. The
sibling merge the reviewer offered was declined: AC7 ("both runtimes decide a
range identically") and AC8 ("a round-limited lane costs no model invocation")
are two contractual verbs, and folding them into one bullet trades this MEDIUM
for `ac-multi-behaviour`, which is the stronger rule. The merge text proposed
also restored "before it is demanded" - false for Claude, whose runner demands
the lane and short-circuits inside it.

A ninth criterion should split the REQ rather than extend it.
