# Review Queue

Layout-resolved triage file for the nested SDD layout. Holds open PR-boundary review findings only; resolved findings are removed (git history is the audit trail).

## Open

- **Finding ID:** ac-count-over-cap-1 (2026-07-02)
  **Category:** ac-count-over-cap
  **Affected:** REQ-AGENT-062

  REQ-AGENT-062 sits at 8 ACs (MEDIUM `ac-count-over-cap`) — escalated, not split. AC8 (ground-truth monitor-liveness early reclaim → prompt re-spawn) is a distinct delivery-reliability guarantee, so it is genuinely a new AC rather than a rider on AC1's claim-dedup. First-12-content-word clustering does find two lexical clusters — {AC1,AC8} (claim/monitor tokens) and {AC2,AC3,AC4,AC7} (lane/summary tokens) — but they are not operationally-distinct sub-features: they are sequential phases of one review claim→wait→summary→completion→validate→failed→manual-fallback→liveness-reclaim lifecycle (e.g. AC1 creates the single monitor claim and AC8 reclaims that same claim — one actor, one resource). A mechanical median split would sever that lifecycle across two REQs for no product-boundary gain. Disposition: accept at 8 on sequential-phase coherence (`concern-boundary-split-required` prong 2 fails — clusters are lexical, not operational) rather than force a low-quality split. Revisit only if a 9th AC is proposed (would cross into a mandatory split at >10).
