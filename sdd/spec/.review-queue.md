# Review Queue

Layout-resolved triage file for the nested SDD layout. Holds open PR-boundary review findings only; resolved findings are removed (git history is the audit trail).

## Open

### PR #601 (develop→main), head 37fbfa2 — spec-reviewer follow-up pass, 2026-07-01

**HIGH — `spec-vs-shipped` / sync gap: REQ-ENTERPRISE-018 AC4/AC8 prose is stale relative to shipped+tested behavior added by 37fbfa2**

File: `sdd/spec/enterprise-mode.md`, AC4 (line 574) and AC8 (line 578) of `REQ-ENTERPRISE-018`.

Commit 37fbfa2 (code-review MEDIUM/LOW fixes) shipped and tested two new behaviors that AC4/AC8 text does not describe:
1. `RegimeState` gained `halted?: boolean` (`src/lib/r2-regime-state.ts:59`) — AC4's state-shape literal still lists `..., stuckCount?, lastFailedKey?, total?, processed?}` with no `halted?`.
2. `countObjects` (`src/lib/r2-migration.ts:350`) gained a `deadline` param and now bails to `total: null` (⇒ no %) if a list page would not finish before the shared work deadline — not mentioned in AC4's "≤ COUNT_LIST_CAP pages" description.
3. `bucketMigrationPercent` in `src/routes/session/lifecycle.ts:194` is now gated `bucketMigrating && !regimeState.halted && ...` — AC4/AC8 prose doesn't mention percent suppression when halted (a wedged migration at the verify-retry ceiling).

All three are real, tested (`src/__tests__/lib/r2-migration.test.ts`: "skips the object count (no %) when the work budget is already spent..." + "suppresses the progress % when the migration has halted..."; `src/__tests__/routes/session-batch-status.test.ts`: "suppresses the progress % when the migration has halted so the button never reads a misleading 99%"). REQ Status stays honestly `Implemented` (the AC as currently worded is still met and tested) — this is a spec-under-describes-shipped-behavior gap, not an unmet-AC gap.

Proposed fix (ready to paste):
- AC4 state-shape literal: append `, halted?` → `{status: ready|migrating|mixed-recovery, regime, from?, to?, generation, cursor?, phase?, drained?, leaseExpiresAt?, keyMd5?, stuckCount?, lastFailedKey?, total?, processed?, halted?}`.
- AC4 prose addendum (append before the `<!-- @impl -->` chain): "The one-time object count itself respects the same work deadline — `countObjects` bails to `total: null` (⇒ no %) if another list page would not finish before the shared deadline, so it can never strand the one-time drained-commit. If verify retries exhaust at the retry ceiling, the state is marked `halted: true` and `bucketMigrationPercent` is suppressed so a wedged migration never displays a stale/misleading percentage."
- AC8 prose: append "...omitted while `halted` (a wedged migration awaiting admin review)." after the existing `bucketMigrationPercent` mention.
- Changelog: amend the existing 2026-07-01 `sdd/spec/changes.md` entry (already says "AC4 + AC8 amended") rather than adding a new entry — append one clause noting the halted-suppression + deadline-bail refinement from the code-review follow-up.

**LOW — anchor-convention nit (pre-existing, informational only): `countObjects` `@impl` anchor points to a non-exported symbol**

File: `sdd/spec/enterprise-mode.md` AC4, anchor `<!-- @impl: src/lib/r2-migration.ts::countObjects -->`. `countObjects` (`src/lib/r2-migration.ts:350`) is a module-private `async function` (no `export`), unlike every other `::symbol` anchor in this REQ (all exported). The symbol still resolves (real function, called by the already-anchored `advanceMigration` in the same file) so this is not a broken anchor — just a convention inconsistency. Introduced in the prior round (ec738ae), not by 37fbfa2. No action required unless the reviewer wants anchor-convention parity; safe to leave.

**Restated for the record (pre-existing, explicitly deferred per user instruction — NOT new, NOT re-opened this round):**
- HIGH `ac-count-over-cap` / granularity — REQ-ENTERPRISE-018 has grown to 9 ACs with heavy embedded prose/rationale; candidate for a REQ-family split.
- HIGH `constraint-section-bloat` — REQ-ENTERPRISE-018's Constraints section.
- HIGH dependency cycle — REQ-BROWSER-008 ↔ REQ-ENTERPRISE-016.
- HIGH `function-names-in-ac-prose` — AC bullets across REQ-ENTERPRISE-018 name implementation functions directly in prose (e.g. `countObjects`, `planRegimeReconcile`, `resolveReadRegime`).

These four are tracked as a dedicated spec-hygiene pass per the user's own commit message on 37fbfa2 ("Deferred (pre-existing, out of scope for this UX fix)...").
