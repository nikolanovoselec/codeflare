# Review Queue

No open findings.

## 2026-07-19 - PR MERGED un-acked at merge/close
- PR for branch `develop` reached MERGED with un-acked HEAD `7020f4b` (last ack: `e36d33a`). Review pipeline did not complete before merge.
- **Triaged 2026-07-19: false positive — squash-merge SHA artifact.** PR #680 (`develop` → `main`) was squash-merged by the user, so the merge lands as a new commit (`e36d33a`) that never equals the reviewed branch head (`7020f4b`). The review pipeline DID complete for that head: all three lanes returned clean before merge. No action.
