# Review Queue

Layout-resolved triage file for the nested SDD layout. Holds open PR-boundary review findings only; resolved findings are removed (git history is the audit trail).

## Open

- **Finding ID:** ac-count-over-cap-1 (2026-07-02)
  **Category:** ac-count-over-cap
  **Affected:** REQ-AGENT-062

  REQ-AGENT-062 sits at 8 ACs (MEDIUM `ac-count-over-cap`) — escalated, not split. AC8 (ground-truth monitor-liveness early reclaim → prompt re-spawn) is a distinct delivery-reliability guarantee, so it is genuinely a new AC rather than a rider on AC1's claim-dedup. First-12-content-word clustering does not yield a clean >=2-cluster partition: AC1 bridges two candidate groupings (shares `claim`/`monitor` with AC8, and `repo`/`head` with AC5), leaving AC2, AC3, AC4, AC5, AC7 chained into one connected cluster via shared `lane`/`summary`/`monitor`/`result` vocabulary, with only AC6 unlinked. `concern-boundary-split-required` prong 1 (>=2 clusters, each with >=2 ACs) is not met, so the rule does not fire — independent of that, AC1 and AC8 govern the same resource (the monitor claim: AC1 creates it, AC8 reclaims it), so a median split would also sever a create/reclaim pair for no product-boundary gain. Disposition: escalate and accept at 8 (a rule-sanctioned option for a fired `ac-count-over-cap`) rather than force a low-quality split. Revisit if further ACs are proposed — a 9th or 10th stays in the 8–10 MEDIUM band; an 11th would trip the `>10` mandatory-split threshold.
