# Review Queue

**Finding ID:** enforcement-skill-not-invoked-1
**Category:** enforcement-skill-not-invoked
**Severity:** HIGH
**Affected:** spec-reviewer agent invocation protocol (not repo content)
**Opened:** 2026-07-17, PR #645 full-diff wave at the deps-merge head
**Detail:** The full-diff spec-reviewer run self-reported performing manual diff analysis before invoking `spec-enforce`, and substituting manual verification for a `spec-enforce-truth` sub-invocation whose trigger condition (Implemented REQs touched) was met. The content checks those skills own were performed manually and returned clean, so no repo-content finding was lost — the open item is the protocol gap itself.
**Closure condition:** A subsequent spec-reviewer wave (or `/sdd clean` pass) demonstrates skill-first invocation with `spec-enforce-truth` fired when its trigger condition is met. The first incremental wave after the finding already reported `spec-enforce (ran)` with correctly-inert sub-skills; one more compliant full-trigger run closes this.
