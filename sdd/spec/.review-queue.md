# Review Queue

Layout-resolved triage file for the nested SDD layout. Holds open PR-boundary review findings only; resolved findings are removed (git history is the audit trail).

## Open

### Accepted convention — REQ-ENTERPRISE-020 (structural/style, not tied to a specific PR round)

**Finding ID:** function-names-in-ac-prose-1  (2026-07-01)
**Category:** function-names-in-ac-prose
**Affected:** REQ-ENTERPRISE-020

AC prose names implementation symbols (`planRegimeReconcile`, `advanceMigration`, `fetchObjectWithRegimeFallback`, `markMixedRecovery`, …) beyond the `@impl` anchors, for this state-machine REQ (moved here from REQ-ENTERPRISE-018 by the 2026-07-01 split; the finding is unchanged by the split, only its REQ home). A `forbidden_content_overrides` entry was tried to sanction it via config and reverted as guardrail-gaming — the check stays live and intentionally keeps firing. Correction (2026-07-01, spec-reviewer): `sdd/spec/config.yml`'s `forbidden_content_overrides` is empty and its `changelog_entry_style: verbose` override is scoped to `changes.md` only, NOT to REQ AC/Constraints bodies — the earlier "soft-warnings-only per config" framing here was inaccurate. This is pre-existing carried-over content (not newly authored), tracked pending a deliberate `/sdd clean --all` granularization pass, not silently dropped. Re-triage should keep this entry.

### Deferred restructuring — AC-verbosity / constraint-section-bloat on REQ-ENTERPRISE-020 + REQ-VAULT-021/022/023 (not tied to a specific PR round)

**Finding ID:** ac-verbose-constraint-bloat-1  (2026-07-01)
**Category:** ac-verbose, ac-run-on, constraint-section-bloat
**Affected:** REQ-ENTERPRISE-020, REQ-VAULT-021, REQ-VAULT-022, REQ-VAULT-023

The 2026-07-01 REQ splits (draining `ac-count-over-cap`) correctly resolved AC-count, but the individual ACs and Constraints carried over verbatim are independently over the per-AC word/sentence/multi-behaviour thresholds and the Constraints-section total-word threshold (REQ-ENTERPRISE-020 AC2 alone is ~600 words bundling 6+ distinct behaviors; REQ-ENTERPRISE-020 and REQ-VAULT-021 Constraints sections both exceed 150 words). This content is pre-existing (byte-identical to the pre-split REQs, not newly authored by the split), so it is tracked here rather than attempted as a rushed second-order split during an already-large change — granularizing REQ-ENTERPRISE-020 AC2/AC4 and REQ-VAULT-022/023's ACs further could itself push a REQ over the 7-AC soft-split trigger, cascading into more splits. Left for a deliberate `/sdd clean --all` pass. Also flagged in the same sweep, same disposition: mechanism leakage in REQ-VAULT-022 AC1 (cookie name `cf_vault_sid`, localStorage key pattern, and internal function names in AC prose rather than only `@impl` anchors) and pre-existing Rule-B shape drift in REQ-VAULT-008's `**Notes:**` field (8+ sentences with mechanism tokens, over the 2-sentence doc-pointer shape).

### Deferred cleanup — REQ-ENTERPRISE-018→020 plain-comment drift (not tied to a specific PR round)

**Finding ID:** stale-comment-req-id-1  (2026-07-01)
**Category:** stale-comment-req-id
**Affected:** REQ-ENTERPRISE-018, REQ-ENTERPRISE-020

~38 plain inline comments (not `@impl`/`@test` anchors, not test-name strings) across ~25 source files still cite `REQ-ENTERPRISE-018` for behavior the 2026-07-01 split moved to `REQ-ENTERPRISE-020` (state machine, chunked scan, zero-secret invariant, backend 409 gate, dual-regime reads). Files include `src/routes/{preferences,storage/seed,storage/browse,storage/delete,storage/download,storage/preview,vault,container/lifecycle,container/lifecycle-init,session/lifecycle}.ts`, `src/lib/{sync-fanout,session-helpers,migration-containers,r2-regime-state,error-types,r2-migration,r2-sse,kv-keys}.ts`, `src/container/container-env.ts`, `src/types.ts`, `web-ui/src/{lib/schemas,stores/session,stores/session-polling}.ts`, and several test files. Not blocking: no `@impl`/`@test` anchor or spec-truth check depends on these comments (confirmed by two independent code-reviewer passes on the split commits — both explicitly declined to raise this as a diff finding, calling a flat rename "scope-creep into unchanged code"). Cosmetic precision only; sweep in a dedicated `/sdd clean --all` or similar pass, not worth its own PR-boundary review cycle.
