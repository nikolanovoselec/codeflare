# Review Queue

Layout-resolved triage file for the nested SDD layout. Holds open PR-boundary review findings only; resolved findings are removed (git history is the audit trail).

## Open

### Deferred spec-hygiene — REQ-ENTERPRISE-018 (structural/style, not tied to a specific PR round)

Long-accreting items on REQ-ENTERPRISE-018, dispositioned as soft/judgment per `sdd/spec/config.yml` (`req_length` is "soft warnings only, never auto-fix"; verbose content is accepted; `mode: auto` escalates JUDGMENT restructurings here rather than auto-applying). Not defects, not blocking:

- **`ac-count-over-cap` / REQ-family split** — the REQ has grown to 9 ACs with heavy embedded rationale; candidate for splitting the migration-engine ACs (copy mechanics / state-machine driver / dual-read) into a sibling REQ. A JUDGMENT-class restructuring (renames + re-anchors ~30 `@impl`/`@test` references), left for a deliberate `/sdd` pass.
- **`constraint-section-bloat`** — the Constraints section carries load-bearing rejected-alternatives rationale; verbose is accepted per config, so trim only as part of the split above.
- **`function-names-in-ac-prose`** — AC prose names implementation symbols (`planRegimeReconcile`, `advanceMigration`, …); the deliberate convention for this state-machine REQ, with the `@impl` anchors carrying the symbols. (A `forbidden_content_overrides` entry was tried to sanction this and reverted as guardrail-gaming — the check stays live.)
