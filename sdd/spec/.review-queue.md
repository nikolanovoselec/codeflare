# Review Queue

Layout-resolved triage file for the nested SDD layout. Holds open PR-boundary review findings only; resolved findings are removed (git history is the audit trail).

## Open

- **Finding ID:** ac-count-over-cap-1 (2026-07-02)
  **Category:** ac-count-over-cap
  **Affected:** REQ-AGENT-062

  REQ-AGENT-062 sits at 8 ACs (MEDIUM `ac-count-over-cap`) — escalated, not split. AC8 (ground-truth monitor-liveness early reclaim → prompt re-spawn) is a distinct delivery-reliability guarantee, so it is genuinely a new AC rather than a rider on AC1's claim-dedup. Running `spec-enforce-ac`'s first-12-content-word clustering over the 8 ACs does not yield two qualifying clusters (each with >=2 ACs) under any tokenization tried — they collapse into one connected component plus singletons — so `concern-boundary-split-required` prong 1 is not met and the rule does not force a split. Independently, AC1 and AC8 govern the same resource (the monitor claim: AC1 creates it, AC8 reclaims it), so a median split would sever a create/reclaim pair for no product-boundary gain. Disposition: escalate and accept at 8 (a rule-sanctioned option for a fired `ac-count-over-cap`) rather than force a low-quality split. Revisit if further ACs are proposed — a 9th or 10th stays in the 8–10 MEDIUM band; an 11th trips the `>10` mandatory-split threshold.
