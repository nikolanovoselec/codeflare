# Review Queue

Layout-resolved triage file for the nested SDD layout. Holds open PR-boundary review findings only; resolved findings are removed (git history is the audit trail).

## Open

- **Finding ID:** ac-count-over-cap-1 (2026-07-02)
  **Category:** ac-count-over-cap
  **Affected:** REQ-AGENT-062

  REQ-AGENT-062 sits at 8 ACs (MEDIUM `ac-count-over-cap`) — escalated, not split. AC8 (ground-truth monitor-liveness early reclaim → prompt re-spawn) is a distinct delivery-reliability guarantee, so it is genuinely a new AC rather than a rider on AC1's claim-dedup. `concern-boundary-split-required` still does not fire on prong 2, independent of tokenization: even where `spec-enforce-ac`'s first-12-word clustering surfaces an {AC1, AC8} cluster versus the rest (it can, under a plain non-alphanumeric-boundary tokenizer that splits `claim's`→`claim`), AC1 and AC8 are not operationally distinct sub-jobs — they are the create and reclaim phases of the same monitor-claim resource, so a split there would sever one lifecycle concern rather than separate two features. Disposition: escalate and accept at 8 (a rule-sanctioned option for a fired `ac-count-over-cap`) rather than force a low-quality split. Revisit if further ACs are proposed — a 9th or 10th stays in the 8–10 MEDIUM band; an 11th trips the `>10` mandatory-split threshold.
