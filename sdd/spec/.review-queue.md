# Review Queue

Layout-resolved triage file for the nested SDD layout. Sections labelled by origin.

## 2026-05-22 - /sdd clean pass 3 (AC-cap split deferrals)

23 REQs exceed the 7-AC soft cap. Splits are judgment work (deciding logical AC clusters, allocating new REQ IDs, updating cross-references) and the /sdd clean run was invoked in autonomous mode with the explicit instruction to route judgment gaps here rather than auto-apply. Each entry below records the over-cap count and a one-line split sketch the user can validate or override. No spec content was changed in pass 3 itself.

### OPEN -- REQ-AGENT-007 Multi-Agent Adaptation Pipeline (8 ACs)

**Context:** ACs 1-4 cover the high-level adaptation contract (per-agent generation, tool name remap, monolithic-vs-rule-files, frontmatter format). ACs 5-8 cover specific transforms (model field removal, path rewrites, file extensions, exclusion lists).
**Recommendation:** Defer. Slight overrun (8/7) and ACs read as one coherent pipeline; splitting would force an artificial boundary. Consider leaving as-is until a real refactor changes the implementation.

### OPEN -- REQ-AGENT-009 LLM API Key Storage (8 ACs)

**Context:** ACs 1-5 cover the storage contract (endpoint shape, KV path, encryption, masking, GET behaviour). ACs 6-8 cover the propagation contract (env-var injection, MCP server wiring, no-DO-persistence).
**Recommendation:** Optional split into REQ-AGENT-009 "LLM API Key Storage" (ACs 1-5) + new REQ-AGENT-NNN "LLM API Key Propagation to Container" (ACs 6-8). Borderline (8/7); skip unless a future change forces the boundary.

### OPEN -- REQ-AGENT-010 Deploy Credential Storage (11 ACs)

**Context:** ACs 1-8 cover storage + propagation (CRUD endpoints, KV encryption, env-var injection, git credential helper, CF account ID auto-fetch). ACs 9-11 cover the UX/UI for token creation (GitHub scope tiers selector, Cloudflare template direction, scope documentation page).
**Recommendation:** Split into REQ-AGENT-010 "Deploy Credential Storage" (ACs 1-8) + new REQ-AGENT-NNN "Deploy Credential Token-Creation UX" (ACs 9-11). The UX boundary is clear; the new REQ would dep on REQ-AGENT-010 + REQ-AGENT-019 (Branded settings UI).

### OPEN -- REQ-AGENT-011 Agent Configs Manually Recreatable (8 ACs)

**Context:** ACs 1-5 cover the recreate-skills button + cleanup semantics. ACs 6-8 cover the recreate-docs button, rate limiting, cache invalidation.
**Recommendation:** Defer. The two buttons are conceptually one feature ("Recreate user-facing seed content from Settings"); splitting would scatter the contract.

### OPEN -- REQ-AGENT-021 Spec-Driven Development Workflow (26 ACs)

**Context:** The largest REQ in the spec. ACs span: preseed contents (AC1), /sdd init scaffolding (AC2-AC6), PR-boundary review pipeline (AC7-AC11), /sdd clean semantics (AC12), agent self-limit (AC13), branch-checkout semantics (AC14), three-discipline content quality (AC15), stop-hook bypass surfaces (AC16), /sdd init two-confirm flow (AC17), enrichment pass (AC18), canonical REQ shape (AC19), audit file layout (AC20), tool-surface portability (AC21), graphify enrichment (AC22-AC24), Phase 7a verifier (AC25), Phase 7b verifier (AC26).
**Recommendation:** Split into at least 4 REQs: (a) `/sdd init` lifecycle (greenfield + import + enrichment + Phase 7a/7b verifier gates) - ACs 2-6, 17-20, 25, 26; (b) PR-boundary review pipeline (lane classification, sequential agents, fix-push cascade, stop-hook bypass) - ACs 7-11, 16; (c) /sdd clean + autonomy modes + per-discipline content quality - ACs 12, 13, 14, 15; (d) Pro-mode preseed integration with graphify-driven enrichment - ACs 1, 21, 22, 23, 24. This is high-risk surgery; recommend user-driven split rather than auto.

### OPEN -- REQ-AGENT-022 Legacy-codebase transition to SDD (14 ACs)

**Context:** ACs 1-5 cover Import Mode discovery surface + triage entry shape + transition-suspend semantics. ACs 6-11 cover Resume Mode (refresh context, five decision verbs, commit-per-decision, drain-condition + closure). ACs 12-14 cover the review-pipeline short-circuit, GitHub-corpus unreachable degradation, and enforce_tdd-aware status default.
**Recommendation:** Split into REQ-AGENT-022 "Import Mode discovery + triage" (ACs 1-5, 13, 14) + new REQ-AGENT-NNN "Resume Mode drain workflow" (ACs 6-12). The drain loop is logically separable from the initial import pass.

### OPEN -- REQ-MEM-001 Conversation context auto-captured (9 ACs)

**Context:** Per-AC review needed; likely splits along capture-pipeline / graph-merge / timezone-handling lines.
**Recommendation:** User review required - the memory pipeline's AC clustering is not obvious from titles alone.

### OPEN -- REQ-MEM-006 Memory only in Pro mode (8 ACs)

**Context:** Mode-gating contract has both behaviour (memory enabled/disabled) and infrastructure (plugin enablement, hook registration).
**Recommendation:** Defer. Slight overrun; the contract reads as one gate.

### OPEN -- REQ-MOB-003 Samsung Internet keyboard quirks (8 ACs)

**Context:** Browser-specific workarounds for one engine. Likely splits along visualViewport vs autocorrect vs paste-event handling.
**Recommendation:** Defer or split along the three sub-surfaces (viewport, autocorrect, paste). User review.

### OPEN -- REQ-MOB-004 Scroll stability (9 ACs)

**Context:** Splits along scroll-anchor vs keyboard-transition vs xterm-stack-trace lines.
**Recommendation:** Optional split into REQ-MOB-004 "Scroll anchor during output" + new REQ "Scroll behaviour during keyboard transitions". User review.

### OPEN -- REQ-MOB-007 Voice input (9 ACs)

**Context:** Web Speech API support detection + UI button + interim/final transcript handling + permission gate + send-on-stop semantics.
**Recommendation:** Defer. The voice flow is conceptually one feature; splitting would scatter UX context.

### OPEN -- REQ-OPS-001 Deploy triggered by push to main (9 ACs)

**Context:** Workflow trigger + job ordering + secret resolution + artifact passing + post-deploy verification.
**Recommendation:** Split into REQ-OPS-001 "Deploy workflow trigger + job graph" (ACs 1-5) + new REQ-OPS-NNN "Deploy verification + post-deploy hooks" (ACs 6-9).

### OPEN -- REQ-OPS-002 Docker image built, scanned, deployed (9 ACs)

**Context:** Image build + Trivy scan + registry push + DO container reference.
**Recommendation:** Optional split into image-build pipeline vs DO-binding contract. Probably best left whole; user judgment.

### OPEN -- REQ-OPS-004 E2E tests on deployed worker (8 ACs)

**Context:** Playwright suite invocation + service-token injection + retry budget + report archival.
**Recommendation:** Defer. Coherent one-feature contract.

### OPEN -- REQ-OPS-006 Idle containers cost zero (10 ACs)

**Context:** Hibernation contract spans DO storage shape, WS close-codes, KV polling, billing implications.
**Recommendation:** Split candidate but ACs are heavily cross-referenced; user review required.

### OPEN -- REQ-SEC-004 Credential encryption at rest (8 ACs)

**Context:** Key import validation + AES-256-GCM round-trip + ciphertext format + AAD binding + isolate caching + masking + plaintext-mode warning + non-secret-allowlist.
**Recommendation:** Defer. All eight ACs are facets of the same encryption-at-rest contract.

### OPEN -- REQ-SEC-007 Rate limiting on mutation endpoints (11 ACs)

**Context:** ACs 1-4 cover the general rate-limiter contract (key derivation, KV storage with in-memory fallback, 429 shape, headers). ACs 5-9 cover specific limits (WS, sessions, fail-closed-vs-fail-open, stress-test bypass). ACs 10-11 cover the close-code-4503 and close-code-1013 pre-rate-limit short-circuits.
**Recommendation:** Split into REQ-SEC-007 "Rate-limiting infrastructure" (ACs 1-4) + new REQ-SEC-NNN "Per-endpoint rate-limit policy" (ACs 5-9) + new REQ-SEC-NNN "WS-upgrade rate-limit short-circuits" (ACs 10-11).

### OPEN -- REQ-SEC-008 Security headers (9 ACs)

**Context:** Eight individual header policies + automated-pentest verification.
**Recommendation:** Defer. The nine ACs are one composite contract ("send these headers on every response").

### OPEN -- REQ-SETUP-006 Setup streams progress via NDJSON (9 ACs)

**Context:** Stream protocol shape + reconnect handling + idempotency token + completion marker.
**Recommendation:** Defer. NDJSON streaming is one feature.

### OPEN -- REQ-STOR-007 Web File Browser (8 ACs)

**Context:** Browse + upload + download + delete + preview endpoints + drawer/bottom-sheet UI + direct-from-R2 read + traversal guard.
**Recommendation:** Defer. The five CRUD endpoints share rate-limit budget and UI surface.

### OPEN -- REQ-VAULT-001 Persistent vault directory (9 ACs)

**Context:** Vault filesystem layout + bisync include + sentinel files + ownership contract + size guard.
**Recommendation:** User review - vault structure changes are coupled to vault-005 proxy and vault-003 ingestion.

### OPEN -- REQ-VAULT-003 User-curated edits ingested ~60s (8 ACs)

**Context:** Watcher cadence + change-detection algorithm + chunk shape + graph-merge contract + PDF-ingestion E2E.
**Recommendation:** Defer. Coherent ingestion-pipeline contract.

### OPEN -- REQ-VAULT-005 Worker proxy exposes vault editor (10 ACs)

**Context:** Proxy route + auth gate + bootstrap-hop encryption flow + WebSocket upgrade + URL rewriter + offline fallback.
**Recommendation:** Split candidate along encryption-bootstrap vs proxy-routing lines; user review required (this REQ is referenced in active development).

## Escalations (from .review-needed.md)

## 2026-05-18 - PR #385 commit 43cf208 (spec-reviewer r12) - RESOLVED

### RESOLVED -- REQ-VAULT-003 Verification field now covers AC7/AC8 (PDF ingestion)

Verification field extended 2026-05-19 with the PDF-ingestion E2E (drop a `.pdf` into `Raw/Pasted/`, wait for next daemon tick, confirm a `document` node appears in the global graph and a corrupt PDF emits a bare document node without blocking the high-water marker). Resolved.

## 2026-05-18 - PR #385 (develop -> main) - ALL RESOLVED

### RESOLVED -- REQ-MEM-001 AC3 names implementation script (LOW)

AC3 referred to `prefilter-transcript.sh` by name. The filename was removed from the AC body in the prior spec-reviewer pass (F-003); this entry confirms the fix was applied. Resolved 2026-05-18.

### RESOLVED -- REQ-MEM-001 field order: Acceptance Criteria before Applies To (LOW)

Moved `**Applies To:** User` to immediately after the Intent paragraph. AC9 also reordered from after AC3 to its correct position after AC8. Canonical field order restored. Resolved 2026-05-18.

### RESOLVED -- REQ-SESSION-006 AC1 run-on bullet (LOW)

Removed rationale clause ("the trap reads R2 credentials from process env vars baked in at container start, so clearing DO storage first does not affect bisync") from AC1 body. Moved to a new Constraints bullet. AC1 now states only the observable contract. Resolved 2026-05-18.

### RESOLVED -- glossary: vault encryption key (LOW)

Added "Vault Encryption Key" entry to `sdd/glossary.md` defining the per-session 32-byte AES key generated by the Container DO, its storage location, transmission boundary, and forward-secret wipe on destroy(). Resolved 2026-05-18.

### RESOLVED -- REQ-VAULT-008 length / splitting candidate (LOW)

Not actioned. REQ-VAULT-008 splitting deferred until the `pending.md` entry closes (ACs 3, 4, 5 blocked on SilverBullet upstream). Will re-evaluate when those ACs resolve.

### RESOLVED -- session-lifecycle.md field order (LOW)

All 15 REQs in `sdd/session-lifecycle.md` (REQ-SESSION-001 through REQ-SESSION-015) swapped to Intent-before-Applies-To, matching the canonical render template. Pre-existing domain-wide inversion resolved in a single mechanical pass. No AC text touched. Resolved 2026-05-18.

### RESOLVED -- REQ-STOR-004 splitting candidate (MEDIUM)

Split applied: REQ-STOR-004 keeps ACs 1-7 (sync-restore + bisync baseline establishment); ACs 8-9 (port-readiness flag, WebSocket-1013-until-pre-warm) moved to new REQ-SESSION-015 in `sdd/session-lifecycle.md` with dependency edge REQ-SESSION-015 -> REQ-STOR-004. Lifecycle domain owns the readiness contract; storage domain stops carrying it. Both REQs Status: Implemented (existing code in `entrypoint.sh` + `host/src/server.ts` already implements the gate). Resolved 2026-05-18.

## 2026-05-17 - PR #380 (develop -> main) - ALL RESOLVED

### RESOLVED -- REQ-VAULT-005 AC10 leaks JS predicate (LOW)

Rewritten 2026-05-18 in the same pass that resolved the PR #385 STOR-004 split. AC10 now describes the readiness contract as observable behaviour ("disabled until per-session probe responds 200, retries until first success, switching sessions resets") with no file/component/state names and no retry-interval literal. The user-visible error code (`VAULT_UPSTREAM_UNREACHABLE`) is retained because it is an external contract, not implementation. Probe wiring detail (HEAD endpoint, 3 s cadence, `vaultReady` prop) belongs in `documentation/vault.md` if it is not already there; doc-updater will check on its next pass.

## 2026-05-15 - PR #360 (develop -> main) - ALL RESOLVED

### RESOLVED -- Bisync filter contents not REQ-governed (LOW)

REQ-STOR-011 AC4 extended 2026-05-18 to enumerate the categorical excludes: regenerable XDG tool state (e.g. wrangler state, generic `.config/` user-app caches) and vendor credential caches alongside the previously-listed package-manager caches, rclone caches, agent session logs, ephemeral agent data, and build artifacts. AC explicitly defers the per-path inventory to `entrypoint.sh` and `documentation/storage-and-sync.md` and frames itself as the verification anchor for categorical correctness. Closes the gap PR #360 surfaced.

## 2026-05-11 - PR #320 (develop -> main) - ALL RESOLVED

### RESOLVED - Possible changelog drift: 2026-05-11 entry 1 (Stop-hook review gate)
Decision: keep entry. The entry documents a real shift in *which calls
trigger review* - a behavioral observation under the "auto-demote /
behavioral observation worth recording" carveout. Strict drift rule does not
apply when an entry describes closure of a silent coverage gap that changes
what users observe their tooling doing, even if no AC text was edited.
Resolved 2026-05-11.

The other two 2026-05-11 entries (attribution gate on MCP tools; CI
monitoring rewritten) are anchored to REQ-AGENT-005, whose AC4 was updated
in this same PR by spec-reviewer to add MCP shell-tool matcher coverage -
no drift.

## 2026-05-10 — PR #307 (develop → main) — ALL RESOLVED

### RESOLVED -- AD-49 Decision section stale MCP-registration wording
Resolved by doc-updater commit `[doc-updater] fix(ad-49)` on 2026-05-10.
`documentation/decisions/README.md` AD-49 lines 719 and 723 updated to reflect
the two-path wiring: Custom+Pro via plugin.json mcpServers block; non-Custom
via entrypoint.sh injection into ~/.claude.json. ELv2 posture unchanged.

### RESOLVED -- REQ-AGENT-005 AC7 wording stale after d74cb06
Resolved by spec-reviewer commit `d60c9a4` on 2026-05-10.
AC5 and AC7 in `sdd/agents.md` updated to describe universal MCP availability
without mechanism detail; AC7 rewrites the two-path wiring as the gate contract.
