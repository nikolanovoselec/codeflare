# Review Needed

Findings escalated for human or spec-reviewer action. doc-updater never edits `sdd/`.

## 2026-05-10 — PR #307 (develop → main)

### RESOLVED -- AD-49 Decision section stale MCP-registration wording (doc-updater lane)

**File:** `documentation/decisions/README.md` (AD-49)
**Resolved by:** doc-updater commit `[doc-updater]` on 2026-05-10.

AD-49 lines 719 and 723 rewrote to reflect the two-path wiring introduced by d74cb06:
- Custom + Pro: MCP server registered via `plugin.json` `mcpServers` block (plugin loader).
- Non-Custom: `entrypoint.sh` injects into `~/.claude.json` directly.
ELv2 posture and implementation-reference lines unchanged.

---

### MEDIUM -- REQ-AGENT-005 AC7 wording does not match post-d74cb06 behavior (spec-reviewer lane)

**File:** `sdd/agents.md`, line 161 (AC7 of REQ-AGENT-005)
**Filed by:** doc-updater (2026-05-10)

**Current wording (stale):**
> "The container's `entrypoint.sh` registers the `context-mode` MCP server in
> `~/.claude.json` unconditionally (using a hardcoded version pin as fallback
> when the preseed manifest is absent)."

**Actual behavior after d74cb06:**
- Custom + Pro: MCP server registered via `mcpServers` block in preseed `plugin.json`;
  entrypoint does NOT inject into `~/.claude.json` (avoids duplicate registration).
- Non-Custom: entrypoint injects `mcpServers["context-mode"]` into `~/.claude.json`.

The word "unconditionally" is no longer accurate. doc-updater has already updated
`documentation/decisions/README.md` AD-49 to match. spec-reviewer should update
AC7 in `sdd/agents.md` to close the spec-vs-code gap.

**Proposed AC7 rewrite:**
> "The container's `entrypoint.sh` detects the preseeded plugin manifest.
> When the manifest is absent (non-Custom tier), the entrypoint injects
> `mcpServers["context-mode"]` into `~/.claude.json` using the version pin
> so `ctx_*` tools are available. When the manifest is present (Custom + Pro),
> Claude Code's plugin loader registers the MCP server via the `mcpServers`
> block in `plugin.json`; the entrypoint skips the `~/.claude.json` injection
> to avoid duplicate registration and instead adds `context-mode: true` to
> `enabledPlugins`."
