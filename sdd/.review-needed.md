# Review Needed

Findings escalated for human or doc-updater review. Spec-reviewer never edits files outside `sdd/`.

## 2026-05-10 — PR #307 (develop → main)

### MEDIUM — AD-49 Decision section has stale MCP-registration mechanism wording (doc-updater lane)

**File:** `documentation/decisions/README.md` (AD-49, lines ~717-725)

**Finding:** AD-49's Decision section states:

> The **MCP server layer** registers `mcpServers["context-mode"]` in `~/.claude.json` for every user on every session at a hardcoded version pin in `entrypoint.sh`.

and

> When the manifest is absent, the entrypoint falls back to the hardcoded version pin for the MCP registration only and skips the `enabledPlugins` entry.

After commit d74cb06 (this PR), MCP registration is now a **two-path** wiring:

1. **Custom + Pro** — plugin manifest is preseeded; the plugin loader registers the MCP server via the declarative `mcpServers` block in `plugin.json`; entrypoint.sh **does not** inject into `~/.claude.json` (would be a duplicate registration).
2. **Non-Custom** — plugin manifest absent; entrypoint.sh injects the MCP config into `~/.claude.json` directly.

Implementation reference at `entrypoint.sh:1090-1115` (conditional on `[ ! -f "$CONTEXT_MODE_MANIFEST" ]`).

**Why doc-updater, not spec-reviewer:** AD-49 is in `documentation/decisions/`. Spec-reviewer enforces the lane boundary and does not touch documentation. The corresponding REQ-AGENT-005 AC7 has already been reworded in this PR to describe the user-observable contract (MCP universally available; manifest presence selects the wiring path).

**Suggested rewrite (doc-updater):** revise AD-49 Decision lines 719 and 723 to describe both wiring paths explicitly, mirroring the new entrypoint.sh structure. The decision itself (two-layer ship: MCP universal, hooks tier-gated) is unchanged — only the **mechanism description** drifted.

**ELv2 stance unchanged:** the new `mcpServers` block in `plugin.json` declares `npx -y context-mode@<ver>` — the same fetch path AD-49 documents. Our preseed still contains only plugin metadata (our own configuration code), not context-mode source. AD-49's "License posture" subsection (lines 747-755) does not need amendment.

**Implementation references already listed in AD-49 line 766** are still accurate ("conditional `mcpServers["context-mode"]` jq merge" still happens — just only on the non-Custom branch now). A clarifying note that the conditional now also includes the inverse branch (declarative wiring via plugin.json) would help future readers.
