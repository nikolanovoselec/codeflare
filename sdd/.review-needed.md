# Review Needed

## 2026-05-12 - spec-reviewer round limit reached on HEAD 54eca72

**Round counter**: 3 of the last 3 commits are `[autonomous]` AND touch `sdd/**` AND share the same category (SDD framework alignment / second-wave triage follow-ups):

- 54eca72 `fix(sdd): align unleashed enforce_tdd semantics across preseed` (touched sdd/agents.md, sdd/glossary.md)
- 6d29c83 `fix(sdd): second-wave architect triage on framework` (touched sdd/agents.md, sdd/changes.md)
- 60090ee `fix(sdd): simplify transition rule + rename /sdd autonomous to /sdd mode` (touched sdd/agents.md, sdd/changes.md)

Per spec-discipline `## The 2-round commit cycle limit`, spec-reviewer hard-stops at >=2 same-category counted commits in the last 3. **No auto-fixes applied this run.** The counter resets when a non-agent commit lands.

### Spec-vs-shipped verification result on 54eca72

The user's described follow-up (align unleashed `enforce_tdd` wording across spec-reviewer.md, SKILL.md, sdd.md, sdd/glossary.md; tighten sdd/agents.md AC 10 to per-lane scoping) cross-checks **clean** against the authoritative rule in `preseed/agents/claude/rules/spec-discipline.md` lines 577, 596, and 601:

- spec-reviewer.md Phase 3 unleashed step 4: refuse + emit finding -> ALIGNED
- SKILL.md unleashed paragraph: refuse + emit finding -> ALIGNED
- sdd.md MODES help text: "Refuses to run when enforce_tdd: false" -> ALIGNED
- sdd/glossary.md Enforce TDD entry: "Unleashed mode refuses to run when `enforce_tdd: false`" -> ALIGNED
- sdd/glossary.md Implements Annotation entry: softened from "required" to "recommended/enforced when true" -> ALIGNED with authoritative rule lines 463-470 in spec-discipline.md
- sdd/agents.md AC 10: per-lane round counter (path-based discrimination) -> ALIGNED with spec-reviewer.md Phase 0c (counts commits touching `sdd/`) and doc-updater.md Step 0c (counts commits touching `documentation/`)

### HIGH finding - drift the HEAD commit missed (one 5th downstream location)

**File**: `preseed/agents/claude/skills/spec-driven-development/references/templates/sdd-config.yml` line 24

**Current text** (in the `enforce_tdd` comment header that ships into every new `sdd/config.yml`):

```
# Forced to true in unleashed mode (commits land on the current branch; TDD enforcement is non-negotiable).
```

**Why this is drift**: this comment contradicts the authoritative rule in `spec-discipline.md` line 601 ("unleashed *refuses to run* on a project with `enforce_tdd: false`") and the four downstream files the HEAD commit aligned. Every new project bootstrapped via `/sdd init` would receive a `config.yml` whose own header documents the now-removed "forced true" semantics - re-introducing the exact silent-override pattern the second-wave triage and HEAD commit were meant to remove.

**Severity**: HIGH (internal inconsistency in framework that ships into every new SDD project; the wording the user just spent an `[autonomous]` commit removing is preserved verbatim in the template's user-facing comment).

**Why spec-reviewer cannot auto-fix**: the file is at `preseed/agents/claude/skills/.../templates/sdd-config.yml`, which is source code (preseed assets), not `sdd/`. Per lane separation rule in spec-discipline.md, spec-reviewer never edits `preseed/` -- that is code-reviewer or developer territory.

**Proposed resolution** (for the user or code-reviewer):

```diff
-# Forced to true in unleashed mode (commits land on the current branch; TDD enforcement is non-negotiable).
+# Unleashed mode refuses to run when this is false (preserves the per-project opt-out;
+# the user flips this value manually when ready for TDD enforcement). Use `auto` mode
+# if opting out per project and you want the rest of the autonomous behavior.
```

This brings the template's user-facing comment in line with the authoritative rule and the four other downstream files. Single-line edit.

**Note on lane**: this drift is technically code-reviewer's lane (preseed source code), not spec-reviewer's. Surfacing here because the verification request was framed as "spec-vs-shipped consistency" and the spec at `sdd/glossary.md` (which spec-reviewer just verified is correct) is now inconsistent with the template comment that gets copied into every new project's `sdd/config.yml` -- so the symptom is observable inside `sdd/` even though the root cause is in `preseed/`.

---

## 2026-05-11 - PR #320 (develop -> main) - ALL RESOLVED

### RESOLVED - Possible changelog drift: 2026-05-11 entry 1 (Stop-hook review gate)
Decision: keep entry. The entry documents a real shift in *which calls
trigger review* - a behavioral observation under the "auto-demote /
behavioral observation worth recording" carveout. Strict drift rule does not
apply when an entry describes closure of a silent coverage gap that changes
what users observe their tooling doing, even if no AC text was edited.
Resolved 2026-05-11.

The other two 2026-05-11 entries (attribution gate on MCP tools; CI
monitoring rewritten) are anchored to REQ-AGENT-005, whose AC4 was updated
in this same PR by spec-reviewer to add MCP shell-tool matcher coverage -
no drift.

## 2026-05-10 — PR #307 (develop → main) — ALL RESOLVED

### RESOLVED -- AD-49 Decision section stale MCP-registration wording
Resolved by doc-updater commit `[doc-updater] fix(ad-49)` on 2026-05-10.
`documentation/decisions/README.md` AD-49 lines 719 and 723 updated to reflect
the two-path wiring: Custom+Pro via plugin.json mcpServers block; non-Custom
via entrypoint.sh injection into ~/.claude.json. ELv2 posture unchanged.

### RESOLVED -- REQ-AGENT-005 AC7 wording stale after d74cb06
Resolved by spec-reviewer commit `d60c9a4` on 2026-05-10.
AC5 and AC7 in `sdd/agents.md` updated to describe universal MCP availability
without mechanism detail; AC7 rewrites the two-path wiring as the gate contract.
