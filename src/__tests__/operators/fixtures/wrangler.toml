# CI-only runtime fixture. Never used by deployment workflows.
name = "operator-loader-fixture"
main = "loader-worker.ts"
compatibility_date = "2026-02-05"
compatibility_flags = ["nodejs_compat"]
workers_dev = false

[[worker_loaders]]
binding = "LOADER"

[[durable_objects.bindings]]
name = "REGISTRY"
class_name = "OperatorRegistry"

[[migrations]]
tag = "fixture-v1"
new_sqlite_classes = ["OperatorRegistry"]

[vars]
PARENT_SECRET = "fixture-parent-only"
